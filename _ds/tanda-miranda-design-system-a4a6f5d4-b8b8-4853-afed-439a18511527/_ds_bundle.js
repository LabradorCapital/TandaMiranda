/* @ds-bundle: {"format":4,"namespace":"TandaMirandaDesignSystem_a4a6f5","components":[{"name":"ButtonGroup","sourcePath":"components/actions/ButtonGroup.jsx"},{"name":"CopyButton","sourcePath":"components/actions/CopyButton.jsx"},{"name":"Fab","sourcePath":"components/actions/Fab.jsx"},{"name":"IconButton","sourcePath":"components/actions/IconButton.jsx"},{"name":"SegmentedControl","sourcePath":"components/actions/SegmentedControl.jsx"},{"name":"SpeedDial","sourcePath":"components/actions/SpeedDial.jsx"},{"name":"ToggleGroup","sourcePath":"components/actions/ToggleGroup.jsx"},{"name":"Badge","sourcePath":"components/core/Badge.jsx"},{"name":"Button","sourcePath":"components/core/Button.jsx"},{"name":"FeatureCard","sourcePath":"components/core/FeatureCard.jsx"},{"name":"Input","sourcePath":"components/core/Input.jsx"},{"name":"SectionHeading","sourcePath":"components/core/SectionHeading.jsx"},{"name":"Accordion","sourcePath":"components/data/Accordion.jsx"},{"name":"AnchorBadge","sourcePath":"components/data/AnchorBadge.jsx"},{"name":"Avatar","sourcePath":"components/data/Avatar.jsx"},{"name":"Card","sourcePath":"components/data/Card.jsx"},{"name":"Carousel","sourcePath":"components/data/Carousel.jsx"},{"name":"BarChart","sourcePath":"components/data/Charts.jsx"},{"name":"LineChart","sourcePath":"components/data/Charts.jsx"},{"name":"DonutChart","sourcePath":"components/data/Charts.jsx"},{"name":"Charts","sourcePath":"components/data/Charts.jsx"},{"name":"Clamp","sourcePath":"components/data/Clamp.jsx"},{"name":"DataGrid","sourcePath":"components/data/DataGrid.jsx"},{"name":"DescriptionList","sourcePath":"components/data/DescriptionList.jsx"},{"name":"Divider","sourcePath":"components/data/Divider.jsx"},{"name":"Image","sourcePath":"components/data/Image.jsx"},{"name":"List","sourcePath":"components/data/List.jsx"},{"name":"ListItem","sourcePath":"components/data/List.jsx"},{"name":"OrgChart","sourcePath":"components/data/OrgChart.jsx"},{"name":"PageHeader","sourcePath":"components/data/PageHeader.jsx"},{"name":"StatTile","sourcePath":"components/data/StatTile.jsx"},{"name":"StatusIndicator","sourcePath":"components/data/StatusIndicator.jsx"},{"name":"Table","sourcePath":"components/data/Table.jsx"},{"name":"Tag","sourcePath":"components/data/Tag.jsx"},{"name":"Timeline","sourcePath":"components/data/Timeline.jsx"},{"name":"TreeView","sourcePath":"components/data/TreeView.jsx"},{"name":"Typography","sourcePath":"components/data/Typography.jsx"},{"name":"AmountInput","sourcePath":"components/domain/AmountInput.jsx"},{"name":"CatDisclosure","sourcePath":"components/domain/CatDisclosure.jsx"},{"name":"ClabeInput","sourcePath":"components/domain/ClabeInput.jsx"},{"name":"MaskedValue","sourcePath":"components/domain/MaskedValue.jsx"},{"name":"OtpInput","sourcePath":"components/domain/OtpInput.jsx"},{"name":"Alert","sourcePath":"components/feedback/Alert.jsx"},{"name":"Backdrop","sourcePath":"components/feedback/Backdrop.jsx"},{"name":"EmptyState","sourcePath":"components/feedback/EmptyState.jsx"},{"name":"ProgressBar","sourcePath":"components/feedback/ProgressBar.jsx"},{"name":"ProgressRing","sourcePath":"components/feedback/ProgressRing.jsx"},{"name":"Skeleton","sourcePath":"components/feedback/Skeleton.jsx"},{"name":"Spinner","sourcePath":"components/feedback/Spinner.jsx"},{"name":"Toast","sourcePath":"components/feedback/Toast.jsx"},{"name":"ToastRegion","sourcePath":"components/feedback/Toast.jsx"},{"name":"Checkbox","sourcePath":"components/forms/Checkbox.jsx"},{"name":"Combobox","sourcePath":"components/forms/Combobox.jsx"},{"name":"DateInput","sourcePath":"components/forms/DateInput.jsx"},{"name":"DatePicker","sourcePath":"components/forms/DatePicker.jsx"},{"name":"Field","sourcePath":"components/forms/Field.jsx"},{"name":"FileUpload","sourcePath":"components/forms/FileUpload.jsx"},{"name":"NumberInput","sourcePath":"components/forms/NumberInput.jsx"},{"name":"RadioGroup","sourcePath":"components/forms/RadioGroup.jsx"},{"name":"Rating","sourcePath":"components/forms/Rating.jsx"},{"name":"Select","sourcePath":"components/forms/Select.jsx"},{"name":"Slider","sourcePath":"components/forms/Slider.jsx"},{"name":"Switch","sourcePath":"components/forms/Switch.jsx"},{"name":"Textarea","sourcePath":"components/forms/Textarea.jsx"},{"name":"TimePicker","sourcePath":"components/forms/TimePicker.jsx"},{"name":"TransferList","sourcePath":"components/forms/TransferList.jsx"},{"name":"AppBar","sourcePath":"components/navigation/AppBar.jsx"},{"name":"BottomNav","sourcePath":"components/navigation/BottomNav.jsx"},{"name":"Breadcrumbs","sourcePath":"components/navigation/Breadcrumbs.jsx"},{"name":"CommandPalette","sourcePath":"components/navigation/CommandPalette.jsx"},{"name":"Link","sourcePath":"components/navigation/Link.jsx"},{"name":"MegaMenu","sourcePath":"components/navigation/MegaMenu.jsx"},{"name":"Menu","sourcePath":"components/navigation/Menu.jsx"},{"name":"Pagination","sourcePath":"components/navigation/Pagination.jsx"},{"name":"ScrollProgress","sourcePath":"components/navigation/ScrollProgress.jsx"},{"name":"SideNav","sourcePath":"components/navigation/SideNav.jsx"},{"name":"Stepper","sourcePath":"components/navigation/Stepper.jsx"},{"name":"Tabs","sourcePath":"components/navigation/Tabs.jsx"},{"name":"Coachmark","sourcePath":"components/overlays/Coachmark.jsx"},{"name":"Drawer","sourcePath":"components/overlays/Drawer.jsx"},{"name":"Lightbox","sourcePath":"components/overlays/Lightbox.jsx"},{"name":"Modal","sourcePath":"components/overlays/Modal.jsx"},{"name":"Popover","sourcePath":"components/overlays/Popover.jsx"},{"name":"Tooltip","sourcePath":"components/overlays/Tooltip.jsx"}],"sourceHashes":{"components/actions/ButtonGroup.jsx":"1242aaea2b02","components/actions/CopyButton.jsx":"efacaf70dc7d","components/actions/Fab.jsx":"3c3c0b2bf071","components/actions/IconButton.jsx":"740a39a0b2fb","components/actions/SegmentedControl.jsx":"84669bc1ed86","components/actions/SpeedDial.jsx":"b59817135540","components/actions/ToggleGroup.jsx":"512f8efec349","components/core/Badge.jsx":"b804baf1682b","components/core/Button.jsx":"f9bb895bceb0","components/core/FeatureCard.jsx":"014ab602d4b1","components/core/Input.jsx":"ed31ec3b1faa","components/core/SectionHeading.jsx":"5fb3aecd4749","components/data/Accordion.jsx":"4471d8cf9f8b","components/data/AnchorBadge.jsx":"adeb252351cf","components/data/Avatar.jsx":"31bdac28e113","components/data/Card.jsx":"4516c98619b4","components/data/Carousel.jsx":"e179988ba2bc","components/data/Charts.jsx":"2c57e5203102","components/data/Clamp.jsx":"0bd7b7cb443c","components/data/DataGrid.jsx":"600fa531f4fa","components/data/DescriptionList.jsx":"77fbde605c43","components/data/Divider.jsx":"3ddd7d461426","components/data/Image.jsx":"32a8b6166412","components/data/List.jsx":"3516b0fab706","components/data/OrgChart.jsx":"bb8f24420cf5","components/data/PageHeader.jsx":"07224b574cc9","components/data/StatTile.jsx":"f9d167a83e39","components/data/StatusIndicator.jsx":"2aa6b1a3fe82","components/data/Table.jsx":"66bd306d7b16","components/data/Tag.jsx":"620682a9bf29","components/data/Timeline.jsx":"77f7d27b9f7b","components/data/TreeView.jsx":"4c5f9001e9cb","components/data/Typography.jsx":"ff15c4fe269c","components/domain/AmountInput.jsx":"419efe1c5af9","components/domain/CatDisclosure.jsx":"24e260230a13","components/domain/ClabeInput.jsx":"78d7205fda25","components/domain/MaskedValue.jsx":"58d8fadc94fd","components/domain/OtpInput.jsx":"5655a370f895","components/feedback/Alert.jsx":"40e7a0f8fa9f","components/feedback/Backdrop.jsx":"c348ca4faccf","components/feedback/EmptyState.jsx":"ad8bcbaaf5b4","components/feedback/ProgressBar.jsx":"2b95a7252131","components/feedback/ProgressRing.jsx":"2a3999429128","components/feedback/Skeleton.jsx":"69bf29ee8ccf","components/feedback/Spinner.jsx":"609ffdc66f10","components/feedback/Toast.jsx":"42c93c82ca3f","components/forms/Checkbox.jsx":"17ba3038512f","components/forms/Combobox.jsx":"cadaafca845a","components/forms/DateInput.jsx":"d1bed973a8c0","components/forms/DatePicker.jsx":"be338cc3d36c","components/forms/Field.jsx":"c4336ddabbfb","components/forms/FileUpload.jsx":"15db7b53afce","components/forms/NumberInput.jsx":"3449716e4535","components/forms/RadioGroup.jsx":"b83e2149a1de","components/forms/Rating.jsx":"ffda2ac6d3b4","components/forms/Select.jsx":"e4aa150f817e","components/forms/Slider.jsx":"afc97239898d","components/forms/Switch.jsx":"aca86efecd94","components/forms/Textarea.jsx":"13b00851b601","components/forms/TimePicker.jsx":"ee4d9f49c7b7","components/forms/TransferList.jsx":"4df4de73bb37","components/navigation/AppBar.jsx":"8831f88049dd","components/navigation/BottomNav.jsx":"ca59a1cbb466","components/navigation/Breadcrumbs.jsx":"11221b55e184","components/navigation/CommandPalette.jsx":"a36233f7da50","components/navigation/Link.jsx":"f5fdbbededfc","components/navigation/MegaMenu.jsx":"8f314597480e","components/navigation/Menu.jsx":"99d27c0b892f","components/navigation/Pagination.jsx":"898e231e6388","components/navigation/ScrollProgress.jsx":"e3fee342b6b3","components/navigation/SideNav.jsx":"17b507af7d18","components/navigation/Stepper.jsx":"197ce53f0f39","components/navigation/Tabs.jsx":"dc90b4102a54","components/overlays/Coachmark.jsx":"cb1db43dd266","components/overlays/Drawer.jsx":"c80f863d2f3f","components/overlays/Lightbox.jsx":"7c9a5c5f18e8","components/overlays/Modal.jsx":"5d7fb0ebbb05","components/overlays/Popover.jsx":"7e6ed51f6483","components/overlays/Tooltip.jsx":"f73999173de9","showcase/app.jsx":"bb8fe2bce953","showcase/sections-a.jsx":"c05fbd921823","showcase/sections-b.jsx":"ed5baacdc1dc","ui_kits/app/Dashboard.jsx":"c1812865cf68","ui_kits/app/tweaks-panel.jsx":"4f181eb354cd","ui_kits/ops/OpsConsole.jsx":"0a6a5a63c4b9","ui_kits/website/Homepage.jsx":"fd4a4bd70b0c"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.TandaMirandaDesignSystem_a4a6f5 = window.TandaMirandaDesignSystem_a4a6f5 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/actions/ButtonGroup.jsx
try { (() => {
/**
 * ButtonGroup — attached set of related actions (export options, view
 * modes). role="group" + mandatory accessible name. Register-aware:
 * pill ends on marketing, md on app. For single-choice use
 * SegmentedControl; for multi-toggle use ToggleGroup.
 */
function ButtonGroup({
  items = [],
  label,
  size = "md",
  disabled = false,
  style
}) {
  const [hover, setHover] = React.useState(-1);
  return /*#__PURE__*/React.createElement("span", {
    role: "group",
    "aria-label": label,
    style: {
      display: "inline-flex",
      borderRadius: "var(--control-radius)",
      border: "var(--border-width-md) solid var(--color-border-strong)",
      overflow: "hidden",
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, items.map((it, i) => {
    const dis = disabled || it.disabled;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      disabled: dis,
      "aria-pressed": it.selected != null ? it.selected : undefined,
      onClick: it.onClick,
      onMouseEnter: () => setHover(i),
      onMouseLeave: () => setHover(-1),
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "var(--space-2)",
        height: `calc(var(--control-height-${size}) - 3px)`,
        paddingInline: `var(--control-pad-x-${size})`,
        font: `var(--control-font-weight) var(--control-font-size-${size})/1.2 var(--font-sans)`,
        letterSpacing: "var(--control-tracking)",
        textTransform: "var(--control-transform)",
        color: it.selected ? "var(--color-fg-on-solid)" : "var(--color-fg-brand)",
        background: it.selected ? "var(--color-bg-brand-solid)" : hover === i && !dis ? "var(--color-bg-hover)" : "var(--color-bg-surface)",
        border: "none",
        borderLeft: i > 0 ? "var(--border-width-md) solid var(--color-border-strong)" : "none",
        cursor: dis ? "default" : "pointer",
        whiteSpace: "nowrap",
        transition: "background var(--duration-fast-2) var(--ease-standard)"
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex"
      }
    }, it.icon), it.label);
  }));
}
Object.assign(__ds_scope, { ButtonGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ButtonGroup.jsx", error: String((e && e.message) || e) }); }

// components/actions/CopyButton.jsx
try { (() => {
/**
 * CopyButton — copy-to-clipboard with visible confirmation (icon →
 * check + "Copiado") and a polite live region. Use next to folios,
 * CLABEs, referencias de pago. For masked values use MaskedValue,
 * which pairs copy with the auditable reveal.
 */
function CopyButton({
  value,
  label = "Copiar",
  showLabel = false,
  size = "sm",
  onCopy,
  style
}) {
  const [copied, setCopied] = React.useState(false);
  const copy = async () => {
    let ok = false;
    try {
      await navigator.clipboard.writeText(value);
      ok = true;
    } catch (e) {
      try {
        const ta = document.createElement("textarea");
        ta.value = value;
        ta.style.position = "fixed";
        ta.style.opacity = "0";
        document.body.appendChild(ta);
        ta.select();
        ok = document.execCommand("copy");
        document.body.removeChild(ta);
      } catch (e2) {
        ok = false;
      }
    }
    if (ok) {
      setCopied(true);
      onCopy && onCopy(value);
      setTimeout(() => setCopied(false), 1600);
    }
  };
  const h = {
    sm: 28,
    md: 34
  }[size] || 28;
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": copied ? "Copiado" : label,
    title: showLabel ? undefined : copied ? "Copiado" : label,
    onClick: copy,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 6,
      height: h,
      minWidth: h,
      paddingInline: showLabel ? 10 : 0,
      font: "600 12px/1 var(--font-sans)",
      color: copied ? "var(--color-fg-success)" : "var(--color-fg-muted)",
      background: "transparent",
      border: showLabel ? "1.5px solid var(--color-border-strong)" : "none",
      borderRadius: "var(--radius-sm)",
      cursor: "pointer",
      transition: "color var(--duration-fast-2) var(--ease-standard)",
      ...style
    }
  }, copied ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  })) : /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "9",
    y: "9",
    width: "13",
    height: "13",
    rx: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"
  })), showLabel && (copied ? "Copiado" : label), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, copied ? "Copiado al portapapeles" : ""));
}
Object.assign(__ds_scope, { CopyButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/CopyButton.jsx", error: String((e && e.message) || e) }); }

// components/actions/Fab.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Fab — floating action button for THE one primary action of a mobile
 * screen (max one per view). Circle (icon-only, label mandatory as
 * aria-label + tooltip) or extended pill (icon + visible label).
 * position="fixed" pins bottom-right above content at z-sticky.
 */
function Fab({
  icon,
  label,
  extended = false,
  variant = "primary",
  size = "md",
  position = "static",
  offset = 24,
  disabled = false,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  const dim = size === "sm" ? 44 : 56;
  const colors = variant === "navy" ? {
    background: hover ? "var(--color-bg-brand-solid-hover)" : "var(--color-bg-brand-solid)"
  } : {
    background: hover ? "var(--button-bg-primary-hover)" : "var(--button-bg-primary)"
  };
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    "aria-label": extended ? undefined : label,
    title: extended ? undefined : label,
    disabled: disabled,
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      gap: "var(--space-2)",
      height: dim,
      minWidth: dim,
      paddingInline: extended ? 20 : 0,
      borderRadius: "var(--radius-pill)",
      border: "none",
      color: "var(--color-fg-on-solid)",
      font: `700 14px/1.2 var(--font-sans)`,
      boxShadow: hover && !disabled ? "var(--elevation-400)" : "var(--elevation-300)",
      cursor: disabled ? "default" : "pointer",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "background var(--duration-fast-2) var(--ease-standard), box-shadow var(--duration-fast-2) var(--ease-standard)",
      ...(position === "fixed" ? {
        position: "fixed",
        right: offset,
        bottom: offset,
        zIndex: "var(--z-sticky)"
      } : {}),
      ...colors,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex"
    }
  }, icon), extended && label);
}
Object.assign(__ds_scope, { Fab });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/Fab.jsx", error: String((e && e.message) || e) }); }

// components/actions/IconButton.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * IconButton — square icon-only action. MUST carry an accessible name
 * (label prop → aria-label + title tooltip). Hit area follows control
 * heights (≥32px; 44+ on touch).
 */
const IconButton = React.forwardRef(function IconButton({
  icon,
  label,
  variant = "ghost",
  size = "md",
  disabled = false,
  selected = false,
  onClick,
  style,
  ...rest
}, ref) {
  const [hover, setHover] = React.useState(false);
  const variants = {
    ghost: {
      background: selected ? "var(--color-bg-selected)" : hover ? "var(--color-bg-hover)" : "transparent",
      color: selected ? "var(--color-fg-brand)" : "var(--color-fg-muted)"
    },
    outline: {
      background: hover ? "var(--color-bg-hover)" : "transparent",
      color: "var(--color-fg-brand)",
      boxShadow: "inset 0 0 0 1.5px var(--color-border-strong)"
    },
    solid: {
      background: hover ? "var(--color-bg-brand-solid-hover)" : "var(--color-bg-brand-solid)",
      color: "var(--color-fg-on-solid)"
    },
    inverse: {
      background: hover ? "var(--tm-alpha-white-200)" : "transparent",
      color: "var(--color-fg-inverse)"
    }
  };
  return /*#__PURE__*/React.createElement("button", _extends({
    ref: ref,
    type: "button",
    "aria-label": label,
    title: label,
    "aria-pressed": rest["aria-pressed"],
    disabled: disabled,
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: `var(--control-height-${size})`,
      height: `var(--control-height-${size})`,
      flexShrink: 0,
      border: "none",
      borderRadius: "var(--control-radius)",
      cursor: disabled ? "default" : "pointer",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "background var(--duration-fast-2) var(--ease-standard), color var(--duration-fast-2) var(--ease-standard)",
      ...(variants[variant] || variants.ghost),
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex"
    }
  }, icon));
});
Object.assign(__ds_scope, { IconButton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/IconButton.jsx", error: String((e && e.message) || e) }); }

// components/actions/SegmentedControl.jsx
try { (() => {
/**
 * SegmentedControl — single-select among 2–5 short peer options
 * (view switches, density toggle). Radio semantics: role="radiogroup",
 * one tab stop, arrows move + select.
 */
function SegmentedControl({
  options = [],
  value,
  onChange,
  label,
  size = "sm",
  disabled = false,
  style
}) {
  const refs = React.useRef([]);
  const idx = options.findIndex(o => o.value === value);
  const onKeyDown = e => {
    const dir = e.key === "ArrowRight" || e.key === "ArrowDown" ? 1 : e.key === "ArrowLeft" || e.key === "ArrowUp" ? -1 : 0;
    if (!dir) return;
    e.preventDefault();
    const next = (idx + dir + options.length) % options.length;
    onChange && onChange(options[next].value);
    const el = refs.current[next];
    if (el) el.focus();
  };
  return /*#__PURE__*/React.createElement("div", {
    role: "radiogroup",
    "aria-label": label,
    onKeyDown: onKeyDown,
    style: {
      display: "inline-flex",
      padding: 3,
      gap: 2,
      background: "var(--color-bg-muted)",
      borderRadius: "var(--control-radius)",
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, options.map((o, i) => {
    const on = o.value === value;
    return /*#__PURE__*/React.createElement("button", {
      key: String(o.value),
      ref: el => refs.current[i] = el,
      type: "button",
      role: "radio",
      "aria-checked": on,
      tabIndex: on || idx < 0 && i === 0 ? 0 : -1,
      disabled: disabled,
      onClick: () => onChange && onChange(o.value),
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "var(--space-1)",
        height: `calc(var(--control-height-${size}) - 6px)`,
        paddingInline: "var(--space-3)",
        font: `600 var(--control-font-size-${size})/1.2 var(--font-sans)`,
        letterSpacing: "var(--control-tracking)",
        textTransform: "var(--control-transform)",
        color: on ? "var(--color-fg-brand)" : "var(--color-fg-muted)",
        background: on ? "var(--color-bg-surface)" : "transparent",
        border: "none",
        borderRadius: "calc(var(--control-radius) - 2px)",
        boxShadow: on ? "var(--elevation-100)" : "none",
        cursor: disabled ? "default" : "pointer",
        transition: "background var(--duration-fast-2) var(--ease-standard), color var(--duration-fast-2) var(--ease-standard)",
        whiteSpace: "nowrap"
      }
    }, o.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex"
      }
    }, o.icon), o.label);
  }));
}
Object.assign(__ds_scope, { SegmentedControl });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/SegmentedControl.jsx", error: String((e && e.message) || e) }); }

// components/actions/SpeedDial.jsx
try { (() => {
/**
 * SpeedDial — a Fab that fans out 2–5 labeled mini-actions. Every
 * action shows a visible label chip (never icon-only hover-mystery).
 * Keyboard: Enter/Space opens, ↑↓ move, Esc closes and returns focus.
 * If the actions are co-equal to the primary one, prefer a visible
 * toolbar instead.
 */
function SpeedDial({
  icon,
  label,
  actions = [],
  direction = "up",
  style
}) {
  const [open, setOpen] = React.useState(false);
  const rootRef = React.useRef(null);
  const mainRef = React.useRef(null);
  const itemRefs = React.useRef([]);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    const onEsc = e => {
      if (e.key === "Escape") {
        setOpen(false);
        if (mainRef.current) mainRef.current.focus();
      }
    };
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onEsc);
    return () => {
      document.removeEventListener("mousedown", onDoc);
      document.removeEventListener("keydown", onEsc);
    };
  }, [open]);
  React.useEffect(() => {
    if (open && itemRefs.current[0]) itemRefs.current[0].focus();
  }, [open]);
  const onListKey = e => {
    const idx = itemRefs.current.findIndex(el => el === document.activeElement);
    if (e.key === "ArrowUp" || e.key === "ArrowDown") {
      e.preventDefault();
      const d = e.key === "ArrowUp" === (direction === "up") ? 1 : -1;
      const next = (idx + d + actions.length) % actions.length;
      itemRefs.current[next].focus();
    }
  };
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: "inline-flex",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    ref: mainRef,
    type: "button",
    "aria-label": label,
    title: label,
    "aria-expanded": open,
    "aria-haspopup": "true",
    onClick: () => setOpen(!open),
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 56,
      height: 56,
      borderRadius: "50%",
      border: "none",
      background: open ? "var(--color-bg-brand-solid)" : "var(--button-bg-primary)",
      color: "var(--color-fg-on-solid)",
      boxShadow: "var(--elevation-300)",
      cursor: "pointer",
      transition: "background var(--duration-fast-2) var(--ease-standard), transform var(--duration-fast-2) var(--ease-standard)",
      transform: open ? "rotate(45deg)" : "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex"
    }
  }, icon)), open && /*#__PURE__*/React.createElement("span", {
    role: "menu",
    "aria-label": label,
    onKeyDown: onListKey,
    style: {
      position: "absolute",
      [direction === "up" ? "bottom" : "top"]: "calc(100% + 10px)",
      right: 0,
      display: "flex",
      flexDirection: direction === "up" ? "column-reverse" : "column",
      gap: 10,
      alignItems: "flex-end",
      zIndex: "var(--z-popover)"
    }
  }, actions.map((a, i) => /*#__PURE__*/React.createElement("span", {
    key: a.label,
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "600 12.5px/1.2 var(--font-sans)",
      color: "var(--color-fg-default)",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-sm)",
      padding: "5px 10px",
      boxShadow: "var(--elevation-100)",
      whiteSpace: "nowrap"
    }
  }, a.label), /*#__PURE__*/React.createElement("button", {
    ref: el => itemRefs.current[i] = el,
    type: "button",
    role: "menuitem",
    "aria-label": a.label,
    onClick: () => {
      setOpen(false);
      a.onSelect && a.onSelect();
      if (mainRef.current) mainRef.current.focus();
    },
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 42,
      height: 42,
      borderRadius: "50%",
      border: "1px solid var(--color-border-subtle)",
      background: "var(--color-bg-surface-raised)",
      color: "var(--color-fg-brand)",
      boxShadow: "var(--elevation-200)",
      cursor: "pointer",
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex"
    }
  }, a.icon))))));
}
Object.assign(__ds_scope, { SpeedDial });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/SpeedDial.jsx", error: String((e && e.message) || e) }); }

// components/actions/ToggleGroup.jsx
try { (() => {
/**
 * ToggleGroup — multi-select attached toggles (filters, format bold/
 * italic). Each button carries aria-pressed; state is never color
 * alone (pressed = solid fill + check-capable slot). Single-choice
 * belongs to SegmentedControl.
 */
function ToggleGroup({
  options = [],
  value = [],
  onChange,
  label,
  size = "sm",
  disabled = false,
  style
}) {
  const [hover, setHover] = React.useState(-1);
  const toggle = v => {
    if (!onChange) return;
    onChange(value.includes(v) ? value.filter(x => x !== v) : [...value, v]);
  };
  return /*#__PURE__*/React.createElement("span", {
    role: "group",
    "aria-label": label,
    style: {
      display: "inline-flex",
      borderRadius: "var(--control-radius)",
      border: "var(--border-width-md) solid var(--color-border-strong)",
      overflow: "hidden",
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, options.map((o, i) => {
    const on = value.includes(o.value);
    const dis = disabled || o.disabled;
    return /*#__PURE__*/React.createElement("button", {
      key: String(o.value),
      type: "button",
      "aria-pressed": on,
      "aria-label": o.iconOnly ? String(o.label) : undefined,
      title: o.iconOnly ? String(o.label) : undefined,
      disabled: dis,
      onClick: () => toggle(o.value),
      onMouseEnter: () => setHover(i),
      onMouseLeave: () => setHover(-1),
      style: {
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        gap: "var(--space-1)",
        height: `calc(var(--control-height-${size}) - 3px)`,
        minWidth: `calc(var(--control-height-${size}) - 3px)`,
        paddingInline: o.iconOnly ? 0 : "var(--control-pad-x-sm)",
        font: `600 var(--control-font-size-${size})/1.2 var(--font-sans)`,
        letterSpacing: "var(--control-tracking)",
        textTransform: "var(--control-transform)",
        color: on ? "var(--color-fg-on-solid)" : "var(--color-fg-muted)",
        background: on ? "var(--color-bg-brand-solid)" : hover === i && !dis ? "var(--color-bg-hover)" : "var(--color-bg-surface)",
        border: "none",
        borderLeft: i > 0 ? "var(--border-width-md) solid var(--color-border-strong)" : "none",
        cursor: dis ? "default" : "pointer",
        whiteSpace: "nowrap",
        transition: "background var(--duration-fast-2) var(--ease-standard), color var(--duration-fast-2) var(--ease-standard)"
      }
    }, o.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex"
      }
    }, o.icon), !o.iconOnly && o.label);
  }));
}
Object.assign(__ds_scope, { ToggleGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ToggleGroup.jsx", error: String((e && e.message) || e) }); }

// components/core/Badge.jsx
try { (() => {
const tones = {
  red: {
    background: "var(--tm-red-050)",
    color: "var(--tm-red-700)"
  },
  navy: {
    background: "var(--tm-navy-050)",
    color: "var(--tm-navy-900)"
  },
  solidRed: {
    background: "var(--tm-red-600)",
    color: "var(--tm-white)"
  },
  solidNavy: {
    background: "var(--tm-navy-900)",
    color: "var(--tm-white)"
  }
};

/**
 * Small pill label — used for the "No.1" style emphasis chip, tags, or status labels.
 * Not present verbatim on the source site (no product UI to draw from) — included as
 * an intentional addition for consumers who need lightweight tagging. See readme.
 */
function Badge({
  children,
  tone = "navy"
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      fontFamily: "var(--font-sans)",
      fontWeight: 700,
      fontSize: 12,
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase",
      borderRadius: "var(--radius-pill)",
      padding: "6px 14px",
      ...tones[tone]
    }
  }, children);
}
Object.assign(__ds_scope, { Badge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Badge.jsx", error: String((e && e.message) || e) }); }

// components/core/Button.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/* Keyframes injected once (self-contained; no stylesheet dependency). */
function ensureButtonKeyframes() {
  if (typeof document === "undefined" || document.getElementById("tm-kf-spin")) return;
  const s = document.createElement("style");
  s.id = "tm-kf-spin";
  s.textContent = "@keyframes tm-spin{to{transform:rotate(360deg)}}";
  document.head.appendChild(s);
}
const VARIANT_ALIASES = {
  outline: "secondary",
  ghost: "tertiary",
  brand: "navy"
};

/**
 * Button — triggers an action or event. Not for navigation (use href
 * to render true link semantics styled as a button).
 *
 * Register-aware: on marketing surfaces (default) it is the brand's
 * uppercase red pill CTA; inside [data-surface="app"] it becomes a
 * sentence-case md-radius control at reference sizing (32/40/48).
 *
 * Rules: max ONE primary per view/section · verb-first labels ≤3 words
 * ("Crear solicitud", never "Sí"/"OK") · label never wraps · loading
 * preserves width and blocks duplicate activation.
 */
const Button = React.forwardRef(function Button({
  children,
  variant = "primary",
  size = "md",
  disabled = false,
  loading = false,
  fullWidth = false,
  iconStart,
  iconEnd,
  href,
  target,
  type = "button",
  onClick,
  style,
  ...rest
}, ref) {
  const [hover, setHover] = React.useState(false);
  const [pressed, setPressed] = React.useState(false);
  React.useEffect(ensureButtonKeyframes, []);
  const v = VARIANT_ALIASES[variant] || variant;
  const blocked = disabled || loading;
  const variants = {
    primary: {
      background: pressed || hover ? "var(--button-bg-primary-hover)" : "var(--button-bg-primary)",
      color: "var(--button-fg-primary)"
    },
    navy: {
      background: pressed || hover ? "var(--color-bg-brand-solid-hover)" : "var(--color-bg-brand-solid)",
      color: "var(--color-fg-on-solid)"
    },
    secondary: {
      background: hover || pressed ? "var(--color-bg-hover)" : "transparent",
      color: "var(--color-fg-brand)",
      boxShadow: "inset 0 0 0 1.5px currentColor"
    },
    tertiary: {
      background: hover || pressed ? "var(--color-bg-hover)" : "transparent",
      color: "var(--color-fg-brand)"
    },
    destructive: {
      background: pressed || hover ? "var(--color-bg-danger-solid-hover)" : "var(--color-bg-danger-solid)",
      color: "var(--color-fg-on-solid)"
    },
    link: {
      background: "transparent",
      color: hover ? "var(--color-fg-link-hover)" : "var(--color-fg-link)",
      textDecoration: "underline",
      textDecorationColor: "var(--color-link-underline)",
      textUnderlineOffset: 2,
      height: "auto",
      padding: 0
    }
  };
  const base = {
    position: "relative",
    display: fullWidth ? "flex" : "inline-flex",
    width: fullWidth ? "100%" : undefined,
    alignItems: "center",
    justifyContent: "center",
    gap: "var(--space-2)",
    boxSizing: "border-box",
    height: `var(--control-height-${size})`,
    paddingInline: v === "link" ? 0 : `var(--control-pad-x-${size})`,
    fontFamily: "var(--font-sans)",
    fontSize: `var(--control-font-size-${size})`,
    fontWeight: "var(--control-font-weight)",
    letterSpacing: "var(--control-tracking)",
    textTransform: v === "link" ? "none" : "var(--control-transform)",
    whiteSpace: "nowrap",
    border: "none",
    borderRadius: "var(--control-radius)",
    cursor: blocked ? "default" : "pointer",
    opacity: disabled ? "var(--opacity-disabled)" : 1,
    transition: "background-color var(--duration-fast-2) var(--ease-standard), color var(--duration-fast-2) var(--ease-standard)",
    ...(variants[v] || variants.primary),
    ...style
  };
  const content = /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-2)",
      visibility: loading ? "hidden" : "visible"
    }
  }, iconStart, children, iconEnd), loading && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: "1em",
      height: "1em",
      border: "2px solid currentColor",
      borderRightColor: "transparent",
      borderRadius: "50%",
      animation: "tm-spin 0.8s linear infinite"
    }
  })));
  const shared = {
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setPressed(false);
    },
    onMouseDown: () => setPressed(true),
    onMouseUp: () => setPressed(false),
    style: base,
    "aria-busy": loading || undefined,
    ...rest
  };
  if (href && !blocked) {
    return /*#__PURE__*/React.createElement("a", _extends({
      ref: ref,
      href: href,
      target: target,
      rel: target === "_blank" ? "noreferrer" : undefined,
      onClick: onClick
    }, shared), content);
  }
  return /*#__PURE__*/React.createElement("button", _extends({
    ref: ref,
    type: type,
    disabled: disabled,
    onClick: blocked ? undefined : onClick
  }, shared), content);
});
Object.assign(__ds_scope, { Button });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Button.jsx", error: String((e && e.message) || e) }); }

// components/core/FeatureCard.jsx
try { (() => {
/**
 * Icon + label + description feature block, matching the homepage's four-up
 * "RAPIDEZ / COMUNIDAD / CRÉDITO / ASESORÍA" row. Icon is any node — pass a Lucide
 * icon or an <img>. Stack several in a flex/grid row with even gaps.
 */
function FeatureCard({
  icon,
  label,
  description,
  align = "center"
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: align === "center" ? "center" : "flex-start",
      textAlign: align,
      gap: 10,
      maxWidth: 220
    }
  }, icon ? /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--tm-red-600)",
      width: 32,
      height: 32,
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, icon) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontWeight: 700,
      fontSize: 13,
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase",
      color: "var(--tm-navy-900)"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      lineHeight: 1.5,
      color: "var(--color-text-secondary)"
    }
  }, description));
}
Object.assign(__ds_scope, { FeatureCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/FeatureCard.jsx", error: String((e && e.message) || e) }); }

// components/core/Input.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Input — single-line text control. Register-aware (pill on marketing,
 * md radius inside [data-surface="app"]).
 *
 * Rules: labels live in <Field>, always visible above — placeholder is
 * for format hints only ("juan@ejemplo.com") and never required to
 * complete the task. Use the right type/inputMode/autoComplete for
 * mobile keyboards (WCAG 1.3.5). Paste is never blocked (WCAG 3.3.8).
 * Validation timing: on blur for format, on submit for completeness.
 */
const Input = React.forwardRef(function Input({
  placeholder,
  type = "text",
  size = "md",
  value,
  defaultValue,
  onChange,
  onBlur,
  onFocus,
  invalid = false,
  disabled = false,
  readOnly = false,
  mono = false,
  align = "left",
  iconStart,
  addonEnd,
  fullWidth = false,
  style,
  ...rest
}, ref) {
  const [focus, setFocus] = React.useState(false);
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  const wrap = {
    display: fullWidth ? "flex" : "inline-flex",
    width: fullWidth ? "100%" : undefined,
    alignItems: "center",
    gap: "var(--space-2)",
    boxSizing: "border-box",
    height: `var(--control-height-${size})`,
    paddingInline: "var(--space-3)",
    background: readOnly ? "var(--color-bg-subtle)" : disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
    border: `${invalid || focus ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
    borderRadius: "var(--input-radius)",
    minWidth: fullWidth ? 0 : 220,
    opacity: disabled ? "var(--opacity-disabled)" : 1,
    transition: "border-color var(--duration-fast-2) var(--ease-standard)",
    cursor: disabled ? "default" : "text",
    ...style
  };
  return /*#__PURE__*/React.createElement("span", {
    style: wrap
  }, iconStart && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex",
      color: "var(--color-fg-subtle)",
      flexShrink: 0
    }
  }, iconStart), /*#__PURE__*/React.createElement("input", _extends({
    ref: ref,
    type: type,
    placeholder: placeholder,
    value: value,
    defaultValue: defaultValue,
    onChange: onChange,
    disabled: disabled,
    readOnly: readOnly,
    "aria-invalid": invalid || undefined,
    onFocus: e => {
      setFocus(true);
      onFocus && onFocus(e);
    },
    onBlur: e => {
      setFocus(false);
      onBlur && onBlur(e);
    },
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: mono ? "var(--font-mono)" : "var(--font-sans)",
      fontSize,
      fontVariantNumeric: "var(--numeric-tabular)",
      textAlign: align,
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }, rest)), addonEnd && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      color: "var(--color-fg-subtle)",
      fontSize: fontSize - 1,
      flexShrink: 0,
      fontFamily: "var(--font-sans)"
    }
  }, addonEnd));
});
Object.assign(__ds_scope, { Input });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/Input.jsx", error: String((e && e.message) || e) }); }

// components/core/SectionHeading.jsx
try { (() => {
/**
 * Section heading pattern used across the site: an optional serif-italic display line
 * ("¡Alcancemos juntos tus metas!") paired with a bold sans headline where one word/phrase
 * is picked out in brand red (e.g. "siempre eres el No.1").
 */
function SectionHeading({
  eyebrowScript,
  title,
  accent,
  align = "center",
  inverse = false
}) {
  const color = inverse ? "var(--tm-white)" : "var(--tm-navy-900)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: align,
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, eyebrowScript ? /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-display-sm)",
      color
    }
  }, eyebrowScript) : null, title ? /*#__PURE__*/React.createElement("h2", {
    style: {
      font: "var(--text-h2)",
      color,
      margin: 0
    }
  }, title, accent ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--tm-red-600)"
    }
  }, " ", accent) : null) : null);
}
Object.assign(__ds_scope, { SectionHeading });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/core/SectionHeading.jsx", error: String((e && e.message) || e) }); }

// components/data/Accordion.jsx
try { (() => {
/**
 * Accordion — expandable sections. Header is a full-width button with
 * aria-expanded + rotating chevron. Don't hide critical or legal
 * content collapsed by default. singleExpand for exclusive open.
 */
function Accordion({
  items = [],
  defaultOpen = [],
  singleExpand = false,
  style
}) {
  const [open, setOpen] = React.useState(defaultOpen);
  const auto = React.useId();
  const toggle = id => {
    setOpen(prev => prev.includes(id) ? prev.filter(x => x !== id) : singleExpand ? [id] : [...prev, id]);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      fontFamily: "var(--font-sans)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      overflow: "hidden",
      ...style
    }
  }, items.map((it, i) => {
    const on = open.includes(it.id);
    return /*#__PURE__*/React.createElement("div", {
      key: it.id,
      style: {
        borderTop: i > 0 ? "1px solid var(--color-border-subtle)" : "none"
      }
    }, /*#__PURE__*/React.createElement("h3", {
      style: {
        margin: 0
      }
    }, /*#__PURE__*/React.createElement("button", {
      type: "button",
      "aria-expanded": on,
      "aria-controls": `${auto}-${it.id}`,
      onClick: () => toggle(it.id),
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-3)",
        width: "100%",
        padding: "var(--space-4)",
        font: "600 15px/1.4 var(--font-sans)",
        textAlign: "left",
        color: "var(--color-fg-default)",
        background: on ? "var(--color-bg-subtle)" : "transparent",
        border: "none",
        cursor: "pointer",
        transition: "background var(--duration-fast-2) var(--ease-standard)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, it.title), /*#__PURE__*/React.createElement("svg", {
      "aria-hidden": "true",
      width: "16",
      height: "16",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "var(--color-fg-subtle)",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      style: {
        flexShrink: 0,
        transform: on ? "rotate(180deg)" : "none",
        transition: "transform var(--duration-moderate) var(--ease-standard)"
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "m6 9 6 6 6-6"
    })))), on && /*#__PURE__*/React.createElement("div", {
      id: `${auto}-${it.id}`,
      role: "region",
      style: {
        padding: "0 var(--space-4) var(--space-4)",
        font: "var(--text-sm)",
        color: "var(--color-fg-muted)"
      }
    }, it.content));
  }));
}
Object.assign(__ds_scope, { Accordion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Accordion.jsx", error: String((e && e.message) || e) }); }

// components/data/AnchorBadge.jsx
try { (() => {
/**
 * AnchorBadge — dot or count anchored to an icon/avatar (unread
 * notifications). Count caps at `max` ("99+"). The count is never the
 * only signal: srLabel describes it for assistive tech ("3
 * notificaciones sin leer"). Chip-style labels live in Badge/Tag.
 */
function AnchorBadge({
  children,
  count,
  dot = false,
  max = 99,
  tone = "accent",
  srLabel,
  hidden = false,
  style
}) {
  const bg = {
    accent: "var(--color-bg-accent-solid)",
    navy: "var(--color-bg-brand-solid)",
    success: "var(--color-bg-success-solid)",
    neutral: "var(--tm-neutral-500)"
  }[tone] || "var(--color-bg-accent-solid)";
  const show = !hidden && (dot || typeof count === "number" && count > 0);
  const text = typeof count === "number" ? count > max ? `${max}+` : String(count) : "";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-flex",
      ...style
    }
  }, children, show && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: dot ? -2 : -6,
      right: dot ? -2 : -8,
      minWidth: dot ? 9 : 17,
      height: dot ? 9 : 17,
      boxSizing: "border-box",
      padding: dot ? 0 : "0 4px",
      borderRadius: "var(--radius-pill)",
      background: bg,
      color: "var(--color-fg-on-solid)",
      font: "700 10.5px/17px var(--font-sans)",
      fontVariantNumeric: "var(--numeric-tabular)",
      textAlign: "center",
      border: "2px solid var(--color-bg-page)"
    }
  }, !dot && text), show && srLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, srLabel));
}
Object.assign(__ds_scope, { AnchorBadge });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/AnchorBadge.jsx", error: String((e && e.message) || e) }); }

// components/data/Avatar.jsx
try { (() => {
/**
 * Avatar — image → initials (deterministic tint from name) → icon.
 * Sizes 24/32/40/64. Optional status dot slot.
 */
function Avatar({
  name = "",
  src,
  size = 40,
  status,
  style
}) {
  const [broken, setBroken] = React.useState(false);
  const initials = name.split(/\s+/).filter(Boolean).slice(0, 2).map(w => w[0]).join("").toUpperCase();
  const tints = [["var(--tm-navy-100)", "var(--tm-navy-900)"], ["var(--tm-blue-100)", "var(--tm-blue-800)"], ["var(--tm-red-100)", "var(--tm-red-800)"], ["var(--tm-green-100)", "var(--tm-green-800)"], ["var(--tm-amber-100)", "var(--tm-amber-800)"]];
  let hash = 0;
  for (let i = 0; i < name.length; i++) hash = (hash * 31 + name.charCodeAt(i)) % 997;
  const [bg, fg] = tints[hash % tints.length];
  const statusColor = {
    online: "var(--color-bg-success-solid)",
    away: "var(--color-bg-warning-solid)",
    offline: "var(--tm-neutral-400)"
  }[status];
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-flex",
      flexShrink: 0,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    role: "img",
    "aria-label": name || "Usuario",
    style: {
      width: size,
      height: size,
      borderRadius: "50%",
      overflow: "hidden",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      background: bg,
      color: fg,
      font: `700 ${Math.round(size * 0.38)}px/1 var(--font-sans)`,
      letterSpacing: "0.02em",
      userSelect: "none"
    }
  }, src && !broken ? /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: "",
    onError: () => setBroken(true),
    style: {
      width: "100%",
      height: "100%",
      objectFit: "cover"
    }
  }) : initials ? initials : /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: size * 0.55,
    height: size * 0.55,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M19 21v-2a4 4 0 0 0-4-4H9a4 4 0 0 0-4 4v2"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "7",
    r: "4"
  }))), statusColor && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      right: 0,
      bottom: 0,
      width: Math.max(8, size * 0.24),
      height: Math.max(8, size * 0.24),
      borderRadius: "50%",
      background: statusColor,
      border: "2px solid var(--color-bg-surface)",
      boxSizing: "border-box"
    }
  }));
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/data/Card.jsx
try { (() => {
/**
 * Card — product container (marketing feature blocks stay with
 * FeatureCard). A container, not an interaction: pass onClick only
 * for a single whole-card target, with secondary actions layered in
 * the header. Padding/radius follow density & register tokens.
 */
function Card({
  title,
  meta,
  actions,
  children,
  footer,
  interactive = false,
  onClick,
  padding,
  style
}) {
  const [hover, setHover] = React.useState(false);
  const clickable = interactive || !!onClick;
  return /*#__PURE__*/React.createElement("div", {
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    role: onClick ? "button" : undefined,
    tabIndex: onClick ? 0 : undefined,
    onKeyDown: onClick ? e => {
      if (e.key === "Enter" || e.key === " ") {
        e.preventDefault();
        onClick(e);
      }
    } : undefined,
    style: {
      display: "flex",
      flexDirection: "column",
      background: "var(--color-bg-surface)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--card-radius)",
      boxShadow: clickable && hover ? "var(--elevation-200)" : "var(--elevation-0)",
      cursor: clickable ? "pointer" : "default",
      transition: "box-shadow var(--duration-fast-2) var(--ease-standard)",
      fontFamily: "var(--font-sans)",
      boxSizing: "border-box",
      minWidth: 0,
      ...style
    }
  }, (title || actions || meta) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: "var(--space-3)",
      padding: `var(--card-padding) var(--card-padding) 0`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 16px/1.35 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), meta && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      marginTop: 2
    }
  }, meta)), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-1)",
      flexShrink: 0
    },
    onClick: e => e.stopPropagation()
  }, actions)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: padding !== undefined ? padding : "var(--card-padding)",
      flex: 1,
      minWidth: 0
    }
  }, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: `0 var(--card-padding) var(--card-padding)`,
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)"
    }
  }, footer));
}
Object.assign(__ds_scope, { Card });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Card.jsx", error: String((e && e.message) || e) }); }

// components/data/Carousel.jsx
try { (() => {
/**
 * Carousel — sequential media/promos. No autoplay (motion register);
 * arrows + dots + "n de N" counter; keyboard ←→ on the region; swipe
 * has the arrows as its non-drag path (WCAG 2.5.7). Slides off-screen
 * are aria-hidden. Never put must-read content only in slide ≥2.
 */
function Carousel({
  children,
  label = "Carrusel",
  height,
  onChange,
  style
}) {
  const slides = React.Children.toArray(children);
  const [index, setIndex] = React.useState(0);
  const dragRef = React.useRef(null);
  const n = slides.length;
  const go = i => {
    const next = Math.max(0, Math.min(n - 1, i));
    setIndex(next);
    onChange && onChange(next);
  };
  const arrowBtn = dis => ({
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 34,
    height: 34,
    borderRadius: "50%",
    background: "var(--color-bg-surface-raised)",
    color: dis ? "var(--color-fg-disabled)" : "var(--color-fg-brand)",
    border: "1px solid var(--color-border-subtle)",
    boxShadow: "var(--elevation-200)",
    cursor: dis ? "default" : "pointer",
    flexShrink: 0
  });
  return /*#__PURE__*/React.createElement("section", {
    role: "group",
    "aria-roledescription": "carrusel",
    "aria-label": label,
    tabIndex: 0,
    onKeyDown: e => {
      if (e.key === "ArrowRight") {
        e.preventDefault();
        go(index + 1);
      } else if (e.key === "ArrowLeft") {
        e.preventDefault();
        go(index - 1);
      }
    },
    style: {
      position: "relative",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      borderRadius: "var(--radius-lg)",
      touchAction: "pan-y"
    },
    onPointerDown: e => {
      dragRef.current = e.clientX;
    },
    onPointerUp: e => {
      if (dragRef.current == null) return;
      const dx = e.clientX - dragRef.current;
      if (Math.abs(dx) > 48) go(index + (dx < 0 ? 1 : -1));
      dragRef.current = null;
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      transform: `translateX(-${index * 100}%)`,
      transition: "transform var(--duration-moderate) var(--ease-standard)"
    }
  }, slides.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    "aria-hidden": i !== index || undefined,
    style: {
      flex: "0 0 100%",
      minWidth: 0,
      height
    }
  }, s)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-3)",
      marginTop: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Anterior",
    disabled: index === 0,
    onClick: () => go(index - 1),
    style: arrowBtn(index === 0)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m15 18-6-6 6-6"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      gap: 6
    }
  }, slides.map((_, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    type: "button",
    "aria-label": `Ir a la diapositiva ${i + 1}`,
    "aria-current": i === index || undefined,
    onClick: () => go(i),
    style: {
      width: i === index ? 22 : 8,
      height: 8,
      borderRadius: "var(--radius-pill)",
      border: "none",
      padding: 0,
      cursor: "pointer",
      background: i === index ? "var(--tm-red-600)" : "var(--color-border-strong)",
      transition: "width var(--duration-fast-2) var(--ease-standard), background var(--duration-fast-2) var(--ease-standard)"
    }
  }))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, index + 1, " de ", n)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Siguiente",
    disabled: index === n - 1,
    onClick: () => go(index + 1),
    style: arrowBtn(index === n - 1)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m9 18 6-6-6-6"
  })))));
}
Object.assign(__ds_scope, { Carousel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Carousel.jsx", error: String((e && e.message) || e) }); }

// components/data/Charts.jsx
try { (() => {
const W = 100,
  H = 48; // normalized viewBox

/**
 * Charts — dependency-free SVG/CSS charts on the --chart-* tokens.
 * BarChart, LineChart, DonutChart. Rules baked in: categorical order
 * is the colorblind-safe token sequence; every chart is role="img"
 * with a text summary; legends/values render as text (color never
 * sole carrier); axis numbers are tabular. For exploratory/zoomable
 * charts use a real charting lib — these are for dashboard-scale
 * display.
 */
function BarChart({
  data = [],
  label,
  height = 180,
  formatValue = v => String(v),
  color = "var(--chart-cat-1)",
  showValues = true,
  style
}) {
  const max = Math.max(...data.map(d => d.value), 1);
  return /*#__PURE__*/React.createElement("figure", {
    role: "img",
    "aria-label": label || `Gráfica de barras: ${data.map(d => `${d.label} ${formatValue(d.value)}`).join(", ")}`,
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      height,
      display: "flex",
      alignItems: "flex-end",
      gap: "6%",
      padding: "0 2%"
    }
  }, [0.25, 0.5, 0.75, 1].map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    "aria-hidden": "true",
    style: {
      position: "absolute",
      left: 0,
      right: 0,
      bottom: `${t * 100}%`,
      borderTop: "1px solid var(--chart-grid)"
    }
  })), data.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.label,
    style: {
      flex: 1,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 4,
      position: "relative",
      zIndex: 1,
      minWidth: 0
    }
  }, showValues && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 11px/1 var(--font-sans)",
      color: "var(--color-fg-muted)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, formatValue(d.value)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      maxWidth: 52,
      height: Math.max(3, d.value / max * (height - 26)),
      background: d.color || color,
      borderRadius: "4px 4px 0 0"
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "6%",
      padding: "6px 2% 0",
      borderTop: "1.5px solid var(--chart-axis)"
    }
  }, data.map(d => /*#__PURE__*/React.createElement("span", {
    key: d.label,
    style: {
      flex: 1,
      textAlign: "center",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, d.label))));
}
function LineChart({
  data = [],
  label,
  height = 180,
  formatValue = v => String(v),
  color = "var(--chart-cat-1)",
  area = true,
  style
}) {
  const max = Math.max(...data.map(d => d.value), 1);
  const min = Math.min(...data.map(d => d.value), 0);
  const span = max - min || 1;
  const pts = data.map((d, i) => [data.length > 1 ? i / (data.length - 1) * W : W / 2, H - (d.value - min) / span * (H - 6) - 3]);
  const path = pts.map(p => p.join(",")).join(" ");
  return /*#__PURE__*/React.createElement("figure", {
    role: "img",
    "aria-label": label || `Gráfica de línea: ${data.map(d => `${d.label} ${formatValue(d.value)}`).join(", ")}`,
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      height
    }
  }, [0.25, 0.5, 0.75, 1].map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    "aria-hidden": "true",
    style: {
      position: "absolute",
      left: 0,
      right: 0,
      bottom: `${t * 100}%`,
      borderTop: "1px solid var(--chart-grid)"
    }
  })), /*#__PURE__*/React.createElement("svg", {
    viewBox: `0 0 ${W} ${H}`,
    preserveAspectRatio: "none",
    style: {
      position: "absolute",
      inset: 0,
      width: "100%",
      height: "100%",
      overflow: "visible"
    },
    "aria-hidden": "true"
  }, area && /*#__PURE__*/React.createElement("polygon", {
    points: `0,${H} ${path} ${W},${H}`,
    fill: color,
    opacity: "0.12"
  }), /*#__PURE__*/React.createElement("polyline", {
    points: path,
    fill: "none",
    stroke: color,
    strokeWidth: "2",
    vectorEffect: "non-scaling-stroke",
    strokeLinejoin: "round",
    strokeLinecap: "round"
  })), pts.map((p, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    "aria-hidden": "true",
    title: `${data[i].label}: ${formatValue(data[i].value)}`,
    style: {
      position: "absolute",
      left: `${p[0]}%`,
      top: `${p[1] / H * 100}%`,
      width: 7,
      height: 7,
      borderRadius: "50%",
      background: "var(--color-bg-surface)",
      border: `2px solid ${color}`,
      transform: "translate(-50%, -50%)"
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      padding: "6px 0 0",
      borderTop: "1.5px solid var(--chart-axis)"
    }
  }, data.map(d => /*#__PURE__*/React.createElement("span", {
    key: d.label,
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, d.label))));
}
const CAT = ["var(--chart-cat-1)", "var(--chart-cat-2)", "var(--chart-cat-3)", "var(--chart-cat-4)", "var(--chart-cat-5)", "var(--chart-cat-6)"];
function DonutChart({
  data = [],
  label,
  size = 150,
  thickness = 16,
  centerLabel,
  formatValue = v => String(v),
  style
}) {
  const total = data.reduce((s, d) => s + d.value, 0) || 1;
  const r = (size - thickness) / 2;
  const c = 2 * Math.PI * r;
  let acc = 0;
  return /*#__PURE__*/React.createElement("figure", {
    role: "img",
    "aria-label": label || `Distribución: ${data.map(d => `${d.label} ${formatValue(d.value)}`).join(", ")}`,
    style: {
      margin: 0,
      display: "flex",
      alignItems: "center",
      gap: "var(--space-4)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      width: size,
      height: size,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    "aria-hidden": "true",
    style: {
      transform: "rotate(-90deg)"
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--color-bg-muted)",
    strokeWidth: thickness
  }), data.map((d, i) => {
    const frac = d.value / total;
    const el = /*#__PURE__*/React.createElement("circle", {
      key: d.label,
      cx: size / 2,
      cy: size / 2,
      r: r,
      fill: "none",
      stroke: d.color || CAT[i % CAT.length],
      strokeWidth: thickness,
      strokeDasharray: `${Math.max(0, frac * c - 2)} ${c}`,
      strokeDashoffset: -acc * c,
      strokeLinecap: "butt"
    });
    acc += frac;
    return el;
  })), centerLabel && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      font: "700 20px/1.2 var(--font-sans)",
      color: "var(--color-fg-default)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, centerLabel)), /*#__PURE__*/React.createElement("ul", {
    "aria-hidden": "true",
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none",
      display: "flex",
      flexDirection: "column",
      gap: 7
    }
  }, data.map((d, i) => /*#__PURE__*/React.createElement("li", {
    key: d.label,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 8,
      font: "var(--text-sm)",
      color: "var(--color-fg-default)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 10,
      height: 10,
      borderRadius: 3,
      background: d.color || CAT[i % CAT.length],
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--color-fg-muted)"
    }
  }, d.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, formatValue(d.value))))));
}
function Charts() {
  return null;
}
Object.assign(__ds_scope, { BarChart, LineChart, DonutChart, Charts });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Charts.jsx", error: String((e && e.message) || e) }); }

// components/data/Clamp.jsx
try { (() => {
/**
 * Clamp — multi-line truncation with the full text preserved for
 * assistive tech and mouse (title attr when the child is a string).
 * Never clamp legal/disclosure copy (CAT, contratos) — that text must
 * be present AND legible in full.
 */
function Clamp({
  lines = 2,
  children,
  as = "span",
  style
}) {
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, {
    title: typeof children === "string" ? children : undefined,
    style: {
      display: "-webkit-box",
      WebkitLineClamp: lines,
      WebkitBoxOrient: "vertical",
      overflow: "hidden",
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Clamp });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Clamp.jsx", error: String((e && e.message) || e) }); }

// components/data/DataGrid.jsx
try { (() => {
/**
 * DataGrid — virtualized, editable grid for ops-scale data (thousands
 * of rows; Table stays the choice for read-mostly moderate lists).
 * role="grid" with APG keyboard: arrows move cell focus, Home/End row
 * bounds, PgUp/PgDn viewport jumps, Enter/F2 edits an editable cell
 * (Esc cancels, Enter commits), Space toggles row selection in the
 * checkbox column. Only ~visible rows render; header is sticky;
 * numeric cells right-align with tabular figures. Row height follows
 * density unless overridden.
 */
function DataGrid({
  columns = [],
  rows = [],
  rowKey,
  label = "Tabla de datos",
  height = 420,
  rowHeight,
  sortKey,
  sortDir,
  onSort,
  selectable = false,
  selected = [],
  onSelectionChange,
  onCellEdit,
  overscan = 6,
  style
}) {
  const scrollRef = React.useRef(null);
  const headCbRef = React.useRef(null);
  const [scrollTop, setScrollTop] = React.useState(0);
  const [focusPos, setFocusPos] = React.useState({
    r: 0,
    c: 0
  });
  const [editing, setEditing] = React.useState(null); // {r, c, draft}
  const [rh, setRh] = React.useState(rowHeight || 40);
  React.useEffect(() => {
    if (rowHeight) {
      setRh(rowHeight);
      return;
    }
    if (scrollRef.current) {
      const v = parseFloat(getComputedStyle(scrollRef.current).getPropertyValue("--table-row-height"));
      if (v) setRh(v);
    }
  }, [rowHeight]);
  const keyOf = (r, i) => rowKey ? rowKey(r) : i;
  const allKeys = rows.map(keyOf);
  const allSelected = selectable && rows.length > 0 && allKeys.every(k => selected.includes(k));
  const someSelected = selectable && selected.length > 0 && !allSelected;
  React.useEffect(() => {
    if (headCbRef.current) headCbRef.current.indeterminate = someSelected;
  }, [someSelected]);
  const headerH = 40;
  const start = Math.max(0, Math.floor(scrollTop / rh) - overscan);
  const end = Math.min(rows.length, start + Math.ceil(height / rh) + overscan * 2);
  const template = `${selectable ? "44px " : ""}${columns.map(c => c.width ? `${c.width}px` : "minmax(140px, 1fr)").join(" ")}`;
  const colOffset = selectable ? 1 : 0;
  const ensureRowVisible = r => {
    const el = scrollRef.current;
    if (!el) return;
    const top = r * rh,
      bottom = top + rh;
    if (top < el.scrollTop) el.scrollTop = top;else if (bottom > el.scrollTop + height - headerH) el.scrollTop = bottom - height + headerH;
  };
  const focusCell = (r, c) => {
    const rr = Math.max(0, Math.min(rows.length - 1, r));
    const cc = Math.max(0, Math.min(columns.length - 1 + colOffset, c));
    setFocusPos({
      r: rr,
      c: cc
    });
    ensureRowVisible(rr);
    requestAnimationFrame(() => {
      const el = scrollRef.current && scrollRef.current.querySelector(`[data-cell="${rr}-${cc}"]`);
      if (el) el.focus({
        preventScroll: true
      });
    });
  };
  const startEdit = (r, c) => {
    const col = columns[c - colOffset];
    if (!col || !col.editable) return;
    setEditing({
      r,
      c,
      draft: String(rows[r][col.key] != null ? rows[r][col.key] : "")
    });
  };
  const commitEdit = () => {
    if (!editing) return;
    const col = columns[editing.c - colOffset];
    const row = rows[editing.r];
    onCellEdit && onCellEdit(keyOf(row, editing.r), col.key, editing.draft, row);
    setEditing(null);
    focusCell(editing.r, editing.c);
  };
  const onGridKey = e => {
    if (editing) return;
    const {
      r,
      c
    } = focusPos;
    const pageRows = Math.floor((height - headerH) / rh);
    const map = {
      ArrowDown: [r + 1, c],
      ArrowUp: [r - 1, c],
      ArrowRight: [r, c + 1],
      ArrowLeft: [r, c - 1],
      Home: [r, 0],
      End: [r, columns.length - 1 + colOffset],
      PageDown: [r + pageRows, c],
      PageUp: [r - pageRows, c]
    };
    if (map[e.key]) {
      e.preventDefault();
      focusCell(...map[e.key]);
      return;
    }
    if (e.key === "Enter" || e.key === "F2") {
      e.preventDefault();
      startEdit(r, c);
    }
    if (e.key === " " && selectable && c === 0) {
      e.preventDefault();
      const k = keyOf(rows[r], r);
      onSelectionChange && onSelectionChange(selected.includes(k) ? selected.filter(x => x !== k) : [...selected, k]);
    }
  };
  const headCell = {
    display: "flex",
    alignItems: "center",
    paddingInline: "var(--table-cell-pad-x)",
    font: "700 11.5px/1.3 var(--font-sans)",
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "var(--color-fg-subtle)",
    background: "var(--color-bg-subtle)",
    borderBottom: "1px solid var(--color-border-default)",
    height: headerH,
    boxSizing: "border-box"
  };
  return /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    role: "grid",
    "aria-label": label,
    "aria-rowcount": rows.length + 1,
    "aria-colcount": columns.length + colOffset,
    onKeyDown: onGridKey,
    onScroll: e => setScrollTop(e.currentTarget.scrollTop),
    style: {
      height,
      overflow: "auto",
      position: "relative",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      fontFamily: "var(--font-sans)",
      background: "var(--color-bg-surface)",
      boxSizing: "border-box",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "row",
    "aria-rowindex": 1,
    style: {
      display: "grid",
      gridTemplateColumns: template,
      position: "sticky",
      top: 0,
      zIndex: "var(--z-raised)",
      minWidth: "min-content"
    }
  }, selectable && /*#__PURE__*/React.createElement("div", {
    role: "columnheader",
    "aria-colindex": 1,
    style: {
      ...headCell,
      justifyContent: "center",
      paddingInline: 0
    }
  }, /*#__PURE__*/React.createElement("input", {
    ref: headCbRef,
    type: "checkbox",
    "aria-label": "Seleccionar todo",
    checked: allSelected,
    onChange: () => onSelectionChange && onSelectionChange(allSelected ? [] : allKeys),
    style: {
      width: 15,
      height: 15,
      accentColor: "var(--tm-navy-900)",
      cursor: "pointer"
    }
  })), columns.map((c, ci) => {
    const activeSort = sortKey === c.key;
    return /*#__PURE__*/React.createElement("div", {
      key: c.key,
      role: "columnheader",
      "aria-colindex": ci + 1 + colOffset,
      "aria-sort": activeSort ? sortDir === "desc" ? "descending" : "ascending" : undefined,
      style: {
        ...headCell,
        justifyContent: c.numeric ? "flex-end" : "flex-start"
      }
    }, c.sortable && onSort ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: () => onSort(c.key, activeSort && sortDir === "asc" ? "desc" : "asc"),
      style: {
        font: "inherit",
        letterSpacing: "inherit",
        textTransform: "inherit",
        color: activeSort ? "var(--color-fg-brand)" : "inherit",
        background: "transparent",
        border: "none",
        cursor: "pointer",
        display: "inline-flex",
        alignItems: "center",
        gap: 4,
        padding: 0
      }
    }, c.header, /*#__PURE__*/React.createElement("svg", {
      "aria-hidden": "true",
      width: "11",
      height: "11",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: "2.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      style: {
        opacity: activeSort ? 1 : 0.35,
        transform: activeSort && sortDir === "desc" ? "rotate(180deg)" : "none"
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "m18 15-6-6-6 6"
    }))) : c.header);
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: rows.length * rh,
      position: "relative",
      minWidth: "min-content"
    }
  }, rows.slice(start, end).map((row, wi) => {
    const r = start + wi;
    const k = keyOf(row, r);
    const isSel = selectable && selected.includes(k);
    return /*#__PURE__*/React.createElement("div", {
      key: k,
      role: "row",
      "aria-rowindex": r + 2,
      "aria-selected": isSel || undefined,
      style: {
        display: "grid",
        gridTemplateColumns: template,
        position: "absolute",
        top: r * rh,
        left: 0,
        right: 0,
        height: rh,
        background: isSel ? "var(--color-bg-selected)" : r % 2 ? "var(--color-bg-subtle)" : "transparent"
      }
    }, selectable && /*#__PURE__*/React.createElement("div", {
      role: "gridcell",
      "aria-colindex": 1,
      "data-cell": `${r}-0`,
      tabIndex: focusPos.r === r && focusPos.c === 0 ? 0 : -1,
      onClick: () => setFocusPos({
        r,
        c: 0
      }),
      style: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        borderBottom: "1px solid var(--color-border-subtle)",
        outlineOffset: -2,
        boxSizing: "border-box"
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      "aria-label": `Seleccionar fila ${r + 1}`,
      checked: isSel,
      tabIndex: -1,
      onChange: () => onSelectionChange && onSelectionChange(isSel ? selected.filter(x => x !== k) : [...selected, k]),
      style: {
        width: 15,
        height: 15,
        accentColor: "var(--tm-navy-900)",
        cursor: "pointer"
      }
    })), columns.map((c, ci) => {
      const cc = ci + colOffset;
      const isEditing = editing && editing.r === r && editing.c === cc;
      return /*#__PURE__*/React.createElement("div", {
        key: c.key,
        role: "gridcell",
        "aria-colindex": cc + 1,
        "data-cell": `${r}-${cc}`,
        tabIndex: focusPos.r === r && focusPos.c === cc ? 0 : -1,
        onClick: () => setFocusPos({
          r,
          c: cc
        }),
        onDoubleClick: () => startEdit(r, cc),
        style: {
          display: "flex",
          alignItems: "center",
          justifyContent: c.numeric ? "flex-end" : "flex-start",
          paddingInline: isEditing ? 4 : "var(--table-cell-pad-x)",
          font: "var(--text-sm)",
          color: "var(--color-fg-default)",
          fontVariantNumeric: c.numeric ? "var(--numeric-tabular)" : undefined,
          borderBottom: "1px solid var(--color-border-subtle)",
          whiteSpace: "nowrap",
          overflow: "hidden",
          textOverflow: "ellipsis",
          outlineOffset: -2,
          boxSizing: "border-box",
          cursor: c.editable ? "text" : "default"
        }
      }, isEditing ? /*#__PURE__*/React.createElement("input", {
        autoFocus: true,
        value: editing.draft,
        onChange: e => setEditing({
          ...editing,
          draft: e.target.value
        }),
        onKeyDown: e => {
          if (e.key === "Enter") {
            e.preventDefault();
            commitEdit();
          } else if (e.key === "Escape") {
            e.preventDefault();
            setEditing(null);
            focusCell(r, cc);
          }
          e.stopPropagation();
        },
        onBlur: commitEdit,
        style: {
          width: "100%",
          height: rh - 8,
          boxSizing: "border-box",
          font: "var(--text-sm)",
          textAlign: c.numeric ? "right" : "left",
          fontVariantNumeric: "var(--numeric-tabular)",
          color: "var(--color-fg-default)",
          background: "var(--input-bg)",
          border: "var(--border-width-md) solid var(--color-border-focus)",
          borderRadius: "var(--radius-xs)",
          outline: "none",
          paddingInline: 8
        }
      }) : c.render ? c.render(row, r) : row[c.key]);
    }));
  })));
}
Object.assign(__ds_scope, { DataGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DataGrid.jsx", error: String((e && e.message) || e) }); }

// components/data/DescriptionList.jsx
try { (() => {
/**
 * DescriptionList — key–value pairs (record metadata, review steps).
 * Semantic <dl>. mono flag per item for identifiers.
 */
function DescriptionList({
  items = [],
  columns = 1,
  style
}) {
  return /*#__PURE__*/React.createElement("dl", {
    style: {
      display: "grid",
      gridTemplateColumns: `repeat(${columns}, minmax(0, 1fr))`,
      gap: "var(--space-4) var(--space-6)",
      margin: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("dt", {
    style: {
      font: "600 12px/1.4 var(--font-sans)",
      letterSpacing: "0.03em",
      textTransform: "uppercase",
      color: "var(--color-fg-subtle)"
    }
  }, it.term), /*#__PURE__*/React.createElement("dd", {
    style: {
      margin: "3px 0 0",
      font: it.mono ? "var(--text-code-md)" : "var(--text-sm)",
      fontWeight: it.mono ? 400 : 500,
      color: "var(--color-fg-default)",
      fontVariantNumeric: "var(--numeric-tabular)",
      overflowWrap: "break-word"
    }
  }, it.detail))));
}
Object.assign(__ds_scope, { DescriptionList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/DescriptionList.jsx", error: String((e && e.message) || e) }); }

// components/data/Divider.jsx
try { (() => {
/**
 * Divider — separation with hierarchy intact: use space first, a line
 * only when grouping needs it (the brand is nearly flat). Optional
 * inline label ("o", section names) renders as fg-subtle overline.
 */
function Divider({
  children,
  orientation = "horizontal",
  strong = false,
  spacing = "var(--space-4)",
  style
}) {
  const color = strong ? "var(--color-border-default)" : "var(--color-border-subtle)";
  if (orientation === "vertical") {
    return /*#__PURE__*/React.createElement("span", {
      role: "separator",
      "aria-orientation": "vertical",
      style: {
        display: "inline-block",
        width: 1,
        alignSelf: "stretch",
        background: color,
        marginInline: spacing,
        ...style
      }
    });
  }
  if (!children) {
    return /*#__PURE__*/React.createElement("hr", {
      "aria-orientation": "horizontal",
      style: {
        border: "none",
        height: 1,
        background: color,
        marginBlock: spacing,
        ...style
      }
    });
  }
  return /*#__PURE__*/React.createElement("div", {
    role: "separator",
    "aria-orientation": "horizontal",
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginBlock: spacing,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 11px/1.3 var(--font-sans)",
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase",
      color: "var(--color-fg-subtle)",
      whiteSpace: "nowrap"
    }
  }, children), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: color
    }
  }));
}
Object.assign(__ds_scope, { Divider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Divider.jsx", error: String((e && e.message) || e) }); }

// components/data/Image.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const RADII = {
  none: 0,
  sm: "var(--radius-sm)",
  md: "var(--radius-md)",
  lg: "var(--radius-lg)",
  circle: "50%"
};

/**
 * Image — media with reserved space (no layout shift): aspect-ratio
 * box, cover fit, muted placeholder while loading, and an explicit
 * broken-image fallback (icon + alt text) instead of the browser
 * default. Decorative images pass alt="".
 */
function Image({
  src,
  alt = "",
  ratio = "16/9",
  radius = "md",
  fit = "cover",
  caption,
  width,
  lazy = false,
  style,
  ...rest
}) {
  const [state, setState] = React.useState("loading");
  const ref = React.useRef(null);
  React.useEffect(() => {
    const el = ref.current;
    if (el && el.complete) setState(el.naturalWidth > 0 ? "ok" : "error");
  }, [src]);
  const body = /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      position: "relative",
      width: width || "100%",
      aspectRatio: ratio,
      borderRadius: RADII[radius] != null ? RADII[radius] : radius,
      overflow: "hidden",
      background: "var(--color-bg-muted)",
      ...style
    }
  }, state !== "error" && /*#__PURE__*/React.createElement("img", _extends({
    ref: ref,
    src: src,
    alt: alt,
    loading: lazy ? "lazy" : undefined,
    onLoad: () => setState("ok"),
    onError: () => setState("error"),
    style: {
      position: "absolute",
      inset: 0,
      width: "100%",
      height: "100%",
      objectFit: fit,
      opacity: state === "ok" ? 1 : 0,
      transition: "opacity var(--duration-base) var(--ease-enter)"
    }
  }, rest)), state === "error" && /*#__PURE__*/React.createElement("span", {
    role: "img",
    "aria-label": alt || "Imagen no disponible",
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: 6,
      color: "var(--color-fg-subtle)",
      padding: 12,
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "22",
    height: "22",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "3",
    y: "3",
    width: "18",
    height: "18",
    rx: "2"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "9",
    cy: "9",
    r: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"
  })), alt && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)"
    }
  }, alt)));
  if (!caption) return body;
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0
    }
  }, body, /*#__PURE__*/React.createElement("figcaption", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 6,
      fontFamily: "var(--font-sans)"
    }
  }, caption));
}
Object.assign(__ds_scope, { Image });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Image.jsx", error: String((e && e.message) || e) }); }

// components/data/List.jsx
try { (() => {
/**
 * List / ListItem — structured rows: leading icon or avatar, primary +
 * secondary text, trailing meta or action. Interactive rows render a
 * real <button> or <a> (whole row is the ≥44px target); mixed
 * static/interactive lists are fine. Renders semantic ul/li.
 */
function List({
  children,
  dividers = true,
  inset = false,
  style
}) {
  return /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none",
      fontFamily: "var(--font-sans)",
      ...(inset ? {
        border: "1px solid var(--color-border-subtle)",
        borderRadius: "var(--radius-md)",
        overflow: "hidden"
      } : {}),
      ...style
    },
    "data-dividers": dividers ? "true" : undefined
  }, React.Children.map(children, (child, i) => child ? /*#__PURE__*/React.createElement("li", {
    style: {
      borderTop: dividers && i > 0 ? "1px solid var(--color-border-subtle)" : "none"
    }
  }, child) : null));
}
function ListItem({
  icon,
  avatar,
  primary,
  secondary,
  meta,
  action,
  onClick,
  href,
  selected = false,
  disabled = false,
  style
}) {
  const [hover, setHover] = React.useState(false);
  const interactive = !!(onClick || href) && !disabled;
  const Tag = href ? "a" : onClick ? "button" : "div";
  return /*#__PURE__*/React.createElement(Tag, {
    href: href,
    type: Tag === "button" ? "button" : undefined,
    disabled: Tag === "button" ? disabled : undefined,
    "aria-current": selected || undefined,
    onClick: disabled ? undefined : onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      width: "100%",
      boxSizing: "border-box",
      minHeight: "var(--target-touch)",
      padding: "var(--space-2) var(--space-3)",
      background: selected ? "var(--color-bg-selected)" : hover && interactive ? "var(--color-bg-hover)" : "transparent",
      border: "none",
      textAlign: "left",
      textDecoration: "none",
      font: "var(--text-sm)",
      color: "var(--color-fg-default)",
      cursor: interactive ? "pointer" : "default",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      boxShadow: selected ? "inset 3px 0 0 var(--tm-red-600)" : "none",
      transition: "background var(--duration-fast-1) var(--ease-standard)",
      ...style
    }
  }, avatar, icon && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex",
      color: selected ? "var(--color-fg-brand)" : "var(--color-fg-subtle)",
      flexShrink: 0
    }
  }, icon), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      display: "flex",
      flexDirection: "column",
      gap: 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 600,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, primary), secondary && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, secondary)), meta && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      flexShrink: 0,
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, meta), action && /*#__PURE__*/React.createElement("span", {
    style: {
      flexShrink: 0,
      display: "inline-flex"
    },
    onClick: e => e.stopPropagation()
  }, action), interactive && !action && /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "m9 18 6-6-6-6"
  })));
}
Object.assign(__ds_scope, { List, ListItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/List.jsx", error: String((e && e.message) || e) }); }

// components/data/OrgChart.jsx
try { (() => {
function Node({
  node,
  isRoot
}) {
  const initials = (node.name || "").split(/\s+/).map(w => w[0]).slice(0, 2).join("").toUpperCase();
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "inline-flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 6,
      padding: "10px 14px",
      background: isRoot ? "var(--color-bg-brand-solid)" : "var(--color-bg-surface)",
      border: isRoot ? "none" : "1px solid var(--color-border-default)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-100)",
      minWidth: 120,
      maxWidth: 170
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 34,
      height: 34,
      borderRadius: "50%",
      background: isRoot ? "var(--tm-alpha-white-200)" : "var(--color-bg-brand-subtle)",
      color: isRoot ? "var(--color-fg-on-solid)" : "var(--color-fg-brand)",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      font: "700 12px/1 var(--font-sans)"
    }
  }, initials), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 12.5px/1.25 var(--font-sans)",
      color: isRoot ? "var(--color-fg-on-solid)" : "var(--color-fg-default)",
      textAlign: "center"
    }
  }, node.name), node.role && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "400 11px/1.3 var(--font-sans)",
      color: isRoot ? "var(--color-fg-inverse-muted)" : "var(--color-fg-muted)",
      textAlign: "center"
    }
  }, node.role));
}
function Branch({
  node,
  isRoot = false
}) {
  const kids = node.children || [];
  const line = "1.5px solid var(--color-border-strong)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement(Node, {
    node: node,
    isRoot: isRoot
  }), kids.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      width: 0,
      height: 18,
      borderLeft: line
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start"
    }
  }, kids.map((k, i) => /*#__PURE__*/React.createElement("div", {
    key: k.name + i,
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      paddingInline: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      width: "100%"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      borderTop: i > 0 ? line : "none"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      borderTop: i < kids.length - 1 ? line : "none"
    }
  })), /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      width: 0,
      height: 14,
      borderLeft: line,
      marginTop: -1.5
    }
  }), /*#__PURE__*/React.createElement(Branch, {
    node: k
  }))))));
}

/**
 * OrgChart — reporting structure rendered as a connector tree (CSS
 * borders, no images). Root = navy solid. For deep orgs prefer
 * TreeView; this is for ≤3 levels shown at once. The visual tree is
 * decorative for SR users — pair with a nested-list alternative when
 * the chart is the only representation.
 */
function OrgChart({
  data,
  label = "Organigrama",
  style
}) {
  if (!data) return null;
  return /*#__PURE__*/React.createElement("div", {
    role: "img",
    "aria-label": label,
    style: {
      display: "inline-block",
      fontFamily: "var(--font-sans)",
      overflowX: "auto",
      maxWidth: "100%",
      ...style
    }
  }, /*#__PURE__*/React.createElement(Branch, {
    node: data,
    isRoot: true
  }));
}
Object.assign(__ds_scope, { OrgChart });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/OrgChart.jsx", error: String((e && e.message) || e) }); }

// components/data/PageHeader.jsx
try { (() => {
/**
 * PageHeader — top of List/Detail/Dashboard archetypes: breadcrumbs
 * slot, title (+status), meta line, right-aligned actions.
 */
function PageHeader({
  breadcrumbs,
  title,
  status,
  meta,
  actions,
  style
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, breadcrumbs, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: "var(--space-4)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 240
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      font: "700 24px/1.25 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), status), meta && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)",
      marginTop: 4
    }
  }, meta)), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      flexWrap: "wrap"
    }
  }, actions)));
}
Object.assign(__ds_scope, { PageHeader });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/PageHeader.jsx", error: String((e && e.message) || e) }); }

// components/data/StatTile.jsx
try { (() => {
/**
 * StatTile — KPI display: label · value (tabular figures) · delta ·
 * period caption. Deltas carry sign + arrow + color + SR text —
 * never color alone (critical for financial gains/losses).
 */
function StatTile({
  label,
  value,
  delta,
  caption,
  icon,
  style
}) {
  const deltaUp = delta && delta.direction === "up";
  const good = delta && (delta.positive !== undefined ? delta.positive : deltaUp);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-1)",
      padding: "var(--card-padding)",
      background: "var(--color-bg-surface)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--card-radius)",
      fontFamily: "var(--font-sans)",
      minWidth: 0,
      boxSizing: "border-box",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)"
    }
  }, icon && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex",
      color: "var(--color-fg-accent)"
    }
  }, icon), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "600 13px/1.35 var(--font-sans)",
      color: "var(--color-fg-muted)"
    }
  }, label)), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 28px/1.15 var(--font-sans)",
      color: "var(--color-fg-default)",
      fontVariantNumeric: "var(--numeric-tabular)",
      minWidth: 0
    }
  }, value), (delta || caption) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: "var(--space-2)",
      flexWrap: "wrap"
    }
  }, delta && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 3,
      font: "700 13px/1.2 var(--font-sans)",
      color: good ? "var(--chart-positive)" : "var(--chart-negative)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }, deltaUp ? "▲" : "▼"), delta.value, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, deltaUp ? "aumentó" : "disminuyó", " ", delta.value, delta.vs ? ` vs ${delta.vs}` : "")), (caption || delta && delta.vs) && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": delta && delta.vs ? "true" : undefined,
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, caption || `vs ${delta.vs}`)));
}
Object.assign(__ds_scope, { StatTile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StatTile.jsx", error: String((e && e.message) || e) }); }

// components/data/StatusIndicator.jsx
try { (() => {
/**
 * StatusIndicator — status dot + mandatory label. Shape + label carry
 * the meaning alongside color (1.4.1): ● solid = positive/active,
 * ◐ half = pending/in-progress, ■ = neutral/info, ✕ = negative.
 */
function StatusIndicator({
  status = "neutral",
  label,
  size = "md",
  style
}) {
  const conf = {
    success: {
      color: "var(--color-fg-success)",
      glyph: "dot"
    },
    pending: {
      color: "var(--color-fg-warning)",
      glyph: "half"
    },
    warning: {
      color: "var(--color-fg-warning)",
      glyph: "half"
    },
    danger: {
      color: "var(--color-fg-danger)",
      glyph: "x"
    },
    info: {
      color: "var(--color-fg-info)",
      glyph: "square"
    },
    neutral: {
      color: "var(--color-fg-subtle)",
      glyph: "dot"
    }
  }[status] || {
    color: "var(--color-fg-subtle)",
    glyph: "dot"
  };
  const d = size === "sm" ? 8 : 10;
  const font = size === "sm" ? "600 12px/1.3 var(--font-sans)" : "600 13px/1.35 var(--font-sans)";
  const glyph = conf.glyph === "x" ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: d,
    height: d,
    viewBox: "0 0 10 10",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M1.5 1.5 8.5 8.5 M8.5 1.5 1.5 8.5",
    stroke: conf.color,
    strokeWidth: "2.2",
    strokeLinecap: "round",
    fill: "none"
  })) : conf.glyph === "half" ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: d,
    height: d,
    viewBox: "0 0 10 10",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "5",
    cy: "5",
    r: "4",
    fill: "none",
    stroke: conf.color,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M5 1 A4 4 0 0 1 5 9 Z",
    fill: conf.color
  })) : conf.glyph === "square" ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: d,
    height: d,
    viewBox: "0 0 10 10",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("rect", {
    x: "1",
    y: "1",
    width: "8",
    height: "8",
    rx: "1.5",
    fill: conf.color
  })) : /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: d,
    height: d,
    viewBox: "0 0 10 10",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "5",
    cy: "5",
    r: "4",
    fill: conf.color
  }));
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-2)",
      font,
      color: "var(--color-fg-default)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, glyph, label);
}
Object.assign(__ds_scope, { StatusIndicator });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/StatusIndicator.jsx", error: String((e && e.message) || e) }); }

// components/data/Table.jsx
try { (() => {
/**
 * Table — read-mostly data table. Semantic <table> with <th scope>;
 * sticky header; sortable columns are real buttons cycling asc→desc
 * with aria-sort; selection column with tri-state header; numeric
 * columns right-aligned with tabular figures (non-negotiable for
 * amounts). Density follows --table-row-height (comfortable 48 /
 * compact 36). Empty ≠ blank: pass `empty` for the no-data state.
 * Below md: horizontal scroll with the table's own min-width.
 */
function Table({
  columns = [],
  rows = [],
  rowKey,
  caption,
  sortKey,
  sortDir,
  onSort,
  selectable = false,
  selected = [],
  onSelectionChange,
  onRowClick,
  zebra = false,
  loading = false,
  empty,
  minWidth,
  style
}) {
  const keyOf = (r, i) => rowKey ? rowKey(r) : i;
  const allKeys = rows.map(keyOf);
  const allSelected = selectable && rows.length > 0 && allKeys.every(k => selected.includes(k));
  const someSelected = selectable && selected.length > 0 && !allSelected;
  const headRef = React.useRef(null);
  React.useEffect(() => {
    if (headRef.current) headRef.current.indeterminate = someSelected;
  }, [someSelected]);
  const toggleAll = () => {
    if (!onSelectionChange) return;
    onSelectionChange(allSelected ? [] : allKeys);
  };
  const toggleOne = k => {
    if (!onSelectionChange) return;
    onSelectionChange(selected.includes(k) ? selected.filter(x => x !== k) : [...selected, k]);
  };
  const thBase = {
    height: "var(--table-header-height)",
    paddingInline: "var(--table-cell-pad-x)",
    font: "700 12px/1.3 var(--font-sans)",
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    color: "var(--color-fg-subtle)",
    background: "var(--color-bg-subtle)",
    borderBottom: "1px solid var(--color-border-default)",
    position: "sticky",
    top: 0,
    zIndex: "var(--z-raised)",
    whiteSpace: "nowrap"
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      fontFamily: "var(--font-sans)",
      ...style
    },
    "aria-busy": loading || undefined
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      borderCollapse: "collapse",
      width: "100%",
      minWidth: minWidth || 560
    }
  }, caption && /*#__PURE__*/React.createElement("caption", {
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, caption), /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, selectable && /*#__PURE__*/React.createElement("th", {
    scope: "col",
    style: {
      ...thBase,
      width: 40,
      paddingInline: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    ref: headRef,
    type: "checkbox",
    "aria-label": "Seleccionar todo",
    checked: allSelected,
    onChange: toggleAll,
    style: {
      width: 16,
      height: 16,
      accentColor: "var(--tm-navy-900)",
      cursor: "pointer"
    }
  })), columns.map(c => {
    const active = sortKey === c.key;
    const align = c.numeric ? "right" : c.align || "left";
    return /*#__PURE__*/React.createElement("th", {
      key: c.key,
      scope: "col",
      "aria-sort": active ? sortDir === "desc" ? "descending" : "ascending" : undefined,
      style: {
        ...thBase,
        textAlign: align,
        width: c.width
      }
    }, c.sortable && onSort ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: () => onSort(c.key, active && sortDir === "asc" ? "desc" : "asc"),
      style: {
        font: "inherit",
        letterSpacing: "inherit",
        textTransform: "inherit",
        color: active ? "var(--color-fg-brand)" : "inherit",
        background: "transparent",
        border: "none",
        cursor: "pointer",
        display: "inline-flex",
        alignItems: "center",
        gap: 4,
        padding: 0
      }
    }, c.header, /*#__PURE__*/React.createElement("svg", {
      "aria-hidden": "true",
      width: "12",
      height: "12",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: "2.5",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      style: {
        opacity: active ? 1 : 0.35,
        transform: active && sortDir === "desc" ? "rotate(180deg)" : "none"
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "m18 15-6-6-6 6"
    }))) : c.header);
  }))), /*#__PURE__*/React.createElement("tbody", null, loading ? Array.from({
    length: 5
  }).map((_, i) => /*#__PURE__*/React.createElement("tr", {
    key: `sk-${i}`
  }, selectable && /*#__PURE__*/React.createElement("td", {
    style: {
      height: "var(--table-row-height)",
      paddingInline: "var(--space-3)",
      borderBottom: "1px solid var(--color-border-subtle)"
    }
  }), columns.map(c => /*#__PURE__*/React.createElement("td", {
    key: c.key,
    style: {
      height: "var(--table-row-height)",
      paddingInline: "var(--table-cell-pad-x)",
      borderBottom: "1px solid var(--color-border-subtle)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "block",
      height: 10,
      borderRadius: 5,
      background: "var(--color-bg-muted)",
      width: c.numeric ? "50%" : "72%",
      marginLeft: c.numeric ? "auto" : 0
    }
  }))))) : rows.map((r, i) => {
    const k = keyOf(r, i);
    const isSel = selectable && selected.includes(k);
    return /*#__PURE__*/React.createElement("tr", {
      key: k,
      "aria-selected": isSel || undefined,
      onClick: onRowClick ? () => onRowClick(r) : undefined,
      onMouseEnter: e => {
        if (onRowClick && !isSel) e.currentTarget.style.background = "var(--color-bg-hover)";
      },
      onMouseLeave: e => {
        if (onRowClick && !isSel) e.currentTarget.style.background = zebra && i % 2 ? "var(--color-bg-subtle)" : "transparent";
      },
      style: {
        background: isSel ? "var(--color-bg-selected)" : zebra && i % 2 ? "var(--color-bg-subtle)" : "transparent",
        cursor: onRowClick ? "pointer" : "default"
      }
    }, selectable && /*#__PURE__*/React.createElement("td", {
      style: {
        height: "var(--table-row-height)",
        paddingInline: "var(--space-3)",
        borderBottom: "1px solid var(--color-border-subtle)"
      },
      onClick: e => e.stopPropagation()
    }, /*#__PURE__*/React.createElement("input", {
      type: "checkbox",
      "aria-label": `Seleccionar fila ${i + 1}`,
      checked: isSel,
      onChange: () => toggleOne(k),
      style: {
        width: 16,
        height: 16,
        accentColor: "var(--tm-navy-900)",
        cursor: "pointer"
      }
    })), columns.map(c => /*#__PURE__*/React.createElement("td", {
      key: c.key,
      style: {
        height: "var(--table-row-height)",
        paddingInline: "var(--table-cell-pad-x)",
        borderBottom: "1px solid var(--color-border-subtle)",
        font: "var(--text-sm)",
        color: "var(--color-fg-default)",
        textAlign: c.numeric ? "right" : c.align || "left",
        fontVariantNumeric: c.numeric ? "var(--numeric-tabular)" : undefined,
        whiteSpace: c.wrap ? "normal" : "nowrap",
        overflow: "hidden",
        textOverflow: "ellipsis",
        maxWidth: c.maxWidth
      }
    }, c.render ? c.render(r, i) : r[c.key])));
  }))), !loading && rows.length === 0 && (empty || null));
}
Object.assign(__ds_scope, { Table });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Table.jsx", error: String((e && e.message) || e) }); }

// components/data/Tag.jsx
try { (() => {
/**
 * Tag — metadata label / removable filter chip. Removable variant:
 * the × button removes; Delete/Backspace removes while focused.
 * Static tags are not focusable; interactive ones are buttons.
 */
function Tag({
  children,
  tone = "neutral",
  onRemove,
  onClick,
  style
}) {
  const tones = {
    neutral: {
      background: "var(--color-bg-muted)",
      color: "var(--color-fg-muted)"
    },
    brand: {
      background: "var(--color-bg-brand-subtle)",
      color: "var(--color-fg-brand)"
    },
    accent: {
      background: "var(--color-bg-accent-subtle)",
      color: "var(--color-fg-accent)"
    },
    success: {
      background: "var(--color-bg-success-subtle)",
      color: "var(--color-fg-success)"
    },
    warning: {
      background: "var(--color-bg-warning-subtle)",
      color: "var(--color-fg-warning)"
    },
    danger: {
      background: "var(--color-bg-danger-subtle)",
      color: "var(--color-fg-danger)"
    }
  };
  const base = {
    display: "inline-flex",
    alignItems: "center",
    gap: 5,
    font: "600 12px/1.2 var(--font-sans)",
    padding: onRemove ? "4px 6px 4px 10px" : "4px 10px",
    borderRadius: "var(--radius-sm)",
    border: "none",
    fontFamily: "var(--font-sans)",
    cursor: onClick ? "pointer" : "default",
    ...(tones[tone] || tones.neutral),
    ...style
  };
  const removeBtn = onRemove && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": `Quitar ${typeof children === "string" ? children : "etiqueta"}`,
    onClick: e => {
      e.stopPropagation();
      onRemove();
    },
    onKeyDown: e => {
      if (e.key === "Delete" || e.key === "Backspace") {
        e.preventDefault();
        onRemove();
      }
    },
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 16,
      height: 16,
      padding: 0,
      background: "transparent",
      border: "none",
      color: "inherit",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      opacity: 0.7
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "11",
    height: "11",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })));
  if (onClick) {
    return /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: onClick,
      style: base
    }, children, removeBtn);
  }
  return /*#__PURE__*/React.createElement("span", {
    style: base
  }, children, removeBtn);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Tag.jsx", error: String((e && e.message) || e) }); }

// components/data/Timeline.jsx
try { (() => {
/**
 * Timeline — vertical activity/audit stream: timestamp · actor ·
 * action · object. The audit-trail workhorse: every ops record
 * detail should carry one. Tones mark event severity.
 */
function Timeline({
  items = [],
  style
}) {
  const toneColor = {
    success: "var(--color-bg-success-solid)",
    warning: "var(--color-bg-warning-solid)",
    danger: "var(--color-bg-danger-solid)",
    info: "var(--color-bg-info-solid)",
    neutral: "var(--tm-neutral-300)"
  };
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      listStyle: "none",
      margin: 0,
      padding: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, items.map((it, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "flex",
      gap: "var(--space-3)",
      position: "relative",
      paddingBottom: i < items.length - 1 ? "var(--space-5)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      flexShrink: 0,
      width: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 10,
      height: 10,
      marginTop: 5,
      borderRadius: "50%",
      background: toneColor[it.tone || "neutral"] || toneColor.neutral,
      flexShrink: 0
    }
  }), i < items.length - 1 && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 2,
      flex: 1,
      background: "var(--color-border-subtle)",
      marginTop: 4
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      paddingBottom: 2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-default)"
    }
  }, it.actor && /*#__PURE__*/React.createElement("strong", {
    style: {
      fontWeight: 700
    }
  }, it.actor, " "), it.action, it.object && /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 600
    }
  }, " ", it.object)), it.detail && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 2
    }
  }, it.detail), /*#__PURE__*/React.createElement("time", {
    title: it.timeExact,
    style: {
      display: "block",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      marginTop: 3,
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, it.time)))));
}
Object.assign(__ds_scope, { Timeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Timeline.jsx", error: String((e && e.message) || e) }); }

// components/data/TreeView.jsx
try { (() => {
function flatten(items, expanded, level = 1, out = []) {
  items.forEach(it => {
    out.push({
      ...it,
      level
    });
    if (it.children && expanded.includes(it.id)) flatten(it.children, expanded, level + 1, out);
  });
  return out;
}

/**
 * TreeView — hierarchical navigation (expedientes, catálogos,
 * permisos). APG tree: one tab stop; ↑↓ move among visible nodes,
 * → expands / first child, ← collapses / parent, Home/End, Enter
 * selects. aria-expanded/level/selected on every node.
 */
function TreeView({
  items = [],
  label = "Árbol",
  selectedId,
  onSelect,
  defaultExpanded = [],
  style
}) {
  const [expanded, setExpanded] = React.useState(defaultExpanded);
  const [focusId, setFocusId] = React.useState(null);
  const refs = React.useRef({});
  const visible = flatten(items, expanded);
  const focusIdx = Math.max(0, visible.findIndex(v => v.id === (focusId != null ? focusId : selectedId)));
  const focusNode = id => {
    setFocusId(id);
    const el = refs.current[id];
    if (el) el.focus();
  };
  const findParent = (id, list = items, parent = null) => {
    for (const it of list) {
      if (it.id === id) return parent;
      if (it.children) {
        const p = findParent(id, it.children, it);
        if (p !== undefined && p !== null) return p;
        if (it.children.some(c => c.id === id)) return it;
      }
    }
    return null;
  };
  const onKeyDown = (e, node, idx) => {
    const isOpen = expanded.includes(node.id);
    if (e.key === "ArrowDown" && idx < visible.length - 1) {
      e.preventDefault();
      focusNode(visible[idx + 1].id);
    } else if (e.key === "ArrowUp" && idx > 0) {
      e.preventDefault();
      focusNode(visible[idx - 1].id);
    } else if (e.key === "ArrowRight") {
      e.preventDefault();
      if (node.children && !isOpen) setExpanded(x => [...x, node.id]);else if (node.children && visible[idx + 1]) focusNode(visible[idx + 1].id);
    } else if (e.key === "ArrowLeft") {
      e.preventDefault();
      if (node.children && isOpen) setExpanded(x => x.filter(i => i !== node.id));else {
        const p = findParent(node.id);
        if (p) focusNode(p.id);
      }
    } else if (e.key === "Home") {
      e.preventDefault();
      focusNode(visible[0].id);
    } else if (e.key === "End") {
      e.preventDefault();
      focusNode(visible[visible.length - 1].id);
    } else if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      onSelect && onSelect(node.id, node);
      if (node.children) setExpanded(x => isOpen ? x.filter(i => i !== node.id) : [...x, node.id]);
    }
  };
  return /*#__PURE__*/React.createElement("ul", {
    role: "tree",
    "aria-label": label,
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, visible.map((node, idx) => {
    const isOpen = expanded.includes(node.id);
    const isSel = node.id === selectedId;
    return /*#__PURE__*/React.createElement("li", {
      key: node.id,
      role: "none"
    }, /*#__PURE__*/React.createElement("div", {
      ref: el => refs.current[node.id] = el,
      role: "treeitem",
      "aria-level": node.level,
      "aria-expanded": node.children ? isOpen : undefined,
      "aria-selected": isSel,
      tabIndex: idx === focusIdx ? 0 : -1,
      onKeyDown: e => onKeyDown(e, node, idx),
      onClick: () => {
        setFocusId(node.id);
        onSelect && onSelect(node.id, node);
        if (node.children) setExpanded(x => isOpen ? x.filter(i => i !== node.id) : [...x, node.id]);
      },
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-1)",
        minHeight: 32,
        paddingRight: "var(--space-2)",
        paddingLeft: 8 + (node.level - 1) * 20,
        borderRadius: "var(--radius-sm)",
        font: "var(--text-sm)",
        fontWeight: isSel ? 700 : 500,
        color: isSel ? "var(--color-fg-brand)" : "var(--color-fg-default)",
        background: isSel ? "var(--color-bg-selected)" : "transparent",
        boxShadow: isSel ? "inset 3px 0 0 var(--tm-red-600)" : "none",
        cursor: "pointer",
        userSelect: "none"
      },
      onMouseEnter: e => {
        if (!isSel) e.currentTarget.style.background = "var(--color-bg-hover)";
      },
      onMouseLeave: e => {
        if (!isSel) e.currentTarget.style.background = "transparent";
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex",
        width: 16,
        justifyContent: "center",
        color: "var(--color-fg-subtle)",
        flexShrink: 0
      }
    }, node.children && /*#__PURE__*/React.createElement("svg", {
      width: "13",
      height: "13",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: "2.2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      style: {
        transform: isOpen ? "rotate(90deg)" : "none",
        transition: "transform var(--duration-fast-2) var(--ease-standard)"
      }
    }, /*#__PURE__*/React.createElement("path", {
      d: "m9 18 6-6-6-6"
    }))), node.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex",
        color: isSel ? "var(--color-fg-brand)" : "var(--color-fg-subtle)",
        flexShrink: 0
      }
    }, node.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap"
      }
    }, node.label), node.count != null && /*#__PURE__*/React.createElement("span", {
      style: {
        marginLeft: "auto",
        font: "700 10.5px/1 var(--font-sans)",
        color: "var(--color-fg-subtle)",
        background: "var(--color-bg-muted)",
        padding: "3px 7px",
        borderRadius: "var(--radius-pill)",
        fontVariantNumeric: "var(--numeric-tabular)"
      }
    }, node.count)));
  }));
}
Object.assign(__ds_scope, { TreeView });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/TreeView.jsx", error: String((e && e.message) || e) }); }

// components/data/Typography.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const VARIANTS = {
  "accent-xl": {
    font: "var(--text-accent-xl)",
    as: "h1"
  },
  "accent-lg": {
    font: "var(--text-accent-lg)",
    as: "h2"
  },
  "accent-md": {
    font: "var(--text-accent-md)",
    as: "h3"
  },
  h1: {
    font: "var(--text-h1)",
    as: "h1"
  },
  h2: {
    font: "var(--text-h2)",
    as: "h2"
  },
  h3: {
    font: "var(--text-h3)",
    as: "h3"
  },
  h4: {
    font: "var(--text-h4)",
    as: "h4"
  },
  h5: {
    font: "var(--text-h5)",
    as: "h5"
  },
  subtitle: {
    font: "var(--text-subtitle)",
    as: "p"
  },
  "subtitle-sm": {
    font: "var(--text-subtitle-sm)",
    as: "p"
  },
  "body-lg": {
    font: "var(--text-body-lg)",
    as: "p"
  },
  body: {
    font: "var(--text-body-md)",
    as: "p"
  },
  "body-sm": {
    font: "var(--text-body-sm)",
    as: "p"
  },
  sm: {
    font: "var(--text-sm)",
    as: "span"
  },
  xs: {
    font: "var(--text-xs)",
    as: "span"
  },
  overline: {
    font: "var(--text-overline)",
    as: "span",
    extra: {
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase"
    }
  },
  code: {
    font: "var(--text-code-md)",
    as: "code"
  }
};
const COLORS = {
  default: "var(--color-fg-default)",
  muted: "var(--color-fg-muted)",
  subtle: "var(--color-fg-subtle)",
  brand: "var(--color-fg-brand)",
  accent: "var(--color-fg-accent)",
  success: "var(--color-fg-success)",
  warning: "var(--color-fg-warning)",
  danger: "var(--color-fg-danger)",
  inverse: "var(--color-fg-inverse)",
  inherit: "inherit"
};

/**
 * Typography — maps the type tokens to elements so scale and semantics
 * stay decoupled: pick `variant` for the size, `as` for the outline
 * level. Serif accent variants are ONLY ever italic (brand rule) and
 * reserved for emotional headlines ≤5 words. `tabular` turns on
 * lining tabular figures for anything numeric.
 */
function Typography({
  variant = "body",
  as,
  color = "default",
  align,
  tabular = false,
  clamp,
  children,
  style,
  ...rest
}) {
  const v = VARIANTS[variant] || VARIANTS.body;
  const Tag = as || v.as;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    style: {
      margin: 0,
      font: v.font,
      color: COLORS[color] || color,
      textAlign: align,
      fontVariantNumeric: tabular ? "var(--numeric-tabular)" : undefined,
      textWrap: "pretty",
      ...(v.extra || {}),
      ...(clamp ? {
        display: "-webkit-box",
        WebkitLineClamp: clamp,
        WebkitBoxOrient: "vertical",
        overflow: "hidden"
      } : {}),
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Typography });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/data/Typography.jsx", error: String((e && e.message) || e) }); }

// components/domain/AmountInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * AmountInput — money entry. Right-aligned, decimal keyboard,
 * es-MX thousands formatting on blur (raw digits while editing so
 * the caret never jumps), accepts paste of any format ("$1,234.50",
 * "1234.5") and normalizes. Currency is always disambiguated with
 * the ISO code — "$" alone is an ambiguity bug in MX/US corridors.
 */
function AmountInput({
  value,
  onChange,
  currency = "MXN",
  size = "md",
  invalid = false,
  disabled = false,
  readOnly = false,
  fullWidth = false,
  placeholder = "0.00",
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const [text, setText] = React.useState(value != null ? format(value) : "");
  function parse(s) {
    const clean = String(s).replace(/[^\d.]/g, "");
    const parts = clean.split(".");
    const num = parseFloat(parts[0] + (parts.length > 1 ? "." + parts.slice(1).join("").slice(0, 2) : ""));
    return isNaN(num) ? null : num;
  }
  function format(n) {
    return new Intl.NumberFormat("es-MX", {
      minimumFractionDigits: 2,
      maximumFractionDigits: 2
    }).format(n);
  }
  React.useEffect(() => {
    if (!focus) setText(value != null ? format(value) : "");
  }, [value, focus]);
  const borderColor = invalid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 18
  }[size] || 15;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: fullWidth ? "flex" : "inline-flex",
      width: fullWidth ? "100%" : undefined,
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: readOnly ? "var(--color-bg-subtle)" : disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 180,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      font: `600 ${fontSize - 1}px/1 var(--font-sans)`,
      color: "var(--color-fg-subtle)",
      flexShrink: 0
    }
  }, "$"), /*#__PURE__*/React.createElement("input", _extends({
    type: "text",
    inputMode: "decimal",
    autoComplete: "off",
    placeholder: placeholder,
    value: text,
    disabled: disabled,
    readOnly: readOnly,
    "aria-invalid": invalid || undefined,
    onChange: e => {
      const raw = e.target.value.replace(/[^\d.,]/g, "");
      setText(raw);
      onChange && onChange(parse(raw));
    },
    onFocus: e => {
      setFocus(true);
      setText(t => t.replace(/,/g, ""));
    },
    onBlur: () => {
      setFocus(false);
      const n = parse(text);
      setText(n != null ? format(n) : "");
    },
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: "var(--font-sans)",
      fontSize,
      fontWeight: 600,
      fontVariantNumeric: "var(--numeric-tabular)",
      textAlign: "right",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }, rest)), /*#__PURE__*/React.createElement("span", {
    style: {
      font: `600 ${fontSize - 2}px/1 var(--font-sans)`,
      color: "var(--color-fg-subtle)",
      flexShrink: 0,
      letterSpacing: "0.04em"
    }
  }, currency));
}
Object.assign(__ds_scope, { AmountInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/domain/AmountInput.jsx", error: String((e && e.message) || e) }); }

// components/domain/CatDisclosure.jsx
try { (() => {
/**
 * CatDisclosure — mandated total-cost disclosure block (CAT for
 * credit; GAT nominal/real for savings). Componentized so the exact
 * text shown is versioned and loggable with the transaction.
 * Typography floor: text-xs with AA contrast — present AND legible;
 * never grayed below the floor. No cleverness in this register.
 */
function CatDisclosure({
  kind = "CAT",
  value,
  date,
  version,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-disclosure-version": version,
    style: {
      padding: "var(--space-3) var(--space-4)",
      border: "1px solid var(--color-border-default)",
      borderRadius: "var(--radius-sm)",
      background: "var(--color-bg-subtle)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 13px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, kind, " ", kind === "CAT" ? "promedio" : "", " ", value, " sin IVA.", kind === "CAT" ? " Informativo." : ""), children && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 4
    }
  }, children), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 4,
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, "C\xE1lculo al ", date, ".", version ? ` Texto v${version}.` : ""));
}
Object.assign(__ds_scope, { CatDisclosure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/domain/CatDisclosure.jsx", error: String((e && e.message) || e) }); }

// components/domain/ClabeInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const BANKS = {
  "002": "Citibanamex",
  "012": "BBVA México",
  "014": "Santander",
  "021": "HSBC",
  "030": "Bajío",
  "036": "Inbursa",
  "042": "Mifel",
  "044": "Scotiabank",
  "058": "Banregio",
  "059": "Invex",
  "060": "Bansí",
  "062": "Afirme",
  "072": "Banorte",
  "106": "Bank of America",
  "127": "Banco Azteca",
  "137": "BanCoppel",
  "138": "ABC Capital",
  "646": "STP",
  "684": "Transfer"
};

/** CLABE checksum: weights 3,7,1 over the first 17 digits. */
function clabeValid(d) {
  if (d.length !== 18) return false;
  const w = [3, 7, 1];
  let sum = 0;
  for (let i = 0; i < 17; i++) sum += Number(d[i]) * w[i % 3] % 10;
  return (10 - sum % 10) % 10 === Number(d[17]);
}

/**
 * ClabeInput — 18-digit CLABE with visual grouping (3·3·11·1), paste
 * normalization, client-side checksum, and bank auto-identification
 * shown for confirmation ("BBVA México — termina en 4587"). Mono
 * face: transcription accuracy is a UX feature. Server re-validates.
 */
function ClabeInput({
  value = "",
  onChange,
  invalid = false,
  disabled = false,
  fullWidth = false,
  size = "md",
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const digits = String(value).replace(/\D/g, "").slice(0, 18);
  const complete = digits.length === 18;
  const valid = complete && clabeValid(digits);
  const bank = digits.length >= 3 ? BANKS[digits.slice(0, 3)] : null;
  const groups = [digits.slice(0, 3), digits.slice(3, 6), digits.slice(6, 17), digits.slice(17, 18)].filter(Boolean);
  const display = groups.join(" ");
  const borderColor = invalid || complete && !valid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: fullWidth ? "block" : "inline-block",
      width: fullWidth ? "100%" : undefined,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${focus || invalid || complete && !valid ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 260,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("input", _extends({
    type: "text",
    inputMode: "numeric",
    autoComplete: "off",
    placeholder: "000 000 00000000000 0",
    value: display,
    disabled: disabled,
    "aria-invalid": invalid || complete && !valid || undefined,
    onChange: e => onChange && onChange(e.target.value.replace(/\D/g, "").slice(0, 18)),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      font: "var(--text-code-md)",
      fontVariantNumeric: "var(--numeric-tabular)",
      letterSpacing: "0.03em",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }, rest)), complete && /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: valid ? "var(--color-fg-success)" : "var(--color-fg-danger)",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, valid ? /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  }) : /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  }))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      display: "block",
      minHeight: 18,
      font: "var(--text-xs)",
      marginTop: 4,
      color: complete && !valid ? "var(--color-fg-danger)" : "var(--color-fg-muted)"
    }
  }, complete && !valid ? "La CLABE no pasa la validación — revisa los 18 dígitos" : bank ? `${bank}${complete ? ` — termina en ${digits.slice(-4)}` : ""}` : ""));
}
Object.assign(__ds_scope, { ClabeInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/domain/ClabeInput.jsx", error: String((e && e.message) || e) }); }

// components/domain/MaskedValue.jsx
try { (() => {
/**
 * MaskedValue — sensitive identifier rendered masked by default
 * (•••• 4587), with reveal-on-demand (an auditable event — wire
 * onReveal to your audit log) and copy-full affordance. Mono face,
 * middle-truncation keeps both ends of long identifiers.
 */
function MaskedValue({
  value = "",
  visible = 4,
  label = "valor",
  onReveal,
  copyable = true,
  style
}) {
  const [shown, setShown] = React.useState(false);
  const [copied, setCopied] = React.useState(false);
  const masked = value.length > visible ? `•••• ${value.slice(-visible)}` : value;
  const copy = async () => {
    try {
      await navigator.clipboard.writeText(value);
      setCopied(true);
      setTimeout(() => setCopied(false), 1600);
    } catch (e) {/* clipboard unavailable */}
  };
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-1)",
      fontFamily: "var(--font-mono)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-code-md)",
      color: "var(--color-fg-default)",
      letterSpacing: "0.04em",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, shown ? value : masked), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": shown ? `Ocultar ${label}` : `Mostrar ${label}`,
    "aria-pressed": shown,
    onClick: () => {
      const next = !shown;
      setShown(next);
      if (next && onReveal) onReveal();
    },
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)"
    }
  }, shown ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M9.88 9.88a3 3 0 1 0 4.24 4.24M10.73 5.08A10.43 10.43 0 0 1 12 5c7 0 10 7 10 7a13.16 13.16 0 0 1-1.67 2.68M6.61 6.61A13.526 13.526 0 0 0 2 12s3 7 10 7a9.74 9.74 0 0 0 5.39-1.61M2 2l20 20"
  })) : /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "3"
  }))), copyable && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": copied ? "Copiado" : `Copiar ${label}`,
    onClick: copy,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      background: "transparent",
      border: "none",
      color: copied ? "var(--color-fg-success)" : "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)"
    }
  }, copied ? /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  })) : /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "9",
    y: "9",
    width: "13",
    height: "13",
    rx: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"
  }))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, copied ? "Copiado al portapapeles" : ""));
}
Object.assign(__ds_scope, { MaskedValue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/domain/MaskedValue.jsx", error: String((e && e.message) || e) }); }

// components/domain/OtpInput.jsx
try { (() => {
/**
 * OtpInput — one-time code entry (step-up auth). Auto-advance boxes
 * with FULL paste support (distributes digits); Backspace moves back;
 * autocomplete="one-time-code". Paste is never blocked (WCAG 3.3.8).
 */
function OtpInput({
  length = 6,
  value = "",
  onChange,
  onComplete,
  invalid = false,
  disabled = false,
  label = "Código de verificación",
  style
}) {
  const refs = React.useRef([]);
  const digits = String(value).replace(/\D/g, "").slice(0, length);
  const setAt = (i, d) => {
    const arr = digits.split("");
    arr[i] = d;
    const next = arr.join("").slice(0, length);
    onChange && onChange(next);
    if (next.length === length && onComplete) onComplete(next);
  };
  const handleChange = (i, e) => {
    const raw = e.target.value.replace(/\D/g, "");
    if (!raw) return;
    if (raw.length > 1) {
      // paste distributed across boxes
      const next = (digits.slice(0, i) + raw).slice(0, length);
      onChange && onChange(next);
      if (next.length === length && onComplete) onComplete(next);
      const t = refs.current[Math.min(next.length, length - 1)];
      if (t) t.focus();
      return;
    }
    setAt(i, raw);
    const t = refs.current[i + 1];
    if (t) t.focus();
  };
  const handleKey = (i, e) => {
    if (e.key === "Backspace") {
      e.preventDefault();
      if (digits[i]) {
        setAt(i, "");
      } else if (i > 0) {
        const t = refs.current[i - 1];
        if (t) t.focus();
        setAt(i - 1, "");
      }
    } else if (e.key === "ArrowLeft" && i > 0) {
      e.preventDefault();
      refs.current[i - 1].focus();
    } else if (e.key === "ArrowRight" && i < length - 1) {
      e.preventDefault();
      refs.current[i + 1].focus();
    }
  };
  return /*#__PURE__*/React.createElement("div", {
    role: "group",
    "aria-label": label,
    style: {
      display: "flex",
      gap: "var(--space-2)",
      ...style
    }
  }, Array.from({
    length
  }).map((_, i) => /*#__PURE__*/React.createElement("input", {
    key: i,
    ref: el => refs.current[i] = el,
    type: "text",
    inputMode: "numeric",
    autoComplete: i === 0 ? "one-time-code" : "off",
    maxLength: length,
    value: digits[i] || "",
    disabled: disabled,
    "aria-label": `Dígito ${i + 1} de ${length}`,
    "aria-invalid": invalid || undefined,
    onChange: e => handleChange(i, e),
    onKeyDown: e => handleKey(i, e),
    onFocus: e => e.target.select(),
    style: {
      width: 44,
      height: 52,
      boxSizing: "border-box",
      textAlign: "center",
      font: "700 20px/1 var(--font-sans)",
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `var(--border-width-md) solid ${invalid ? "var(--color-border-danger)" : digits[i] ? "var(--color-border-strong)" : "var(--input-border)"}`,
      borderRadius: "var(--radius-md)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      caretColor: "var(--color-fg-accent)"
    }
  })));
}
Object.assign(__ds_scope, { OtpInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/domain/OtpInput.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Alert.jsx
try { (() => {
const ICONS = {
  info: /*#__PURE__*/React.createElement("path", {
    d: "M12 16v-4M12 8h.01M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z"
  }),
  success: /*#__PURE__*/React.createElement("path", {
    d: "M22 11.08V12a10 10 0 1 1-5.93-9.14M22 4 12 14.01l-3-3"
  }),
  warning: /*#__PURE__*/React.createElement("path", {
    d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3ZM12 9v4M12 17h.01"
  }),
  danger: /*#__PURE__*/React.createElement("path", {
    d: "M12 8v4M12 16h.01M12 22a10 10 0 1 0 0-20 10 10 0 0 0 0 20Z"
  })
};

/**
 * Alert / Banner — persistent in-flow message for a section or page.
 * Severity is never color-only: icon + title always accompany it.
 * Subtle variant for in-context notices; solid reserved for
 * page-level critical. Danger alerts announce via role="alert".
 */
function Alert({
  severity = "info",
  variant = "subtle",
  title,
  children,
  action,
  onDismiss,
  style
}) {
  const solid = variant === "solid";
  const fg = solid ? severity === "warning" ? "var(--color-fg-on-warning)" : "var(--color-fg-on-solid)" : `var(--color-fg-${severity})`;
  return /*#__PURE__*/React.createElement("div", {
    role: severity === "danger" ? "alert" : "status",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "flex-start",
      padding: "var(--space-3) var(--space-4)",
      background: solid ? `var(--color-bg-${severity}-solid)` : `var(--color-bg-${severity}-subtle)`,
      border: solid ? "none" : `1px solid var(--color-border-${severity})`,
      borderRadius: "var(--radius-md)",
      fontFamily: "var(--font-sans)",
      color: solid ? fg : "var(--color-fg-default)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: fg,
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0,
      marginTop: 1
    }
  }, ICONS[severity]), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, title && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 14px/1.4 var(--font-sans)",
      color: solid ? fg : `var(--color-fg-${severity})`
    }
  }, title), children && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-sm)",
      marginTop: title ? 2 : 0
    }
  }, children), action && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-2)"
    }
  }, action)), onDismiss && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar aviso",
    onClick: onDismiss,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "inherit",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      opacity: 0.75
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  }))));
}
Object.assign(__ds_scope, { Alert });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Alert.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Backdrop.jsx
try { (() => {
/**
 * Backdrop — blocking scrim for page- or region-level waits (contained
 * mode covers a position:relative parent). role="status" announces the
 * message; pair with a Spinner/ProgressRing child. Use sparingly —
 * prefer inline skeletons; never for waits under ~300ms.
 */
function Backdrop({
  open = false,
  children,
  contained = false,
  blur = false,
  style
}) {
  if (!open) return null;
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      position: contained ? "absolute" : "fixed",
      inset: 0,
      zIndex: contained ? "var(--z-raised)" : "var(--z-overlay)",
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "var(--space-3)",
      background: "var(--color-bg-overlay)",
      backdropFilter: blur ? "blur(3px)" : undefined,
      color: "var(--color-fg-inverse)",
      font: "600 14px/1.4 var(--font-sans)",
      borderRadius: "inherit",
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Backdrop });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Backdrop.jsx", error: String((e && e.message) || e) }); }

// components/feedback/EmptyState.jsx
try { (() => {
/**
 * EmptyState — four distinct types, never one generic screen:
 * first-use (educate + create CTA) · no-results (echo the query,
 * clear filters) · complete (positive) · error (what failed + retry +
 * reference ID). Empty ≠ blank: never a bare table header over
 * whitespace. Title ≤8 words; body ≤2 sentences.
 */
function EmptyState({
  icon,
  title,
  children,
  action,
  secondaryAction,
  referenceId,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      gap: "var(--space-3)",
      padding: "var(--space-8) var(--space-5)",
      textAlign: "center",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, icon && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      width: 56,
      height: 56,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      borderRadius: "50%",
      background: "var(--color-bg-brand-subtle)",
      color: "var(--color-fg-brand)"
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 18px/1.35 var(--font-sans)",
      color: "var(--color-fg-default)",
      maxWidth: 420
    }
  }, title), children && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)",
      maxWidth: 420
    }
  }, children), (action || secondaryAction) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginTop: "var(--space-2)",
      flexWrap: "wrap",
      justifyContent: "center"
    }
  }, action, secondaryAction), referenceId && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-code-sm)",
      color: "var(--color-fg-subtle)",
      marginTop: "var(--space-2)"
    }
  }, "Ref: ", referenceId));
}
Object.assign(__ds_scope, { EmptyState });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/EmptyState.jsx", error: String((e && e.message) || e) }); }

// components/feedback/ProgressBar.jsx
try { (() => {
function ensureBarKeyframes() {
  if (typeof document === "undefined" || document.getElementById("tm-kf-bar")) return;
  const s = document.createElement("style");
  s.id = "tm-kf-bar";
  s.textContent = "@keyframes tm-bar{0%{transform:translateX(-100%)}100%{transform:translateX(250%)}}";
  document.head.appendChild(s);
}

/**
 * ProgressBar — known duration/quantity (unknown → Spinner; page
 * structure → Skeleton). role="progressbar" with value semantics;
 * label + value text visible. Indeterminate only while quantifying.
 */
function ProgressBar({
  value,
  max = 100,
  label,
  showValue = true,
  indeterminate = false,
  size = "md",
  tone = "brand",
  style
}) {
  React.useEffect(() => {
    if (indeterminate) ensureBarKeyframes();
  }, [indeterminate]);
  const pct = indeterminate ? 0 : Math.min(100, Math.max(0, value / max * 100));
  const h = {
    sm: 4,
    md: 6,
    lg: 8
  }[size] || 6;
  const fill = tone === "accent" ? "var(--color-bg-accent-solid)" : tone === "success" ? "var(--color-bg-success-solid)" : "var(--color-bg-brand-solid)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, (label || showValue) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      alignItems: "baseline",
      marginBottom: "var(--space-1)"
    }
  }, label && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-sm)",
      fontWeight: 600,
      color: "var(--color-fg-default)"
    }
  }, label), showValue && !indeterminate && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, Math.round(pct), "%")), /*#__PURE__*/React.createElement("div", {
    role: "progressbar",
    "aria-valuemin": 0,
    "aria-valuemax": max,
    "aria-valuenow": indeterminate ? undefined : value,
    "aria-label": typeof label === "string" ? label : undefined,
    style: {
      height: h,
      borderRadius: h / 2,
      background: "var(--color-bg-muted)",
      overflow: "hidden",
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: "100%",
      width: indeterminate ? "40%" : `${pct}%`,
      borderRadius: h / 2,
      background: fill,
      transition: indeterminate ? "none" : "width var(--duration-moderate) var(--ease-standard)",
      animation: indeterminate ? "tm-bar 1.4s var(--ease-standard) infinite" : "none"
    }
  })));
}
Object.assign(__ds_scope, { ProgressBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/ProgressBar.jsx", error: String((e && e.message) || e) }); }

// components/feedback/ProgressRing.jsx
try { (() => {
/**
 * ProgressRing — determinate circular progress (completeness of an
 * expediente, uso de límite). role="progressbar" with real values;
 * center label shows the figure as text (color never sole carrier).
 * Indeterminate waits use Spinner; linear steps use ProgressBar.
 */
function ProgressRing({
  value = 0,
  max = 100,
  size = 56,
  thickness = 5,
  label,
  tone = "brand",
  showValue = true,
  format,
  style
}) {
  const r = (size - thickness) / 2;
  const c = 2 * Math.PI * r;
  const frac = Math.min(1, Math.max(0, value / (max || 1)));
  const color = {
    brand: "var(--color-bg-brand-solid)",
    accent: "var(--color-bg-accent-solid)",
    success: "var(--color-bg-success-solid)",
    warning: "var(--color-bg-warning-solid)",
    danger: "var(--color-bg-danger-solid)"
  }[tone] || tone;
  const text = format ? format(value, max) : `${Math.round(frac * 100)}%`;
  return /*#__PURE__*/React.createElement("span", {
    role: "progressbar",
    "aria-valuemin": 0,
    "aria-valuemax": max,
    "aria-valuenow": value,
    "aria-label": label,
    style: {
      position: "relative",
      display: "inline-flex",
      width: size,
      height: size,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    "aria-hidden": "true",
    style: {
      transform: "rotate(-90deg)"
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--color-bg-muted)",
    strokeWidth: thickness
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: color,
    strokeWidth: thickness,
    strokeLinecap: "round",
    strokeDasharray: c,
    strokeDashoffset: c * (1 - frac),
    style: {
      transition: "stroke-dashoffset var(--duration-moderate) var(--ease-standard)"
    }
  })), showValue && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      font: `700 ${Math.max(10, size * 0.22)}px/1 var(--font-sans)`,
      color: "var(--color-fg-default)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, text));
}
Object.assign(__ds_scope, { ProgressRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/ProgressRing.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Skeleton.jsx
try { (() => {
function ensurePulseKeyframes() {
  if (typeof document === "undefined" || document.getElementById("tm-kf-pulse")) return;
  const s = document.createElement("style");
  s.id = "tm-kf-pulse";
  s.textContent = "@keyframes tm-pulse{0%,100%{opacity:1}50%{opacity:.55}}";
  document.head.appendChild(s);
}

/**
 * Skeleton — structural placeholder for first loads. Mirror the real
 * layout so content arrival causes NO layout shift. Pulse ≥1.5s.
 * Mark the loading container aria-busy; skeletons are aria-hidden.
 */
function Skeleton({
  variant = "text",
  width,
  height,
  lines = 1,
  style
}) {
  React.useEffect(ensurePulseKeyframes, []);
  const base = {
    background: "var(--color-bg-muted)",
    animation: "tm-pulse 1.6s var(--ease-standard) infinite"
  };
  if (variant === "circle") {
    const s = width || 40;
    return /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        ...base,
        display: "inline-block",
        width: s,
        height: s,
        borderRadius: "50%",
        ...style
      }
    });
  }
  if (variant === "rect") {
    return /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        ...base,
        display: "block",
        width: width || "100%",
        height: height || 120,
        borderRadius: "var(--radius-md)",
        ...style
      }
    });
  }
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8,
      width: width || "100%",
      ...style
    }
  }, Array.from({
    length: lines
  }).map((_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      ...base,
      display: "block",
      height: height || 12,
      borderRadius: 6,
      width: i === lines - 1 && lines > 1 ? "62%" : "100%"
    }
  })));
}
Object.assign(__ds_scope, { Skeleton });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Skeleton.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Spinner.jsx
try { (() => {
function ensureSpinKeyframes() {
  if (typeof document === "undefined" || document.getElementById("tm-kf-spin")) return;
  const s = document.createElement("style");
  s.id = "tm-kf-spin";
  s.textContent = "@keyframes tm-spin{to{transform:rotate(360deg)}}";
  document.head.appendChild(s);
}

/**
 * Spinner — unknown-duration wait under ~10s. Shows only after a
 * 300ms delay to avoid flicker; one spinner per region, never
 * per-element confetti. Longer operations → ProgressBar + step text.
 */
function Spinner({
  size = "md",
  delay = 300,
  label = "Cargando",
  inverse = false,
  style
}) {
  const [show, setShow] = React.useState(delay === 0);
  React.useEffect(() => {
    ensureSpinKeyframes();
    if (delay === 0) return;
    const t = setTimeout(() => setShow(true), delay);
    return () => clearTimeout(t);
  }, [delay]);
  const px = {
    sm: 16,
    md: 24,
    lg: 36
  }[size] || 24;
  if (!show) return /*#__PURE__*/React.createElement("span", {
    role: "status",
    "aria-label": label,
    style: {
      display: "inline-block",
      width: px,
      height: px,
      ...style
    }
  });
  return /*#__PURE__*/React.createElement("span", {
    role: "status",
    "aria-label": label,
    style: {
      display: "inline-flex",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: px,
      height: px,
      boxSizing: "border-box",
      border: `${Math.max(2, px / 9)}px solid ${inverse ? "var(--tm-alpha-white-300)" : "var(--color-bg-muted)"}`,
      borderTopColor: inverse ? "var(--tm-neutral-0)" : "var(--color-bg-accent-solid)",
      borderRadius: "50%",
      animation: "tm-spin 0.9s linear infinite",
      display: "inline-block"
    }
  }));
}
Object.assign(__ds_scope, { Spinner });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Spinner.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Toast.jsx
try { (() => {
/**
 * Toast — transient global confirmation ("Guardado", undo affordance).
 * Render inside a persistent aria-live="polite" region pinned at
 * z-toast. Auto-dismiss: 5s info/success; 8s+ or persistent for
 * errors/with actions (pause on hover). Never the sole carrier of
 * critical info — pair with inline state.
 */
function Toast({
  severity = "success",
  children,
  action,
  onAction,
  onDismiss,
  style
}) {
  const icons = {
    success: /*#__PURE__*/React.createElement("path", {
      d: "M22 11.08V12a10 10 0 1 1-5.93-9.14M22 4 12 14.01l-3-3"
    }),
    info: /*#__PURE__*/React.createElement("path", {
      d: "M12 16v-4M12 8h.01M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z"
    }),
    danger: /*#__PURE__*/React.createElement("path", {
      d: "M12 8v4M12 16h.01M12 22a10 10 0 1 0 0-20 10 10 0 0 0 0 20Z"
    }),
    warning: /*#__PURE__*/React.createElement("path", {
      d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3ZM12 9v4M12 17h.01"
    })
  };
  const iconColor = {
    success: "var(--tm-green-300)",
    info: "var(--tm-blue-300)",
    danger: "var(--tm-red-300)",
    warning: "var(--tm-amber-300)"
  }[severity];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      minWidth: 300,
      maxWidth: 420,
      padding: "var(--space-3) var(--space-4)",
      background: "var(--tm-navy-900)",
      color: "var(--tm-neutral-0)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-400)",
      fontFamily: "var(--font-sans)",
      boxSizing: "border-box",
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: iconColor,
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, icons[severity]), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      font: "var(--text-sm)",
      fontWeight: 500
    }
  }, children), action && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onAction,
    style: {
      font: "700 13px/1.2 var(--font-sans)",
      color: "var(--tm-red-300)",
      background: "transparent",
      border: "none",
      cursor: "pointer",
      padding: "4px 6px",
      borderRadius: "var(--radius-xs)",
      flexShrink: 0,
      whiteSpace: "nowrap"
    }
  }, action), onDismiss && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar notificaci\xF3n",
    onClick: onDismiss,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--tm-neutral-300)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  }))));
}

/**
 * ToastRegion — the persistent polite live region toasts render into.
 * Mount ONCE per app shell, bottom-start, z-toast; stack max ~3.
 */
function ToastRegion({
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    style: {
      position: "fixed",
      bottom: "var(--space-5)",
      left: "var(--space-5)",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-2)",
      zIndex: "var(--z-toast)",
      ...style
    }
  }, children);
}
Object.assign(__ds_scope, { Toast, ToastRegion });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Toast.jsx", error: String((e && e.message) || e) }); }

// components/forms/Checkbox.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Checkbox — independent yes/no; applies on form submit (an instant
 * on/off belongs to Switch). Supports indeterminate (mixed parent).
 * Real <input type="checkbox"> underneath; whole row is clickable.
 */
function Checkbox({
  label,
  description,
  checked,
  defaultChecked,
  indeterminate = false,
  onChange,
  disabled = false,
  invalid = false,
  value,
  name,
  style,
  ...rest
}) {
  const ref = React.useRef(null);
  const [focusRing, setFocusRing] = React.useState(false);
  const [isChecked, setChecked] = React.useState(defaultChecked || false);
  const on = checked !== undefined ? checked : isChecked;
  React.useEffect(() => {
    if (ref.current) ref.current.indeterminate = indeterminate;
  }, [indeterminate]);
  const boxOn = on || indeterminate;
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: "inline-flex",
      alignItems: "flex-start",
      gap: "var(--space-2)",
      fontFamily: "var(--font-sans)",
      cursor: disabled ? "default" : "pointer",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      position: "relative",
      minHeight: "var(--target-min)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("input", _extends({
    ref: ref,
    type: "checkbox",
    checked: checked,
    defaultChecked: defaultChecked,
    onChange: e => {
      if (checked === undefined) setChecked(e.target.checked);
      onChange && onChange(e);
    },
    disabled: disabled,
    value: value,
    name: name,
    "aria-invalid": invalid || undefined,
    onFocus: e => setFocusRing(e.target.matches(":focus-visible")),
    onBlur: () => setFocusRing(false),
    style: {
      position: "absolute",
      width: 18,
      height: 18,
      margin: 2,
      opacity: 0,
      cursor: "inherit"
    }
  }, rest)), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 18,
      height: 18,
      marginTop: 1,
      flexShrink: 0,
      boxSizing: "border-box",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      borderRadius: "var(--radius-xs)",
      border: boxOn ? "none" : `var(--border-width-md) solid ${invalid ? "var(--color-border-danger)" : "var(--color-border-strong)"}`,
      background: boxOn ? "var(--color-bg-brand-solid)" : "var(--input-bg)",
      color: "var(--color-fg-on-solid)",
      boxShadow: focusRing ? "var(--focus-ring)" : "none",
      transition: "background var(--duration-fast-2) var(--ease-standard)"
    }
  }, indeterminate ? /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "3.5",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M5 12h14"
  })) : on ? /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "3.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  })) : null), (label || description) && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-sm)",
      fontWeight: 500,
      color: "var(--color-fg-default)"
    }
  }, label), description && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)"
    }
  }, description)));
}
Object.assign(__ds_scope, { Checkbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Checkbox.jsx", error: String((e && e.message) || e) }); }

// components/forms/Combobox.jsx
try { (() => {
/**
 * Combobox — input + filtered listbox (APG combobox pattern). Use when
 * options > ~15 or the user knows what they're typing (bank, city,
 * agent). Focus stays in the input; aria-activedescendant tracks the
 * highlighted option; result count announced via live region. Empty
 * query shows all options; no-match shows a recovery row, never
 * dead silence.
 */
function Combobox({
  options = [],
  value,
  onChange,
  onInputChange,
  placeholder = "Busca…",
  emptyText = "Sin resultados — intenta otro término",
  size = "md",
  invalid = false,
  disabled = false,
  fullWidth = false,
  id,
  style
}) {
  const auto = React.useId();
  const listId = `tm-cbx-${auto}`;
  const rootRef = React.useRef(null);
  const listRef = React.useRef(null);
  const inputRef = React.useRef(null);
  const selected = options.find(o => o.value === value);
  const [query, setQuery] = React.useState(selected ? String(selected.label) : "");
  const [open, setOpen] = React.useState(false);
  const [active, setActive] = React.useState(0);
  const [focus, setFocus] = React.useState(false);
  React.useEffect(() => {
    const sel = options.find(o => o.value === value);
    if (sel && !open) setQuery(String(sel.label));
  }, [value, options, open]);
  const filtered = onInputChange ? options : options.filter(o => String(o.label).toLowerCase().includes(query.toLowerCase()));
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  React.useEffect(() => {
    if (open && listRef.current && active >= 0) {
      const el = listRef.current.children[active];
      if (!el) return;
      const top = el.offsetTop,
        bottom = top + el.offsetHeight,
        list = listRef.current;
      if (top < list.scrollTop) list.scrollTop = top;else if (bottom > list.scrollTop + list.clientHeight) list.scrollTop = bottom - list.clientHeight;
    }
  }, [open, active]);
  const commit = o => {
    if (!o) return;
    onChange && onChange(o.value, o);
    setQuery(String(o.label));
    setOpen(false);
  };
  const onKeyDown = e => {
    if (e.key === "ArrowDown") {
      e.preventDefault();
      if (!open) setOpen(true);else setActive(a => Math.min(filtered.length - 1, a + 1));
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      setActive(a => Math.max(0, a - 1));
    } else if (e.key === "Home" && open) {
      e.preventDefault();
      setActive(0);
    } else if (e.key === "End" && open) {
      e.preventDefault();
      setActive(filtered.length - 1);
    } else if (e.key === "Enter" && open) {
      e.preventDefault();
      commit(filtered[active]);
    } else if (e.key === "Escape" && open) {
      e.preventDefault();
      setOpen(false);
    } else if (e.key === "Tab") setOpen(false);
  };
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus || open ? "var(--color-border-focus)" : "var(--input-border)";
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: fullWidth ? "block" : "inline-block",
      width: fullWidth ? "100%" : undefined,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus || open ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 220,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "8"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m21 21-4.3-4.3"
  })), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    id: id,
    type: "text",
    role: "combobox",
    "aria-expanded": open,
    "aria-controls": listId,
    "aria-autocomplete": "list",
    "aria-activedescendant": open && filtered[active] ? `${listId}-${active}` : undefined,
    "aria-invalid": invalid || undefined,
    placeholder: placeholder,
    autoComplete: "off",
    value: query,
    disabled: disabled,
    onChange: e => {
      setQuery(e.target.value);
      setActive(0);
      setOpen(true);
      onInputChange && onInputChange(e.target.value);
    },
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    onKeyDown: onKeyDown,
    onClick: () => setOpen(true),
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: "var(--font-sans)",
      fontSize,
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }), query && !disabled && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Limpiar b\xFAsqueda",
    onClick: () => {
      setQuery("");
      setOpen(true);
      onChange && onChange(null, null);
      onInputChange && onInputChange("");
      if (inputRef.current) inputRef.current.focus();
    },
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 20,
      height: 20,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })))), open && /*#__PURE__*/React.createElement("ul", {
    ref: listRef,
    id: listId,
    role: "listbox",
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      right: fullWidth ? 0 : undefined,
      minWidth: "100%",
      maxHeight: 300,
      overflowY: "auto",
      margin: 0,
      padding: "var(--space-1)",
      listStyle: "none",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      boxSizing: "border-box"
    }
  }, filtered.length === 0 && /*#__PURE__*/React.createElement("li", {
    role: "presentation",
    style: {
      padding: "var(--space-3)",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, emptyText), filtered.map((o, i) => /*#__PURE__*/React.createElement("li", {
    key: String(o.value),
    id: `${listId}-${i}`,
    role: "option",
    "aria-selected": o.value === value,
    onMouseEnter: () => setActive(i),
    onMouseDown: e => e.preventDefault(),
    onClick: () => commit(o),
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-2)",
      padding: "8px 10px",
      borderRadius: "var(--radius-sm)",
      font: `400 ${fontSize}px/1.35 var(--font-sans)`,
      color: "var(--color-fg-default)",
      background: i === active ? "var(--color-bg-selected)" : "transparent",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, o.label), o.description && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)"
    }
  }, o.description)), o.value === value && /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-brand)",
    strokeWidth: "2.5",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  }))))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, open ? `${filtered.length} resultado${filtered.length === 1 ? "" : "s"}` : ""));
}
Object.assign(__ds_scope, { Combobox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Combobox.jsx", error: String((e && e.message) || e) }); }

// components/forms/DateInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * DateInput — input-first date entry (es-MX: dd/mm/aaaa, never
 * ambiguous mm/dd). Typing digits auto-inserts slashes; paste of
 * dd/mm/aaaa, dd-mm-aaaa or ddmmaaaa normalizes. Numeric mobile
 * keyboard. A calendar popover is an aid, not the only path —
 * this control stands alone. Pair with <Field>.
 */
const DateInput = React.forwardRef(function DateInput({
  value,
  onChange,
  size = "md",
  invalid = false,
  disabled = false,
  readOnly = false,
  fullWidth = false,
  style,
  ...rest
}, ref) {
  const [focus, setFocus] = React.useState(false);
  const maskDate = raw => {
    const d = raw.replace(/\D/g, "").slice(0, 8);
    if (d.length <= 2) return d;
    if (d.length <= 4) return `${d.slice(0, 2)}/${d.slice(2)}`;
    return `${d.slice(0, 2)}/${d.slice(2, 4)}/${d.slice(4)}`;
  };
  const borderColor = invalid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: fullWidth ? "flex" : "inline-flex",
      width: fullWidth ? "100%" : undefined,
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: readOnly ? "var(--color-bg-subtle)" : disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 160,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("rect", {
    x: "3",
    y: "4",
    width: "18",
    height: "18",
    rx: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M16 2v4M8 2v4M3 10h18"
  })), /*#__PURE__*/React.createElement("input", _extends({
    ref: ref,
    type: "text",
    inputMode: "numeric",
    placeholder: "dd/mm/aaaa",
    autoComplete: "off",
    value: value,
    onChange: e => onChange && onChange(maskDate(e.target.value)),
    disabled: disabled,
    readOnly: readOnly,
    "aria-invalid": invalid || undefined,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: "var(--font-sans)",
      fontSize,
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }, rest)));
});
Object.assign(__ds_scope, { DateInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/DateInput.jsx", error: String((e && e.message) || e) }); }

// components/forms/DatePicker.jsx
try { (() => {
const MONTHS_ES = ["enero", "febrero", "marzo", "abril", "mayo", "junio", "julio", "agosto", "septiembre", "octubre", "noviembre", "diciembre"];
const DAYS_ES = ["L", "M", "M", "J", "V", "S", "D"];
function parseDMY(s) {
  const m = /^(\d{2})\/(\d{2})\/(\d{4})$/.exec(s || "");
  if (!m) return null;
  const d = new Date(Number(m[3]), Number(m[2]) - 1, Number(m[1]));
  return d.getDate() === Number(m[1]) && d.getMonth() === Number(m[2]) - 1 ? d : null;
}
function fmtDMY(d) {
  return `${String(d.getDate()).padStart(2, "0")}/${String(d.getMonth() + 1).padStart(2, "0")}/${d.getFullYear()}`;
}
function sameDay(a, b) {
  return a && b && a.getFullYear() === b.getFullYear() && a.getMonth() === b.getMonth() && a.getDate() === b.getDate();
}

/**
 * DatePicker — input-first (typed dd/mm/aaaa with mask, es-MX order)
 * with a calendar popover as an aid, never the only path. Calendar is
 * an APG grid: arrows move day, PgUp/PgDn month, Shift+PgUp/PgDn year,
 * Home/End week bounds, Enter selects, Esc returns to the input.
 * Today marker is independent of selection. min/max render
 * disabled-with-reason via the title attr; validate server-side too.
 */
function DatePicker({
  value = "",
  onChange,
  min,
  max,
  size = "md",
  invalid = false,
  disabled = false,
  fullWidth = false,
  id,
  style
}) {
  const [open, setOpen] = React.useState(false);
  const [focus, setFocus] = React.useState(false);
  const selected = parseDMY(value);
  const today = new Date();
  const [view, setView] = React.useState(() => selected || today);
  const [focusDay, setFocusDay] = React.useState(() => selected || today);
  const rootRef = React.useRef(null);
  const gridRef = React.useRef(null);
  const btnRef = React.useRef(null);
  const minD = parseDMY(min),
    maxD = parseDMY(max);
  const maskDate = raw => {
    const d = raw.replace(/\D/g, "").slice(0, 8);
    if (d.length <= 2) return d;
    if (d.length <= 4) return `${d.slice(0, 2)}/${d.slice(2)}`;
    return `${d.slice(0, 2)}/${d.slice(2, 4)}/${d.slice(4)}`;
  };
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  React.useEffect(() => {
    if (open && gridRef.current) {
      const el = gridRef.current.querySelector('[tabindex="0"]');
      if (el) el.focus();
    }
  }, [open, view]);
  const openCal = () => {
    const base = selected || today;
    setView(base);
    setFocusDay(base);
    setOpen(true);
  };
  const disabledDay = d => minD && d < minD && !sameDay(d, minD) || maxD && d > maxD && !sameDay(d, maxD);
  const pick = d => {
    if (disabledDay(d)) return;
    onChange && onChange(fmtDMY(d));
    setOpen(false);
    if (btnRef.current) btnRef.current.focus();
  };
  const moveFocus = (days, months, years) => {
    const d = new Date(focusDay);
    if (days) d.setDate(d.getDate() + days);
    if (months) d.setMonth(d.getMonth() + months);
    if (years) d.setFullYear(d.getFullYear() + years);
    setFocusDay(d);
    if (d.getMonth() !== view.getMonth() || d.getFullYear() !== view.getFullYear()) setView(d);
  };
  const onGridKey = e => {
    const map = {
      ArrowRight: () => moveFocus(1),
      ArrowLeft: () => moveFocus(-1),
      ArrowDown: () => moveFocus(7),
      ArrowUp: () => moveFocus(-7),
      PageDown: () => moveFocus(0, e.shiftKey ? 0 : 1, e.shiftKey ? 1 : 0),
      PageUp: () => moveFocus(0, e.shiftKey ? 0 : -1, e.shiftKey ? -1 : 0),
      Home: () => {
        const dow = (focusDay.getDay() + 6) % 7;
        moveFocus(-dow);
      },
      End: () => {
        const dow = (focusDay.getDay() + 6) % 7;
        moveFocus(6 - dow);
      }
    };
    if (map[e.key]) {
      e.preventDefault();
      map[e.key]();
    } else if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      pick(focusDay);
    } else if (e.key === "Escape") {
      e.preventDefault();
      setOpen(false);
      if (btnRef.current) btnRef.current.focus();
    }
  };

  // build month grid (weeks start Monday)
  const first = new Date(view.getFullYear(), view.getMonth(), 1);
  const startOffset = (first.getDay() + 6) % 7;
  const daysInMonth = new Date(view.getFullYear(), view.getMonth() + 1, 0).getDate();
  const cells = [];
  for (let i = 0; i < startOffset; i++) cells.push(null);
  for (let d = 1; d <= daysInMonth; d++) cells.push(new Date(view.getFullYear(), view.getMonth(), d));
  while (cells.length % 7 !== 0) cells.push(null);
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus || open ? "var(--color-border-focus)" : "var(--input-border)";
  const navBtn = {
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 28,
    height: 28,
    background: "transparent",
    border: "none",
    color: "var(--color-fg-muted)",
    cursor: "pointer",
    borderRadius: "var(--radius-sm)"
  };
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: fullWidth ? "block" : "inline-block",
      width: fullWidth ? "100%" : undefined,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus || open ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 180,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    id: id,
    type: "text",
    inputMode: "numeric",
    placeholder: "dd/mm/aaaa",
    autoComplete: "off",
    value: value,
    disabled: disabled,
    "aria-invalid": invalid || undefined,
    onChange: e => onChange && onChange(maskDate(e.target.value)),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: "var(--font-sans)",
      fontSize,
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }), /*#__PURE__*/React.createElement("button", {
    ref: btnRef,
    type: "button",
    "aria-label": "Abrir calendario",
    "aria-expanded": open,
    disabled: disabled,
    onClick: () => open ? setOpen(false) : openCal(),
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "3",
    y: "4",
    width: "18",
    height: "18",
    rx: "2"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M16 2v4M8 2v4M3 10h18"
  })))), open && /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    "aria-label": "Calendario",
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      width: 264,
      padding: "var(--space-3)",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      marginBottom: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Mes anterior",
    style: navBtn,
    onClick: () => {
      const d = new Date(view);
      d.setMonth(d.getMonth() - 1);
      setView(d);
      setFocusDay(d);
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m15 18-6-6 6-6"
  }))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      font: "700 13px/1.2 var(--font-sans)",
      color: "var(--color-fg-default)",
      textTransform: "capitalize"
    }
  }, MONTHS_ES[view.getMonth()], " ", view.getFullYear()), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Mes siguiente",
    style: navBtn,
    onClick: () => {
      const d = new Date(view);
      d.setMonth(d.getMonth() + 1);
      setView(d);
      setFocusDay(d);
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m9 18 6-6-6-6"
  })))), /*#__PURE__*/React.createElement("div", {
    ref: gridRef,
    role: "grid",
    onKeyDown: onGridKey,
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(7, 1fr)",
      gap: 2
    }
  }, DAYS_ES.map((d, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    role: "columnheader",
    "aria-hidden": "true",
    style: {
      textAlign: "center",
      font: "700 10px/1.6 var(--font-sans)",
      color: "var(--color-fg-subtle)"
    }
  }, d)), cells.map((d, i) => {
    if (!d) return /*#__PURE__*/React.createElement("span", {
      key: `e${i}`,
      role: "presentation"
    });
    const isSel = sameDay(d, selected);
    const isToday = sameDay(d, today);
    const isFocus = sameDay(d, focusDay);
    const dis = disabledDay(d);
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      role: "gridcell",
      tabIndex: isFocus ? 0 : -1,
      "aria-selected": isSel || undefined,
      "aria-disabled": dis || undefined,
      title: dis ? minD && d < minD ? `Disponible desde ${min}` : `Disponible hasta ${max}` : undefined,
      onClick: () => pick(d),
      onFocus: () => setFocusDay(d),
      style: {
        height: 30,
        border: "none",
        borderRadius: "var(--radius-sm)",
        font: `600 12.5px/1 var(--font-sans)`,
        fontVariantNumeric: "var(--numeric-tabular)",
        background: isSel ? "var(--color-bg-brand-solid)" : "transparent",
        color: dis ? "var(--color-fg-disabled)" : isSel ? "var(--color-fg-on-solid)" : "var(--color-fg-default)",
        boxShadow: isToday && !isSel ? "inset 0 0 0 1.5px var(--color-border-strong)" : "none",
        cursor: dis ? "default" : "pointer"
      },
      onMouseEnter: e => {
        if (!isSel && !dis) e.currentTarget.style.background = "var(--color-bg-hover)";
      },
      onMouseLeave: e => {
        if (!isSel) e.currentTarget.style.background = "transparent";
      }
    }, d.getDate());
  }))));
}
Object.assign(__ds_scope, { DatePicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/DatePicker.jsx", error: String((e && e.message) || e) }); }

// components/forms/Field.jsx
try { (() => {
/**
 * Field — the wrapper every form control lives in: visible top label,
 * optional/required marker, help text, error message, counter. Wires
 * id + aria-describedby + aria-invalid onto its single child control.
 *
 * Rules: label always visible above (never placeholder-as-label);
 * mark the MINORITY — if most fields are required, mark "(opcional)"
 * instead of asterisks; error = what's wrong + how to fix ("El CLABE
 * debe tener 18 dígitos (ingresaste 16)"), never "Entrada inválida".
 */
function Field({
  label,
  children,
  help,
  error,
  optional = false,
  required = false,
  count,
  max,
  id: idProp,
  style
}) {
  const auto = React.useId();
  const id = idProp || `tm-f-${auto}`;
  const helpId = `${id}-help`;
  const errId = `${id}-err`;
  const describedBy = [error ? errId : null, help && !error ? helpId : null].filter(Boolean).join(" ") || undefined;
  const child = React.Children.only(children);
  const control = React.cloneElement(child, {
    id,
    invalid: !!error || child.props.invalid,
    "aria-describedby": describedBy,
    "aria-required": required || undefined,
    fullWidth: child.props.fullWidth !== undefined ? child.props.fullWidth : true
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: "var(--field-gap)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      justifyContent: "space-between",
      gap: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement("label", {
    htmlFor: id,
    style: {
      font: "600 14px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, label, required && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      color: "var(--color-fg-danger)"
    }
  }, " *"), optional && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "400 13px/1.4 var(--font-sans)",
      color: "var(--color-fg-subtle)"
    }
  }, " (opcional)")), max != null && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      font: "var(--text-xs)",
      color: (count || 0) > max ? "var(--color-fg-danger)" : "var(--color-fg-subtle)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, count || 0, "/", max)), control, error ? /*#__PURE__*/React.createElement("div", {
    id: errId,
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: "var(--space-1)",
      font: "var(--text-sm)",
      color: "var(--color-fg-danger)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    style: {
      marginTop: 2,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "9"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 8v4M12 16h.01"
  })), /*#__PURE__*/React.createElement("span", null, error)) : help ? /*#__PURE__*/React.createElement("div", {
    id: helpId,
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, help) : null);
}
Object.assign(__ds_scope, { Field });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Field.jsx", error: String((e && e.message) || e) }); }

// components/forms/FileUpload.jsx
try { (() => {
function fmtSize(bytes) {
  if (bytes >= 1048576) return `${(bytes / 1048576).toFixed(1)} MB`;
  if (bytes >= 1024) return `${Math.round(bytes / 1024)} KB`;
  return `${bytes} B`;
}

/**
 * FileUpload — dropzone + always-present "Buscar archivo" button (the
 * keyboard/SR path; real <input type="file"> underneath). Limits are
 * declared BEFORE interaction; client-side validation runs pre-upload
 * (server must re-validate). File rows: name · size · remove/error.
 * Status changes announce via a polite live region.
 */
function FileUpload({
  accept,
  multiple = false,
  maxSizeMb,
  hint,
  onFiles,
  disabled = false,
  style
}) {
  const inputRef = React.useRef(null);
  const [files, setFiles] = React.useState([]);
  const [over, setOver] = React.useState(false);
  const [announce, setAnnounce] = React.useState("");
  const addFiles = list => {
    const next = Array.from(list).map(f => ({
      file: f,
      name: f.name,
      size: f.size,
      error: maxSizeMb && f.size > maxSizeMb * 1048576 ? `Supera el límite de ${maxSizeMb} MB` : null
    }));
    const all = multiple ? [...files, ...next] : next;
    setFiles(all);
    setAnnounce(`${next.length} archivo${next.length === 1 ? "" : "s"} agregado${next.length === 1 ? "" : "s"}`);
    onFiles && onFiles(all.filter(f => !f.error).map(f => f.file));
  };
  const remove = i => {
    const all = files.filter((_, j) => j !== i);
    setFiles(all);
    setAnnounce("Archivo eliminado");
    onFiles && onFiles(all.filter(f => !f.error).map(f => f.file));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    onDragOver: e => {
      e.preventDefault();
      if (!disabled) setOver(true);
    },
    onDragLeave: () => setOver(false),
    onDrop: e => {
      e.preventDefault();
      setOver(false);
      if (!disabled) addFiles(e.dataTransfer.files);
    },
    style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: "var(--space-2)",
      padding: "var(--space-5)",
      border: `2px dashed ${over ? "var(--color-border-focus)" : "var(--color-border-strong)"}`,
      borderRadius: "var(--radius-md)",
      background: over ? "var(--color-bg-brand-subtle)" : "var(--color-bg-subtle)",
      textAlign: "center",
      transition: "border-color var(--duration-fast-2) var(--ease-standard), background var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "24",
    height: "24",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M17 8l-5-5-5 5M12 3v12"
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, "Arrastra tu archivo aqu\xED, o"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: disabled,
    onClick: () => inputRef.current && inputRef.current.click(),
    style: {
      font: "600 var(--control-font-size-sm)/1.2 var(--font-sans)",
      color: "var(--color-fg-brand)",
      background: "transparent",
      border: "1.5px solid currentColor",
      borderRadius: "var(--control-radius)",
      height: "var(--control-height-sm)",
      paddingInline: "var(--control-pad-x-sm)",
      letterSpacing: "var(--control-tracking)",
      textTransform: "var(--control-transform)",
      cursor: disabled ? "default" : "pointer"
    }
  }, "Buscar archivo"), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, hint || [accept, maxSizeMb ? `máx. ${maxSizeMb} MB` : null].filter(Boolean).join(" · ")), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    type: "file",
    accept: accept,
    multiple: multiple,
    disabled: disabled,
    onChange: e => {
      addFiles(e.target.files);
      e.target.value = "";
    },
    style: {
      display: "none"
    },
    "aria-hidden": "true",
    tabIndex: -1
  })), files.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      listStyle: "none",
      margin: "var(--space-2) 0 0",
      padding: 0,
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-1)"
    }
  }, files.map((f, i) => /*#__PURE__*/React.createElement("li", {
    key: `${f.name}-${i}`,
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      padding: "var(--space-2) var(--space-3)",
      border: `1px solid ${f.error ? "var(--color-border-danger)" : "var(--color-border-subtle)"}`,
      borderRadius: "var(--radius-sm)",
      background: f.error ? "var(--color-bg-danger-subtle)" : "var(--color-bg-surface)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: f.error ? "var(--color-fg-danger)" : "var(--color-fg-subtle)",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M14 2v6h6"
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      minWidth: 0,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap",
      font: "var(--text-sm)",
      color: "var(--color-fg-default)"
    }
  }, f.name), f.error ? /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-danger)",
      flexShrink: 0
    }
  }, f.error) : /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      flexShrink: 0,
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, fmtSize(f.size)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => remove(i),
    "aria-label": `Eliminar ${f.name}`,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      border: "none",
      background: "transparent",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })))))), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, announce));
}
Object.assign(__ds_scope, { FileUpload });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/FileUpload.jsx", error: String((e && e.message) || e) }); }

// components/forms/NumberInput.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * NumberInput — stepper for small integer-ish quantities (plazos,
 * dependientes). ±  buttons + typed entry; ↑↓ arrows step; min/max
 * clamp on blur; tabular figures. For money use AmountInput; for
 * wide ranges use Slider or plain Input.
 */
function NumberInput({
  value,
  onChange,
  min = -Infinity,
  max = Infinity,
  step = 1,
  suffix,
  size = "md",
  invalid = false,
  disabled = false,
  fullWidth = false,
  label = "Cantidad",
  style,
  ...rest
}) {
  const [focus, setFocus] = React.useState(false);
  const [draft, setDraft] = React.useState(value != null ? String(value) : "");
  React.useEffect(() => {
    if (!focus) setDraft(value != null ? String(value) : "");
  }, [value, focus]);
  const clamp = n => Math.min(max, Math.max(min, n));
  const commit = raw => {
    const n = parseFloat(String(raw).replace(",", "."));
    if (isNaN(n)) {
      onChange && onChange(null);
      setDraft("");
      return;
    }
    const c = clamp(n);
    onChange && onChange(c);
    setDraft(String(c));
  };
  const nudge = d => {
    const base = typeof value === "number" ? value : parseFloat(draft) || 0;
    const c = clamp(base + d * step);
    onChange && onChange(c);
    setDraft(String(c));
  };
  const atMin = typeof value === "number" && value <= min;
  const atMax = typeof value === "number" && value >= max;
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  const stepBtn = (dir, dis) => ({
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 34,
    alignSelf: "stretch",
    flexShrink: 0,
    border: "none",
    background: "transparent",
    color: dis ? "var(--color-fg-disabled)" : "var(--color-fg-muted)",
    cursor: dis || disabled ? "default" : "pointer",
    [dir === "minus" ? "borderRight" : "borderLeft"]: "1px solid var(--color-border-subtle)"
  });
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: fullWidth ? "flex" : "inline-flex",
      width: fullWidth ? "100%" : undefined,
      alignItems: "center",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      overflow: "hidden",
      minWidth: fullWidth ? 0 : 150,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": `Disminuir ${label}`,
    disabled: disabled || atMin,
    onClick: () => nudge(-1),
    style: stepBtn("minus", atMin)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M5 12h14"
  }))), /*#__PURE__*/React.createElement("input", _extends({
    type: "text",
    inputMode: "decimal",
    value: draft,
    "aria-label": label,
    "aria-invalid": invalid || undefined,
    disabled: disabled,
    onChange: e => setDraft(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => {
      setFocus(false);
      commit(draft);
    },
    onKeyDown: e => {
      if (e.key === "ArrowUp") {
        e.preventDefault();
        nudge(1);
      } else if (e.key === "ArrowDown") {
        e.preventDefault();
        nudge(-1);
      } else if (e.key === "Enter") commit(draft);
    },
    style: {
      flex: 1,
      minWidth: 0,
      textAlign: "center",
      font: `600 ${fontSize}px/1.2 var(--font-sans)`,
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }, rest)), suffix && /*#__PURE__*/React.createElement("span", {
    style: {
      font: `400 ${fontSize - 2}px/1.2 var(--font-sans)`,
      color: "var(--color-fg-subtle)",
      paddingRight: 6,
      flexShrink: 0
    }
  }, suffix), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": `Aumentar ${label}`,
    disabled: disabled || atMax,
    onClick: () => nudge(1),
    style: stepBtn("plus", atMax)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.5",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 5v14M5 12h14"
  }))));
}
Object.assign(__ds_scope, { NumberInput });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/NumberInput.jsx", error: String((e && e.message) || e) }); }

// components/forms/RadioGroup.jsx
try { (() => {
/**
 * RadioGroup — one-of-N (N ≤ ~6; more → Select). Renders a
 * <fieldset> with visible <legend> (mandatory). Native radios give
 * the correct roving arrow-key behavior for free.
 */
function RadioGroup({
  legend,
  options = [],
  value,
  defaultValue,
  onChange,
  name,
  disabled = false,
  invalid = false,
  error,
  direction = "column",
  style
}) {
  const auto = React.useId();
  const groupName = name || `tm-rg-${auto}`;
  const [internal, setInternal] = React.useState(defaultValue);
  const current = value !== undefined ? value : internal;
  const [focusVal, setFocusVal] = React.useState(null);
  return /*#__PURE__*/React.createElement("fieldset", {
    "aria-invalid": invalid || !!error || undefined,
    style: {
      border: "none",
      margin: 0,
      padding: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("legend", {
    style: {
      font: "600 14px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)",
      padding: 0,
      marginBottom: "var(--space-2)"
    }
  }, legend), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: direction,
      gap: direction === "column" ? "var(--space-2)" : "var(--space-4)",
      flexWrap: "wrap"
    }
  }, options.map(o => {
    const on = current === o.value;
    const dis = disabled || o.disabled;
    return /*#__PURE__*/React.createElement("label", {
      key: String(o.value),
      style: {
        display: "inline-flex",
        alignItems: "flex-start",
        gap: "var(--space-2)",
        cursor: dis ? "default" : "pointer",
        opacity: dis ? "var(--opacity-disabled)" : 1,
        position: "relative",
        minHeight: "var(--target-min)"
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "radio",
      name: groupName,
      value: String(o.value),
      checked: on,
      onChange: () => {
        if (value === undefined) setInternal(o.value);
        onChange && onChange(o.value);
      },
      disabled: dis,
      onFocus: e => setFocusVal(e.target.matches(":focus-visible") ? o.value : null),
      onBlur: () => setFocusVal(null),
      style: {
        position: "absolute",
        width: 18,
        height: 18,
        margin: 2,
        opacity: 0,
        cursor: "inherit"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: 18,
        height: 18,
        marginTop: 1,
        flexShrink: 0,
        boxSizing: "border-box",
        borderRadius: "50%",
        border: on ? "6px solid var(--color-bg-brand-solid)" : `var(--border-width-md) solid ${invalid || error ? "var(--color-border-danger)" : "var(--color-border-strong)"}`,
        background: "var(--input-bg)",
        boxShadow: focusVal === o.value ? "var(--focus-ring)" : "none",
        transition: "border var(--duration-fast-2) var(--ease-standard)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 2
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: "var(--text-sm)",
        fontWeight: 500,
        color: "var(--color-fg-default)"
      }
    }, o.label), o.description && /*#__PURE__*/React.createElement("span", {
      style: {
        font: "var(--text-xs)",
        color: "var(--color-fg-muted)"
      }
    }, o.description)));
  })), error && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-2)",
      font: "var(--text-sm)",
      color: "var(--color-fg-danger)"
    }
  }, error));
}
Object.assign(__ds_scope, { RadioGroup });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/RadioGroup.jsx", error: String((e && e.message) || e) }); }

// components/forms/Rating.jsx
try { (() => {
const Star = ({
  filled,
  size
}) => /*#__PURE__*/React.createElement("svg", {
  "aria-hidden": "true",
  width: size,
  height: size,
  viewBox: "0 0 24 24",
  fill: filled ? "var(--tm-amber-400)" : "none",
  stroke: filled ? "var(--tm-amber-500)" : "var(--color-border-strong)",
  strokeWidth: "1.6",
  strokeLinejoin: "round"
}, /*#__PURE__*/React.createElement("path", {
  d: "M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01z"
}));

/**
 * Rating — 1–N satisfaction capture (NPS post-flow, atención). Native
 * radios (roving arrows for free) with es-MX value labels; value is
 * never color/shape alone — the numeric label renders beside the
 * stars. readOnly renders a static aria-labeled figure.
 */
function Rating({
  value = 0,
  onChange,
  max = 5,
  label = "Calificación",
  size = "md",
  readOnly = false,
  disabled = false,
  showValue = true,
  style
}) {
  const [hover, setHover] = React.useState(0);
  const auto = React.useId();
  const px = {
    sm: 18,
    md: 24,
    lg: 30
  }[size] || 24;
  const shown = hover || value;
  if (readOnly) {
    return /*#__PURE__*/React.createElement("span", {
      role: "img",
      "aria-label": `${label}: ${value} de ${max}`,
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: 6,
        ...style
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "inline-flex",
        gap: 2
      }
    }, Array.from({
      length: max
    }).map((_, i) => /*#__PURE__*/React.createElement(Star, {
      key: i,
      filled: i < value,
      size: px
    }))), showValue && /*#__PURE__*/React.createElement("span", {
      style: {
        font: "600 13px/1.2 var(--font-sans)",
        color: "var(--color-fg-muted)",
        fontVariantNumeric: "var(--numeric-tabular)"
      }
    }, value, " de ", max));
  }
  return /*#__PURE__*/React.createElement("fieldset", {
    style: {
      border: "none",
      margin: 0,
      padding: 0,
      display: "inline-flex",
      alignItems: "center",
      gap: 8,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("legend", {
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      gap: 2
    },
    onMouseLeave: () => setHover(0)
  }, Array.from({
    length: max
  }).map((_, i) => {
    const n = i + 1;
    return /*#__PURE__*/React.createElement("label", {
      key: n,
      onMouseEnter: () => !disabled && setHover(n),
      style: {
        position: "relative",
        display: "inline-flex",
        cursor: disabled ? "default" : "pointer",
        minWidth: "var(--target-min)",
        minHeight: "var(--target-min)",
        alignItems: "center",
        justifyContent: "center"
      }
    }, /*#__PURE__*/React.createElement("input", {
      type: "radio",
      name: `tm-rate-${auto}`,
      value: n,
      checked: value === n,
      disabled: disabled,
      onChange: () => onChange && onChange(n),
      "aria-label": `${n} de ${max}`,
      style: {
        position: "absolute",
        inset: 0,
        opacity: 0,
        cursor: "inherit",
        margin: 0
      }
    }), /*#__PURE__*/React.createElement(Star, {
      filled: n <= shown,
      size: px
    }));
  })), showValue && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      font: "600 13px/1.2 var(--font-sans)",
      color: "var(--color-fg-muted)",
      fontVariantNumeric: "var(--numeric-tabular)",
      minWidth: 44
    }
  }, value ? `${value} de ${max}` : "—"));
}
Object.assign(__ds_scope, { Rating });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Rating.jsx", error: String((e && e.message) || e) }); }

// components/forms/Select.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function ensureVisible(list, el) {
  if (!list || !el) return;
  const top = el.offsetTop,
    bottom = top + el.offsetHeight;
  if (top < list.scrollTop) list.scrollTop = top;else if (bottom > list.scrollTop + list.clientHeight) list.scrollTop = bottom - list.clientHeight;
}

/**
 * Select — custom listbox (APG select-only combobox pattern).
 * Trigger matches Input sizing; popup at elevation-300, max-height 320.
 * Keyboard: ↓/Enter/Space opens · ↑↓ move · Home/End · type-ahead ·
 * Enter selects · Esc closes and returns focus.
 *
 * Don't use for binary choices (→ Switch/Checkbox/Radio). Never
 * auto-submit on change (WCAG 3.2.2).
 */
function Select({
  options = [],
  value,
  onChange,
  placeholder = "Selecciona…",
  size = "md",
  invalid = false,
  disabled = false,
  fullWidth = false,
  id,
  style,
  ...rest
}) {
  const [open, setOpen] = React.useState(false);
  const [active, setActive] = React.useState(-1);
  const [focus, setFocus] = React.useState(false);
  const rootRef = React.useRef(null);
  const listRef = React.useRef(null);
  const typeRef = React.useRef({
    s: "",
    t: 0
  });
  const auto = React.useId();
  const listId = `tm-sel-${auto}`;
  const selIdx = options.findIndex(o => o.value === value);
  const selected = options[selIdx];
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  React.useEffect(() => {
    if (open && listRef.current && active >= 0) {
      ensureVisible(listRef.current, listRef.current.children[active]);
    }
  }, [open, active]);
  const openList = () => {
    setOpen(true);
    setActive(selIdx >= 0 ? selIdx : 0);
  };
  const commit = i => {
    const o = options[i];
    if (!o || o.disabled) return;
    onChange && onChange(o.value, o);
    setOpen(false);
  };
  const onKeyDown = e => {
    if (disabled) return;
    const move = d => {
      let i = active < 0 ? selIdx : active;
      do {
        i = Math.min(options.length - 1, Math.max(0, i + d));
      } while (options[i] && options[i].disabled && i > 0 && i < options.length - 1);
      setActive(i);
    };
    if (!open && ["ArrowDown", "ArrowUp", "Enter", " "].includes(e.key)) {
      e.preventDefault();
      openList();
      return;
    }
    if (!open) return;
    if (e.key === "ArrowDown") {
      e.preventDefault();
      move(1);
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      move(-1);
    } else if (e.key === "Home") {
      e.preventDefault();
      setActive(0);
    } else if (e.key === "End") {
      e.preventDefault();
      setActive(options.length - 1);
    } else if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();
      commit(active);
    } else if (e.key === "Escape") {
      e.preventDefault();
      setOpen(false);
    } else if (e.key === "Tab") setOpen(false);else if (/^[a-zA-Z0-9ñÑáéíóú]$/.test(e.key)) {
      const now = Date.now();
      typeRef.current.s = (now - typeRef.current.t < 600 ? typeRef.current.s : "") + e.key.toLowerCase();
      typeRef.current.t = now;
      const i = options.findIndex(o => String(o.label).toLowerCase().startsWith(typeRef.current.s));
      if (i >= 0) setActive(i);
    }
  };
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus || open ? "var(--color-border-focus)" : "var(--input-border)";
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: fullWidth ? "block" : "inline-block",
      width: fullWidth ? "100%" : undefined,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    id: id,
    role: "combobox",
    "aria-expanded": open,
    "aria-haspopup": "listbox",
    "aria-controls": listId,
    "aria-invalid": invalid || undefined,
    disabled: disabled,
    onClick: () => open ? setOpen(false) : openList(),
    onKeyDown: onKeyDown,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      width: fullWidth ? "100%" : undefined,
      minWidth: fullWidth ? 0 : 220,
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      font: `400 ${fontSize}px/1.2 var(--font-sans)`,
      color: selected ? "var(--color-fg-default)" : "var(--color-fg-subtle)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus || open ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      cursor: disabled ? "default" : "pointer",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      textAlign: "left",
      transition: "border-color var(--duration-fast-2) var(--ease-standard)"
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, selected ? selected.label : placeholder), /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0,
      color: "var(--color-fg-subtle)",
      transform: open ? "rotate(180deg)" : "none",
      transition: "transform var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "m6 9 6 6 6-6"
  }))), open && /*#__PURE__*/React.createElement("ul", {
    ref: listRef,
    id: listId,
    role: "listbox",
    "aria-label": placeholder,
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      right: fullWidth ? 0 : undefined,
      minWidth: "100%",
      maxHeight: 320,
      overflowY: "auto",
      margin: 0,
      padding: "var(--space-1)",
      listStyle: "none",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      boxSizing: "border-box"
    }
  }, options.length === 0 && /*#__PURE__*/React.createElement("li", {
    role: "presentation",
    style: {
      padding: "var(--space-3)",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, "Sin opciones"), options.map((o, i) => /*#__PURE__*/React.createElement("li", {
    key: String(o.value),
    role: "option",
    "aria-selected": o.value === value,
    "aria-disabled": o.disabled || undefined,
    onMouseEnter: () => setActive(i),
    onMouseDown: e => e.preventDefault(),
    onClick: () => commit(i),
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-2)",
      padding: "8px 10px",
      borderRadius: "var(--radius-sm)",
      font: `400 ${fontSize}px/1.35 var(--font-sans)`,
      color: o.disabled ? "var(--color-fg-disabled)" : "var(--color-fg-default)",
      background: i === active && !o.disabled ? "var(--color-bg-selected)" : "transparent",
      cursor: o.disabled ? "default" : "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, o.label), o.description && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)"
    }
  }, o.description)), o.value === value && /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-brand)",
    strokeWidth: "2.5",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  }))))));
}
Object.assign(__ds_scope, { Select });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Select.jsx", error: String((e && e.message) || e) }); }

// components/forms/Slider.jsx
try { (() => {
/**
 * Slider — numeric selection over a range (e.g. loan amount).
 * role="slider" + aria-valuemin/max/now/text; keyboard: arrows ±step,
 * PgUp/PgDn ±10×, Home/End; visible value output is mandatory.
 * Dragging has a keyboard alternative by construction (WCAG 2.5.7);
 * thumb hit target ≥44px despite the thin track.
 */
function Slider({
  min = 0,
  max = 100,
  step = 1,
  value,
  defaultValue,
  onChange,
  format,
  label,
  disabled = false,
  style
}) {
  const [internal, setInternal] = React.useState(defaultValue != null ? defaultValue : min);
  const val = value !== undefined ? value : internal;
  const [focusRing, setFocusRing] = React.useState(false);
  const trackRef = React.useRef(null);
  const clamp = v => Math.min(max, Math.max(min, Math.round(v / step) * step));
  const set = v => {
    const c = clamp(v);
    if (value === undefined) setInternal(c);
    onChange && onChange(c);
  };
  const pct = (val - min) / (max - min) * 100;
  const fmt = format || (v => String(v));
  const fromPointer = e => {
    const r = trackRef.current.getBoundingClientRect();
    set(min + (e.clientX - r.left) / r.width * (max - min));
  };
  const onPointerDown = e => {
    if (disabled) return;
    e.preventDefault();
    fromPointer(e);
    const move = ev => fromPointer(ev);
    const up = () => {
      window.removeEventListener("pointermove", move);
      window.removeEventListener("pointerup", up);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const onKeyDown = e => {
    if (disabled) return;
    const big = (max - min) / 10;
    const map = {
      ArrowRight: val + step,
      ArrowUp: val + step,
      ArrowLeft: val - step,
      ArrowDown: val - step,
      PageUp: val + big,
      PageDown: val - big,
      Home: min,
      End: max
    };
    if (map[e.key] !== undefined) {
      e.preventDefault();
      set(map[e.key]);
    }
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      alignItems: "baseline",
      marginBottom: "var(--space-2)"
    }
  }, label && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "600 14px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, label), /*#__PURE__*/React.createElement("output", {
    style: {
      font: "var(--text-sm)",
      fontWeight: 700,
      color: "var(--color-fg-brand)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, fmt(val))), /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    onPointerDown: onPointerDown,
    style: {
      position: "relative",
      height: 24,
      display: "flex",
      alignItems: "center",
      cursor: disabled ? "default" : "pointer",
      touchAction: "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      left: 0,
      right: 0,
      height: 4,
      borderRadius: 2,
      background: "var(--color-bg-muted)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      left: 0,
      width: `${pct}%`,
      height: 4,
      borderRadius: 2,
      background: "var(--color-bg-brand-solid)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    role: "slider",
    tabIndex: disabled ? -1 : 0,
    "aria-valuemin": min,
    "aria-valuemax": max,
    "aria-valuenow": val,
    "aria-valuetext": fmt(val),
    "aria-label": typeof label === "string" ? label : undefined,
    "aria-disabled": disabled || undefined,
    onKeyDown: onKeyDown,
    onFocus: e => setFocusRing(e.target.matches(":focus-visible")),
    onBlur: () => setFocusRing(false),
    style: {
      position: "absolute",
      left: `${pct}%`,
      transform: "translateX(-50%)",
      width: 20,
      height: 20,
      boxSizing: "border-box",
      borderRadius: "50%",
      background: "var(--tm-neutral-0)",
      border: "var(--border-width-lg) solid var(--color-bg-brand-solid)",
      boxShadow: focusRing ? "var(--focus-ring)" : "var(--elevation-100)",
      outline: "none",
      transition: "box-shadow var(--duration-fast-1) var(--ease-standard)"
    }
  })));
}
Object.assign(__ds_scope, { Slider });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Slider.jsx", error: String((e && e.message) || e) }); }

// components/forms/Switch.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Switch — immediate on/off with INSTANT effect (no Save button).
 * If a save step exists, use Checkbox instead. role="switch" +
 * aria-checked; Space/Enter toggle. State is never color-only:
 * thumb position + optional inner icon carry it.
 */
function Switch({
  label,
  description,
  checked,
  defaultChecked,
  onChange,
  disabled = false,
  labelPosition = "end",
  style,
  ...rest
}) {
  const [internal, setInternal] = React.useState(defaultChecked || false);
  const on = checked !== undefined ? checked : internal;
  const [focusRing, setFocusRing] = React.useState(false);
  const toggle = () => {
    if (disabled) return;
    if (checked === undefined) setInternal(!on);
    onChange && onChange(!on);
  };
  const track = /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 40,
      height: 22,
      flexShrink: 0,
      boxSizing: "border-box",
      borderRadius: "var(--radius-pill)",
      background: on ? "var(--color-bg-brand-solid)" : "var(--color-border-strong)",
      padding: 2,
      display: "inline-flex",
      justifyContent: on ? "flex-end" : "flex-start",
      boxShadow: focusRing ? "var(--focus-ring)" : "none",
      transition: "background var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18,
      height: 18,
      borderRadius: "50%",
      background: "var(--tm-neutral-0)",
      boxShadow: "var(--elevation-100)",
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      transition: "transform var(--duration-fast-2) var(--ease-standard)"
    }
  }, on && /*#__PURE__*/React.createElement("svg", {
    width: "10",
    height: "10",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--tm-navy-900)",
    strokeWidth: "3.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M20 6 9 17l-5-5"
  }))));
  const text = (label || description) && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 2,
      textAlign: "start"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-sm)",
      fontWeight: 500,
      color: "var(--color-fg-default)"
    }
  }, label), description && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)"
    }
  }, description));
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    role: "switch",
    "aria-checked": on,
    disabled: disabled,
    onClick: toggle,
    onFocus: e => setFocusRing(e.target.matches(":focus-visible")),
    onBlur: () => setFocusRing(false),
    style: {
      display: "inline-flex",
      alignItems: description ? "flex-start" : "center",
      gap: "var(--space-2)",
      background: "transparent",
      border: "none",
      padding: 0,
      margin: 0,
      fontFamily: "var(--font-sans)",
      cursor: disabled ? "default" : "pointer",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      outline: "none",
      minHeight: "var(--target-min)",
      ...style
    }
  }, rest), labelPosition === "start" && text, track, labelPosition === "end" && text);
}
Object.assign(__ds_scope, { Switch });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Switch.jsx", error: String((e && e.message) || e) }); }

// components/forms/Textarea.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Textarea — multi-line text. Always md radius (pills don't scale to
 * multi-line). Pair with <Field> for label/help/error/counter.
 */
const Textarea = React.forwardRef(function Textarea({
  placeholder,
  rows = 4,
  value,
  defaultValue,
  onChange,
  invalid = false,
  disabled = false,
  readOnly = false,
  maxLength,
  fullWidth = false,
  style,
  ...rest
}, ref) {
  const [focus, setFocus] = React.useState(false);
  const borderColor = invalid ? "var(--color-border-danger)" : focus ? "var(--color-border-focus)" : "var(--input-border)";
  return /*#__PURE__*/React.createElement("textarea", _extends({
    ref: ref,
    placeholder: placeholder,
    rows: rows,
    value: value,
    defaultValue: defaultValue,
    onChange: onChange,
    disabled: disabled,
    readOnly: readOnly,
    maxLength: maxLength,
    "aria-invalid": invalid || undefined,
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      boxSizing: "border-box",
      width: fullWidth ? "100%" : undefined,
      minWidth: fullWidth ? 0 : 260,
      font: "var(--text-md)",
      fontFamily: "var(--font-sans)",
      color: "var(--color-fg-default)",
      background: readOnly ? "var(--color-bg-subtle)" : disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--radius-md)",
      padding: "var(--space-3)",
      outline: "none",
      resize: "vertical",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)",
      ...style
    }
  }, rest));
});
Object.assign(__ds_scope, { Textarea });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/Textarea.jsx", error: String((e && e.message) || e) }); }

// components/forms/TimePicker.jsx
try { (() => {
/**
 * TimePicker — typed hh:mm (24 h) with mask, plus a listbox of
 * step-interval times as an aid (typing is never blocked). Timezone
 * is always displayed by the CONSUMER next to the value ("14:00
 * CDMX") per the financial-data rules.
 */
function TimePicker({
  value = "",
  onChange,
  step = 30,
  size = "md",
  invalid = false,
  disabled = false,
  fullWidth = false,
  id,
  style
}) {
  const [open, setOpen] = React.useState(false);
  const [focus, setFocus] = React.useState(false);
  const [active, setActive] = React.useState(0);
  const rootRef = React.useRef(null);
  const listRef = React.useRef(null);
  const mask = raw => {
    let d = raw.replace(/\D/g, "").slice(0, 4);
    if (d.length >= 1 && Number(d[0]) > 2) d = "0" + d;
    if (d.length >= 2 && Number(d.slice(0, 2)) > 23) d = "23" + d.slice(2);
    if (d.length >= 3 && Number(d[2]) > 5) d = d.slice(0, 2) + "5" + d.slice(3);
    return d.length <= 2 ? d : `${d.slice(0, 2)}:${d.slice(2)}`;
  };
  const times = React.useMemo(() => {
    const out = [];
    for (let m = 0; m < 1440; m += step) {
      out.push(`${String(Math.floor(m / 60)).padStart(2, "0")}:${String(m % 60).padStart(2, "0")}`);
    }
    return out;
  }, [step]);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  React.useEffect(() => {
    if (open && listRef.current) {
      const el = listRef.current.children[active];
      if (el) listRef.current.scrollTop = el.offsetTop - 80;
    }
  }, [open, active]);
  const openList = () => {
    const idx = Math.max(0, times.findIndex(t => t >= (value || "00:00")));
    setActive(idx === -1 ? 0 : idx);
    setOpen(true);
  };
  const fontSize = {
    sm: 13,
    md: 15,
    lg: 16
  }[size] || 15;
  const borderColor = invalid ? "var(--color-border-danger)" : focus || open ? "var(--color-border-focus)" : "var(--input-border)";
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: fullWidth ? "block" : "inline-block",
      width: fullWidth ? "100%" : undefined,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      boxSizing: "border-box",
      height: `var(--control-height-${size})`,
      paddingInline: "var(--space-3)",
      background: disabled ? "var(--color-bg-muted)" : "var(--input-bg)",
      border: `${invalid || focus || open ? "var(--border-width-md)" : "var(--border-width-sm)"} solid ${borderColor}`,
      borderRadius: "var(--input-radius)",
      minWidth: fullWidth ? 0 : 140,
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      transition: "border-color var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    id: id,
    type: "text",
    inputMode: "numeric",
    placeholder: "hh:mm",
    autoComplete: "off",
    value: value,
    disabled: disabled,
    "aria-invalid": invalid || undefined,
    "aria-label": "Hora (24 horas)",
    onChange: e => onChange && onChange(mask(e.target.value)),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      flex: 1,
      minWidth: 0,
      fontFamily: "var(--font-sans)",
      fontSize,
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none",
      padding: 0,
      height: "100%"
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Elegir hora de la lista",
    "aria-expanded": open,
    disabled: disabled,
    onClick: () => open ? setOpen(false) : openList(),
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 24,
      height: 24,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      padding: 0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "12",
    cy: "12",
    r: "10"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 6v6l4 2"
  })))), open && /*#__PURE__*/React.createElement("ul", {
    ref: listRef,
    role: "listbox",
    "aria-label": "Horas",
    tabIndex: 0,
    onKeyDown: e => {
      if (e.key === "ArrowDown") {
        e.preventDefault();
        setActive(a => Math.min(times.length - 1, a + 1));
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        setActive(a => Math.max(0, a - 1));
      } else if (e.key === "Enter") {
        e.preventDefault();
        onChange && onChange(times[active]);
        setOpen(false);
      } else if (e.key === "Escape") {
        e.preventDefault();
        setOpen(false);
      }
    },
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      left: 0,
      minWidth: "100%",
      maxHeight: 224,
      overflowY: "auto",
      margin: 0,
      padding: "var(--space-1)",
      listStyle: "none",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      boxSizing: "border-box"
    }
  }, times.map((t, i) => /*#__PURE__*/React.createElement("li", {
    key: t,
    role: "option",
    "aria-selected": t === value,
    onMouseEnter: () => setActive(i),
    onMouseDown: e => e.preventDefault(),
    onClick: () => {
      onChange && onChange(t);
      setOpen(false);
    },
    style: {
      padding: "6px 12px",
      borderRadius: "var(--radius-sm)",
      font: `400 ${fontSize}px/1.4 var(--font-sans)`,
      fontVariantNumeric: "var(--numeric-tabular)",
      color: "var(--color-fg-default)",
      background: i === active ? "var(--color-bg-selected)" : "transparent",
      fontWeight: t === value ? 700 : 400,
      cursor: "pointer"
    }
  }, t))));
}
Object.assign(__ds_scope, { TimePicker });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/TimePicker.jsx", error: String((e && e.message) || e) }); }

// components/forms/TransferList.jsx
try { (() => {
function Panel({
  title,
  options,
  checked,
  onToggle,
  onToggleAll,
  height
}) {
  const all = options.length > 0 && options.every(o => checked.includes(o.value));
  const some = options.some(o => checked.includes(o.value)) && !all;
  const cbRef = React.useRef(null);
  React.useEffect(() => {
    if (cbRef.current) cbRef.current.indeterminate = some;
  }, [some]);
  const n = options.filter(o => checked.includes(o.value)).length;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      border: "1px solid var(--color-border-default)",
      borderRadius: "var(--radius-md)",
      overflow: "hidden",
      background: "var(--color-bg-surface)"
    }
  }, /*#__PURE__*/React.createElement("label", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      padding: "8px 12px",
      borderBottom: "1px solid var(--color-border-subtle)",
      background: "var(--color-bg-subtle)",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    ref: cbRef,
    type: "checkbox",
    checked: all,
    onChange: onToggleAll,
    "aria-label": `Seleccionar todo en ${title}`,
    style: {
      width: 15,
      height: 15,
      accentColor: "var(--tm-navy-900)",
      cursor: "pointer"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 12.5px/1.3 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: "auto",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, n, "/", options.length)), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: "var(--space-1)",
      listStyle: "none",
      height,
      overflowY: "auto",
      boxSizing: "border-box"
    }
  }, options.length === 0 && /*#__PURE__*/React.createElement("li", {
    style: {
      padding: "var(--space-3)",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      textAlign: "center"
    }
  }, "Vac\xEDo"), options.map(o => /*#__PURE__*/React.createElement("li", {
    key: String(o.value)
  }, /*#__PURE__*/React.createElement("label", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      padding: "6px 8px",
      borderRadius: "var(--radius-sm)",
      cursor: "pointer",
      font: "var(--text-sm)",
      color: "var(--color-fg-default)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: checked.includes(o.value),
    onChange: () => onToggle(o.value),
    style: {
      width: 15,
      height: 15,
      accentColor: "var(--tm-navy-900)",
      cursor: "pointer",
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, o.label))))));
}

/**
 * TransferList — assign items between two labeled sets (sucursales a
 * una campaña, permisos a un rol). Checkbox lists (native semantics)
 * + explicit move buttons with text labels; counts announced in the
 * headers. For >~30 items prefer a Combobox+Tag pattern.
 */
function TransferList({
  options = [],
  value = [],
  onChange,
  titles = {
    available: "Disponibles",
    selected: "Asignados"
  },
  height = 200,
  disabled = false,
  style
}) {
  const [checked, setChecked] = React.useState([]);
  const left = options.filter(o => !value.includes(o.value));
  const right = options.filter(o => value.includes(o.value));
  const toggle = v => setChecked(c => c.includes(v) ? c.filter(x => x !== v) : [...c, v]);
  const checkedLeft = checked.filter(v => left.some(o => o.value === v));
  const checkedRight = checked.filter(v => right.some(o => o.value === v));
  const move = adding => {
    const moving = adding ? checkedLeft : checkedRight;
    if (!moving.length || !onChange) return;
    onChange(adding ? [...value, ...moving] : value.filter(v => !moving.includes(v)));
    setChecked(c => c.filter(v => !moving.includes(v)));
  };
  const btn = dis => ({
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 4,
    height: "var(--control-height-sm)",
    paddingInline: "var(--control-pad-x-sm)",
    font: "600 var(--control-font-size-sm)/1.2 var(--font-sans)",
    letterSpacing: "var(--control-tracking)",
    textTransform: "var(--control-transform)",
    color: dis ? "var(--color-fg-disabled)" : "var(--color-fg-brand)",
    background: "var(--color-bg-surface)",
    border: `1.5px solid ${dis ? "var(--color-border-disabled)" : "currentColor"}`,
    borderRadius: "var(--control-radius)",
    cursor: dis ? "default" : "pointer",
    whiteSpace: "nowrap"
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "stretch",
      gap: "var(--space-3)",
      fontFamily: "var(--font-sans)",
      opacity: disabled ? "var(--opacity-disabled)" : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement(Panel, {
    title: titles.available,
    options: left,
    checked: checked,
    height: height,
    onToggle: toggle,
    onToggleAll: () => {
      const allIn = left.every(o => checked.includes(o.value));
      setChecked(c => allIn ? c.filter(v => !left.some(o => o.value === v)) : [...c, ...left.map(o => o.value).filter(v => !c.includes(v))]);
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: disabled || !checkedLeft.length,
    onClick: () => move(true),
    style: btn(disabled || !checkedLeft.length)
  }, "Agregar", /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M5 12h14M12 5l7 7-7 7"
  }))), /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: disabled || !checkedRight.length,
    onClick: () => move(false),
    style: btn(disabled || !checkedRight.length)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M19 12H5M12 19l-7-7 7-7"
  })), "Quitar")), /*#__PURE__*/React.createElement(Panel, {
    title: titles.selected,
    options: right,
    checked: checked,
    height: height,
    onToggle: toggle,
    onToggleAll: () => {
      const allIn = right.every(o => checked.includes(o.value));
      setChecked(c => allIn ? c.filter(v => !right.some(o => o.value === v)) : [...c, ...right.map(o => o.value).filter(v => !c.includes(v))]);
    }
  }));
}
Object.assign(__ds_scope, { TransferList });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/forms/TransferList.jsx", error: String((e && e.message) || e) }); }

// components/navigation/AppBar.jsx
try { (() => {
/**
 * AppBar — product top bar. Slots: brand (logo lockup), title,
 * actions (right side), user (account menu). The environment badge
 * (sandbox vs producción) is a fintech must-have in back-office
 * tooling — it prevents real-money test mistakes.
 */
function AppBar({
  brand,
  title,
  env,
  actions,
  user,
  sticky = false,
  inverse = true,
  style
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-4)",
      height: 60,
      paddingInline: "var(--space-5)",
      boxSizing: "border-box",
      /* Brand bar stays navy in BOTH themes (navy-900 = the dark raised
         surface); the theme-flipping inverse role would turn it light in
         dark mode. Hairline separates it from the navy page in dark. */
      background: inverse ? "var(--tm-navy-900)" : "var(--color-bg-surface)",
      color: inverse ? "var(--tm-neutral-0)" : "var(--color-fg-default)",
      borderBottom: inverse ? "1px solid var(--tm-navy-700)" : "1px solid var(--color-border-subtle)",
      position: sticky ? "sticky" : "static",
      top: 0,
      zIndex: "var(--z-sticky)",
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, brand && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      flexShrink: 0
    }
  }, brand), title && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 15px/1.2 var(--font-sans)",
      whiteSpace: "nowrap"
    }
  }, title), env && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 10px/1 var(--font-sans)",
      letterSpacing: "0.08em",
      textTransform: "uppercase",
      padding: "5px 10px",
      borderRadius: "var(--radius-pill)",
      background: env === "sandbox" ? "var(--color-bg-warning-solid)" : "var(--color-bg-success-solid)",
      color: env === "sandbox" ? "var(--color-fg-on-warning)" : "var(--color-fg-on-solid)",
      flexShrink: 0
    }
  }, env), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)"
    }
  }, actions), user && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      paddingLeft: "var(--space-4)",
      borderLeft: `1px solid ${inverse ? "var(--tm-alpha-white-200)" : "var(--color-border-subtle)"}`
    }
  }, user));
}
Object.assign(__ds_scope, { AppBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/AppBar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/BottomNav.jsx
try { (() => {
/**
 * BottomNav — mobile primary navigation: 3–5 destinations, icon +
 * ALWAYS-visible label (never icon-only), red top indicator +
 * aria-current on the active item, ≥44px targets, safe-area padding.
 * More destinations → move the rest behind a "Más" item.
 */
function BottomNav({
  items = [],
  activeId,
  onSelect,
  position = "static",
  label = "Navegación principal",
  style
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    style: {
      display: "flex",
      background: "var(--color-bg-surface)",
      borderTop: "1px solid var(--color-border-subtle)",
      boxShadow: "0 -1px 3px rgba(15,29,65,0.06)",
      paddingBottom: "env(safe-area-inset-bottom, 0px)",
      fontFamily: "var(--font-sans)",
      ...(position === "fixed" ? {
        position: "fixed",
        left: 0,
        right: 0,
        bottom: 0,
        zIndex: "var(--z-sticky)"
      } : {}),
      ...style
    }
  }, items.map(it => {
    const on = it.id === activeId;
    return /*#__PURE__*/React.createElement("button", {
      key: it.id,
      type: "button",
      "aria-current": on ? "page" : undefined,
      onClick: () => onSelect && onSelect(it.id),
      style: {
        flex: 1,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        gap: 3,
        minHeight: "var(--target-touch)",
        padding: "8px 4px 7px",
        background: "transparent",
        border: "none",
        boxShadow: on ? "inset 0 2.5px 0 var(--tm-red-600)" : "none",
        color: on ? "var(--color-fg-brand)" : "var(--color-fg-subtle)",
        cursor: "pointer",
        transition: "color var(--duration-fast-2) var(--ease-standard)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex",
        position: "relative"
      }
    }, it.icon, it.count > 0 && /*#__PURE__*/React.createElement("span", {
      style: {
        position: "absolute",
        top: -4,
        right: -8,
        minWidth: 15,
        height: 15,
        padding: "0 3px",
        boxSizing: "border-box",
        borderRadius: "var(--radius-pill)",
        background: "var(--color-bg-accent-solid)",
        color: "#fff",
        font: "700 9.5px/15px var(--font-sans)",
        textAlign: "center",
        fontVariantNumeric: "var(--numeric-tabular)"
      }
    }, it.count > 99 ? "99+" : it.count)), /*#__PURE__*/React.createElement("span", {
      style: {
        font: `${on ? 700 : 500} 11px/1.2 var(--font-sans)`
      }
    }, it.label));
  }));
}
Object.assign(__ds_scope, { BottomNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/BottomNav.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Breadcrumbs.jsx
try { (() => {
/**
 * Breadcrumbs — location trail (hierarchy, not history). Current page
 * is plain text with aria-current="page", never a link. More than 4
 * levels collapse the middle into an expandable "…".
 */
function Breadcrumbs({
  items = [],
  style
}) {
  const [expanded, setExpanded] = React.useState(false);
  const collapse = items.length > 4 && !expanded;
  const shown = collapse ? [items[0], {
    _ellipsis: true
  }, ...items.slice(-2)] : items;
  const sep = /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "m9 18 6-6-6-6"
  }));
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Breadcrumb",
    style: {
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-1)",
      listStyle: "none",
      margin: 0,
      padding: 0,
      flexWrap: "wrap"
    }
  }, shown.map((it, i) => {
    const last = i === shown.length - 1;
    return /*#__PURE__*/React.createElement("li", {
      key: it._ellipsis ? "…" : `${it.label}-${i}`,
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-1)"
      }
    }, it._ellipsis ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      "aria-label": "Mostrar niveles ocultos",
      onClick: () => setExpanded(true),
      style: {
        font: "var(--text-sm)",
        color: "var(--color-fg-muted)",
        background: "transparent",
        border: "none",
        cursor: "pointer",
        padding: "2px 4px",
        borderRadius: "var(--radius-xs)"
      }
    }, "\u2026") : last ? /*#__PURE__*/React.createElement("span", {
      "aria-current": "page",
      style: {
        font: "var(--text-sm)",
        fontWeight: 600,
        color: "var(--color-fg-default)"
      }
    }, it.label) : /*#__PURE__*/React.createElement("a", {
      href: it.href || "#",
      onClick: it.onClick,
      style: {
        font: "var(--text-sm)",
        color: "var(--color-fg-muted)",
        textDecoration: "none"
      },
      onMouseEnter: e => e.target.style.color = "var(--color-fg-link-hover)",
      onMouseLeave: e => e.target.style.color = "var(--color-fg-muted)"
    }, it.label), !last && sep);
  })));
}
Object.assign(__ds_scope, { Breadcrumbs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Breadcrumbs.jsx", error: String((e && e.message) || e) }); }

// components/navigation/CommandPalette.jsx
try { (() => {
const FLATTEN = (groups, recents) => {
  const out = [];
  groups.forEach(g => g.items.forEach(it => out.push({
    ...it,
    _group: g.header
  })));
  if (!recents || recents.length === 0) return out;
  return out;
};

/**
 * CommandPalette — ⌘K/Ctrl+K keyboard-first launcher: grouped results
 * (actions, navigation, records) + recents on empty query. Combobox
 * semantics; Esc closes and returns focus. A power path — never the
 * ONLY path to a feature.
 */
function CommandPalette({
  open = false,
  onClose,
  groups = [],
  recentIds = [],
  onSelect,
  placeholder = "Busca acciones, secciones o expedientes…",
  hotkey = true,
  onOpen
}) {
  const [query, setQuery] = React.useState("");
  const [active, setActive] = React.useState(0);
  const inputRef = React.useRef(null);
  const listRef = React.useRef(null);
  const auto = React.useId();
  const listId = `tm-cp-${auto}`;
  React.useEffect(() => {
    if (!hotkey) return;
    const onKey = e => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k") {
        e.preventDefault();
        open ? onClose && onClose() : onOpen && onOpen();
      }
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [hotkey, open, onClose, onOpen]);
  React.useEffect(() => {
    if (!open) return;
    // Esc closes the top-most dismissible layer regardless of where focus is.
    const onEsc = e => {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose && onClose();
      }
    };
    document.addEventListener("keydown", onEsc);
    return () => document.removeEventListener("keydown", onEsc);
  }, [open, onClose]);
  React.useEffect(() => {
    if (open) {
      setQuery("");
      setActive(0);
      const t = setTimeout(() => inputRef.current && inputRef.current.focus(), 10);
      return () => clearTimeout(t);
    }
  }, [open]);
  const all = FLATTEN(groups);
  const q = query.trim().toLowerCase();
  let results;
  if (q) {
    results = all.filter(it => String(it.label).toLowerCase().includes(q) || (it.keywords || "").toLowerCase().includes(q));
  } else if (recentIds.length) {
    const rec = recentIds.map(id => all.find(it => it.id === id)).filter(Boolean).map(it => ({
      ...it,
      _group: "Recientes"
    }));
    results = [...rec, ...all.filter(it => !recentIds.includes(it.id))];
  } else {
    results = all;
  }
  React.useEffect(() => {
    if (listRef.current) {
      const el = listRef.current.querySelector(`[data-idx="${active}"]`);
      if (!el) return;
      const top = el.offsetTop,
        bottom = top + el.offsetHeight,
        list = listRef.current;
      if (top < list.scrollTop) list.scrollTop = top - 30;else if (bottom > list.scrollTop + list.clientHeight) list.scrollTop = bottom - list.clientHeight;
    }
  }, [active, query]);
  if (!open) return null;
  const pick = it => {
    onClose && onClose();
    it.onSelect && it.onSelect();
    onSelect && onSelect(it);
  };
  const onKeyDown = e => {
    if (e.key === "ArrowDown") {
      e.preventDefault();
      setActive(a => Math.min(results.length - 1, a + 1));
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      setActive(a => Math.max(0, a - 1));
    } else if (e.key === "Enter") {
      e.preventDefault();
      if (results[active]) pick(results[active]);
    } else if (e.key === "Escape") {
      e.preventDefault();
      onClose && onClose();
    }
  };
  let lastGroup = null;
  return /*#__PURE__*/React.createElement("div", {
    onMouseDown: e => {
      if (e.target === e.currentTarget) onClose && onClose();
    },
    style: {
      position: "fixed",
      inset: 0,
      background: "var(--color-bg-overlay)",
      display: "flex",
      alignItems: "flex-start",
      justifyContent: "center",
      paddingTop: "12vh",
      zIndex: "var(--z-modal)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    "aria-label": "Paleta de comandos",
    style: {
      width: "100%",
      maxWidth: 580,
      background: "var(--color-bg-surface-raised)",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--elevation-500)",
      fontFamily: "var(--font-sans)",
      overflow: "hidden",
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      padding: "var(--space-3) var(--space-4)",
      borderBottom: "1px solid var(--color-border-subtle)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "17",
    height: "17",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--color-fg-subtle)",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "8"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m21 21-4.3-4.3"
  })), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    type: "text",
    role: "combobox",
    "aria-expanded": "true",
    "aria-controls": listId,
    "aria-activedescendant": results[active] ? `${listId}-${active}` : undefined,
    placeholder: placeholder,
    autoComplete: "off",
    value: query,
    onChange: e => {
      setQuery(e.target.value);
      setActive(0);
    },
    onKeyDown: onKeyDown,
    style: {
      flex: 1,
      minWidth: 0,
      font: "400 15px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      outline: "none"
    }
  }), /*#__PURE__*/React.createElement("kbd", {
    style: {
      font: "600 10.5px/1 var(--font-mono)",
      color: "var(--color-fg-subtle)",
      background: "var(--color-bg-muted)",
      padding: "4px 6px",
      borderRadius: "var(--radius-xs)"
    }
  }, "esc")), /*#__PURE__*/React.createElement("div", {
    ref: listRef,
    id: listId,
    role: "listbox",
    style: {
      maxHeight: 340,
      overflowY: "auto",
      padding: "var(--space-2)"
    }
  }, results.length === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--space-5)",
      textAlign: "center",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, "Sin resultados para \xAB", query, "\xBB \u2014 revisa la ortograf\xEDa o intenta otro t\xE9rmino."), results.map((it, i) => {
    const header = it._group !== lastGroup ? it._group : null;
    lastGroup = it._group;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: `${it.id}-${i}`
    }, header && /*#__PURE__*/React.createElement("div", {
      role: "presentation",
      style: {
        font: "700 10px/1.4 var(--font-sans)",
        letterSpacing: ".07em",
        textTransform: "uppercase",
        color: "var(--color-fg-subtle)",
        padding: "8px 10px 4px"
      }
    }, header), /*#__PURE__*/React.createElement("div", {
      id: `${listId}-${i}`,
      "data-idx": i,
      role: "option",
      "aria-selected": i === active,
      onMouseEnter: () => setActive(i),
      onMouseDown: e => e.preventDefault(),
      onClick: () => pick(it),
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-3)",
        padding: "9px 10px",
        borderRadius: "var(--radius-sm)",
        background: i === active ? "var(--color-bg-selected)" : "transparent",
        cursor: "pointer"
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex",
        color: "var(--color-fg-muted)",
        flexShrink: 0
      }
    }, it.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        font: "500 14px/1.35 var(--font-sans)",
        color: "var(--color-fg-default)",
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap"
      }
    }, it.label), it.hint && /*#__PURE__*/React.createElement("kbd", {
      style: {
        font: "600 10.5px/1 var(--font-mono)",
        color: "var(--color-fg-subtle)",
        background: "var(--color-bg-muted)",
        padding: "4px 6px",
        borderRadius: "var(--radius-xs)",
        flexShrink: 0
      }
    }, it.hint)));
  })), /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, `${results.length} resultado${results.length === 1 ? "" : "s"}`)));
}
Object.assign(__ds_scope, { CommandPalette });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/CommandPalette.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Link.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/**
 * Link — navigation (Button is for actions). Navy with the red brand
 * underline; hover shifts to red per the global rule. external adds
 * the icon + SR notice and safe rel. standalone drops the underline
 * until hover (nav lists) — inline prose links keep it always
 * (never color-alone in a sentence).
 */
function Link({
  href = "#",
  children,
  external = false,
  standalone = false,
  muted = false,
  iconStart,
  onClick,
  style,
  ...rest
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("a", _extends({
    href: href,
    target: external ? "_blank" : undefined,
    rel: external ? "noopener noreferrer" : undefined,
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      display: iconStart || external ? "inline-flex" : undefined,
      alignItems: "center",
      gap: "var(--space-1)",
      font: "inherit",
      fontWeight: 600,
      fontFamily: "var(--font-sans)",
      color: hover ? "var(--color-fg-link-hover)" : muted ? "var(--color-fg-muted)" : "var(--color-fg-link)",
      textDecoration: standalone && !hover ? "none" : "underline",
      textDecorationColor: "var(--color-link-underline)",
      textUnderlineOffset: 3,
      transition: "color var(--duration-fast-2) var(--ease-standard)",
      ...style
    }
  }, rest), iconStart && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex"
    }
  }, iconStart), children, external && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "M15 3h6v6M10 14 21 3M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      width: 1,
      height: 1,
      overflow: "hidden",
      clipPath: "inset(50%)"
    }
  }, "(abre en una ventana nueva)")));
}
Object.assign(__ds_scope, { Link });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Link.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MegaMenu.jsx
try { (() => {
/**
 * MegaMenu — marketing-nav disclosure that opens a full-width panel of
 * grouped links + optional promo slot. Disclosure pattern (button +
 * aria-expanded), Esc/outside-click close, links are real <a>. Product
 * shells use SideNav/Menu instead. Mount inside a position:relative
 * nav bar.
 */
function MegaMenu({
  label,
  columns = [],
  promo,
  open: openProp,
  onOpenChange,
  style
}) {
  const [internal, setInternal] = React.useState(false);
  const open = openProp !== undefined ? openProp : internal;
  const setOpen = v => {
    if (openProp === undefined) setInternal(v);
    onOpenChange && onOpenChange(v);
  };
  const rootRef = React.useRef(null);
  const btnRef = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    const onEsc = e => {
      if (e.key === "Escape") {
        setOpen(false);
        if (btnRef.current) btnRef.current.focus();
      }
    };
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onEsc);
    return () => {
      document.removeEventListener("mousedown", onDoc);
      document.removeEventListener("keydown", onEsc);
    };
  }, [open]);
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      display: "inline-flex",
      ...style
    }
  }, /*#__PURE__*/React.createElement("button", {
    ref: btnRef,
    type: "button",
    "aria-expanded": open,
    "aria-haspopup": "true",
    onClick: () => setOpen(!open),
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 6,
      height: "var(--control-height-sm)",
      paddingInline: "var(--space-3)",
      font: "var(--control-font-weight) var(--control-font-size-md)/1.2 var(--font-sans)",
      letterSpacing: "var(--control-tracking)",
      textTransform: "var(--control-transform)",
      color: open ? "var(--color-fg-accent)" : "inherit",
      background: "transparent",
      border: "none",
      cursor: "pointer",
      boxShadow: open ? "inset 0 -2px 0 var(--tm-red-600)" : "none"
    }
  }, label, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      transform: open ? "rotate(180deg)" : "none",
      transition: "transform var(--duration-fast-2) var(--ease-standard)"
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: "m6 9 6 6 6-6"
  }))), open && /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      top: "100%",
      left: 0,
      right: 0,
      display: "grid",
      gridTemplateColumns: `repeat(${columns.length}, 1fr)${promo ? " 280px" : ""}`,
      gap: "var(--space-6)",
      padding: "var(--space-5) var(--space-6)",
      background: "var(--color-bg-surface-raised)",
      borderTop: "3px solid var(--tm-red-600)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-dropdown)",
      fontFamily: "var(--font-sans)",
      textAlign: "left",
      boxSizing: "border-box"
    }
  }, columns.map(col => /*#__PURE__*/React.createElement("div", {
    key: col.header
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "700 11px/1.3 var(--font-sans)",
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase",
      color: "var(--color-fg-subtle)",
      marginBottom: 12
    }
  }, col.header), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none",
      display: "flex",
      flexDirection: "column",
      gap: 4
    }
  }, col.links.map(l => /*#__PURE__*/React.createElement("li", {
    key: l.label
  }, /*#__PURE__*/React.createElement("a", {
    href: l.href || "#",
    onClick: () => setOpen(false),
    style: {
      display: "block",
      padding: "7px 10px",
      margin: "0 -10px",
      borderRadius: "var(--radius-sm)",
      textDecoration: "none",
      color: "var(--color-fg-default)"
    },
    onMouseEnter: e => e.currentTarget.style.background = "var(--color-bg-hover)",
    onMouseLeave: e => e.currentTarget.style.background = "transparent"
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      font: "600 14px/1.35 var(--font-sans)"
    }
  }, l.label), l.description && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 1
    }
  }, l.description))))))), promo && /*#__PURE__*/React.createElement("div", {
    style: {
      borderLeft: "1px solid var(--color-border-subtle)",
      paddingLeft: "var(--space-5)"
    }
  }, promo)));
}
Object.assign(__ds_scope, { MegaMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MegaMenu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Menu.jsx
try { (() => {
/**
 * Menu — dropdown action menu. Trigger gets aria-haspopup/expanded;
 * keyboard: Enter/Space/↓ opens with focus on first item, ↑↓ roving,
 * Home/End, Esc closes and returns focus to trigger. Destructive
 * items render in danger color. Every action here must also exist
 * somewhere discoverable.
 */
function Menu({
  trigger,
  items = [],
  align = "start",
  onSelect,
  style
}) {
  const [open, setOpen] = React.useState(false);
  const [active, setActive] = React.useState(0);
  const rootRef = React.useRef(null);
  const triggerRef = React.useRef(null);
  const itemRefs = React.useRef([]);
  const actionable = items.map((it, i) => ({
    ...it,
    _i: i
  })).filter(it => !it.divider);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  React.useEffect(() => {
    if (open) {
      const el = itemRefs.current[active];
      if (el) el.focus();
    }
  }, [open, active]);
  const openMenu = () => {
    setActive(0);
    setOpen(true);
  };
  const close = (refocus = true) => {
    setOpen(false);
    if (refocus && triggerRef.current) triggerRef.current.focus();
  };
  const pick = it => {
    if (it.disabled) return;
    close();
    it.onSelect && it.onSelect();
    onSelect && onSelect(it);
  };
  const onMenuKey = e => {
    if (e.key === "Escape") {
      e.preventDefault();
      close();
    } else if (e.key === "ArrowDown") {
      e.preventDefault();
      setActive(a => (a + 1) % actionable.length);
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      setActive(a => (a - 1 + actionable.length) % actionable.length);
    } else if (e.key === "Home") {
      e.preventDefault();
      setActive(0);
    } else if (e.key === "End") {
      e.preventDefault();
      setActive(actionable.length - 1);
    } else if (e.key === "Tab") close(false);
  };
  const triggerEl = React.cloneElement(trigger, {
    ref: triggerRef,
    "aria-haspopup": "menu",
    "aria-expanded": open,
    onClick: e => {
      trigger.props.onClick && trigger.props.onClick(e);
      open ? setOpen(false) : openMenu();
    },
    onKeyDown: e => {
      if (e.key === "ArrowDown") {
        e.preventDefault();
        openMenu();
      }
    }
  });
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: "inline-block",
      ...style
    }
  }, triggerEl, open && /*#__PURE__*/React.createElement("div", {
    role: "menu",
    onKeyDown: onMenuKey,
    style: {
      position: "absolute",
      top: "calc(100% + 4px)",
      [align === "end" ? "right" : "left"]: 0,
      minWidth: 200,
      padding: "var(--space-1)",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      fontFamily: "var(--font-sans)"
    }
  }, items.map((it, i) => it.divider ? /*#__PURE__*/React.createElement("div", {
    key: `d-${i}`,
    role: "separator",
    style: {
      height: 1,
      background: "var(--color-border-subtle)",
      margin: "var(--space-1) var(--space-2)"
    }
  }) : /*#__PURE__*/React.createElement("button", {
    key: it.label,
    ref: el => itemRefs.current[actionable.findIndex(a => a._i === i)] = el,
    type: "button",
    role: "menuitem",
    tabIndex: -1,
    disabled: it.disabled,
    "aria-disabled": it.disabled || undefined,
    onClick: () => pick(it),
    onMouseEnter: () => setActive(actionable.findIndex(a => a._i === i)),
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      width: "100%",
      padding: "8px 10px",
      font: "var(--text-sm)",
      fontWeight: 500,
      textAlign: "left",
      color: it.disabled ? "var(--color-fg-disabled)" : it.destructive ? "var(--color-fg-danger)" : "var(--color-fg-default)",
      background: "transparent",
      border: "none",
      borderRadius: "var(--radius-sm)",
      cursor: it.disabled ? "default" : "pointer",
      outline: "none"
    },
    onFocus: e => e.target.style.background = "var(--color-bg-selected)",
    onBlur: e => e.target.style.background = "transparent"
  }, it.icon && /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      display: "inline-flex",
      color: "inherit"
    }
  }, it.icon), it.label))));
}
Object.assign(__ds_scope, { Menu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Menu.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Pagination.jsx
try { (() => {
/**
 * Pagination — deterministic paging for anything a user (or auditor)
 * needs to locate, reference, or reproduce. Summary "1–25 de 3,412"
 * uses es-MX formatting; current page carries aria-current="page".
 * Drive state from the URL (?page=&size=) where possible.
 */
function Pagination({
  page = 1,
  pageSize = 25,
  total = 0,
  onPageChange,
  pageSizeOptions,
  onPageSizeChange,
  style
}) {
  const pages = Math.max(1, Math.ceil(total / pageSize));
  const from = total === 0 ? 0 : (page - 1) * pageSize + 1;
  const to = Math.min(total, page * pageSize);
  const nf = new Intl.NumberFormat("es-MX");
  const windowPages = () => {
    const w = [];
    const lo = Math.max(1, Math.min(page - 2, pages - 4));
    const hi = Math.min(pages, lo + 4);
    for (let i = lo; i <= hi; i++) w.push(i);
    return w;
  };
  const btn = (label, target, {
    disabled,
    current,
    ariaLabel
  } = {}) => /*#__PURE__*/React.createElement("button", {
    key: ariaLabel || String(label),
    type: "button",
    disabled: disabled,
    "aria-label": ariaLabel,
    "aria-current": current ? "page" : undefined,
    onClick: () => !disabled && onPageChange && onPageChange(target),
    style: {
      minWidth: 32,
      height: 32,
      paddingInline: 6,
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      font: "600 13px/1 var(--font-sans)",
      fontVariantNumeric: "var(--numeric-tabular)",
      color: current ? "var(--color-fg-on-solid)" : disabled ? "var(--color-fg-disabled)" : "var(--color-fg-muted)",
      background: current ? "var(--color-bg-brand-solid)" : "transparent",
      border: "none",
      borderRadius: "var(--radius-sm)",
      cursor: disabled || current ? "default" : "pointer"
    }
  }, label);
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Paginaci\xF3n",
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-4)",
      fontFamily: "var(--font-sans)",
      flexWrap: "wrap",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, nf.format(from), "\u2013", nf.format(to), " de ", nf.format(total)), pageSizeOptions && /*#__PURE__*/React.createElement("label", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: "var(--space-1)",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, "Por p\xE1gina", /*#__PURE__*/React.createElement("select", {
    value: pageSize,
    onChange: e => onPageSizeChange && onPageSizeChange(Number(e.target.value)),
    style: {
      font: "600 13px/1.2 var(--font-sans)",
      color: "var(--color-fg-default)",
      background: "var(--color-bg-surface)",
      border: "1px solid var(--color-border-default)",
      borderRadius: "var(--radius-sm)",
      padding: "4px 6px"
    }
  }, pageSizeOptions.map(n => /*#__PURE__*/React.createElement("option", {
    key: n,
    value: n
  }, n))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 2
    }
  }, btn("«", 1, {
    disabled: page <= 1,
    ariaLabel: "Primera página"
  }), btn("‹", page - 1, {
    disabled: page <= 1,
    ariaLabel: "Página anterior"
  }), windowPages().map(p => btn(nf.format(p), p, {
    current: p === page,
    ariaLabel: `Página ${p}`
  })), btn("›", page + 1, {
    disabled: page >= pages,
    ariaLabel: "Página siguiente"
  }), btn("»", pages, {
    disabled: page >= pages,
    ariaLabel: "Última página"
  })));
}
Object.assign(__ds_scope, { Pagination });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Pagination.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ScrollProgress.jsx
try { (() => {
/**
 * ScrollProgress — thin reading-progress bar for long marketing/
 * editorial pages (never product screens). Decorative (aria-hidden);
 * width updates are position-only so reduced-motion is unaffected.
 */
function ScrollProgress({
  color = "var(--tm-red-600)",
  height = 3,
  target,
  style
}) {
  const [pct, setPct] = React.useState(0);
  React.useEffect(() => {
    const el = target && target.current ? target.current : null;
    const src = el || window;
    const read = () => {
      const scrollTop = el ? el.scrollTop : window.scrollY || document.documentElement.scrollTop;
      const max = el ? el.scrollHeight - el.clientHeight : document.documentElement.scrollHeight - window.innerHeight;
      setPct(max > 0 ? Math.min(1, scrollTop / max) : 0);
    };
    read();
    src.addEventListener("scroll", read, {
      passive: true
    });
    window.addEventListener("resize", read);
    return () => {
      src.removeEventListener("scroll", read);
      window.removeEventListener("resize", read);
    };
  }, [target]);
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      top: 0,
      left: 0,
      right: 0,
      height,
      zIndex: "var(--z-banner)",
      background: "transparent",
      pointerEvents: "none",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: `${pct * 100}%`,
      height: "100%",
      background: color
    }
  }));
}
Object.assign(__ds_scope, { ScrollProgress });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ScrollProgress.jsx", error: String((e && e.message) || e) }); }

// components/navigation/SideNav.jsx
try { (() => {
/**
 * SideNav — app navigation rail. Sections with headers; active item =
 * selected background + red left indicator + aria-current="page".
 * Max nesting 2 levels (deeper IA needs restructuring, not indenting).
 */
function SideNav({
  sections = [],
  activeId,
  onSelect,
  footer,
  width = 260,
  style
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Navegaci\xF3n principal",
    style: {
      width,
      flexShrink: 0,
      boxSizing: "border-box",
      display: "flex",
      flexDirection: "column",
      gap: "var(--space-5)",
      padding: "var(--space-4) var(--space-3)",
      background: "var(--color-bg-subtle)",
      borderRight: "1px solid var(--color-border-subtle)",
      fontFamily: "var(--font-sans)",
      minHeight: "100%",
      ...style
    }
  }, sections.map((sec, si) => /*#__PURE__*/React.createElement("div", {
    key: sec.header || si
  }, sec.header && /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-overline)",
      letterSpacing: "var(--label-tracking)",
      textTransform: "uppercase",
      color: "var(--color-fg-subtle)",
      padding: "0 var(--space-3) var(--space-2)"
    }
  }, sec.header), /*#__PURE__*/React.createElement("ul", {
    style: {
      listStyle: "none",
      margin: 0,
      padding: 0,
      display: "flex",
      flexDirection: "column",
      gap: 2
    }
  }, sec.items.map(it => {
    const on = it.id === activeId;
    return /*#__PURE__*/React.createElement("li", {
      key: it.id
    }, /*#__PURE__*/React.createElement("a", {
      href: it.href || "#",
      "aria-current": on ? "page" : undefined,
      onClick: e => {
        if (onSelect) {
          e.preventDefault();
          onSelect(it.id, it);
        }
      },
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-2)",
        padding: "9px var(--space-3)",
        borderRadius: "var(--radius-md)",
        font: `${on ? 700 : 500} 14px/1.3 var(--font-sans)`,
        color: on ? "var(--color-fg-brand)" : "var(--color-fg-muted)",
        background: on ? "var(--color-bg-selected)" : "transparent",
        boxShadow: on ? "inset 3px 0 0 var(--tm-red-600)" : "none",
        textDecoration: "none",
        transition: "background var(--duration-fast-1) var(--ease-standard)"
      },
      onMouseEnter: e => {
        if (!on) e.currentTarget.style.background = "var(--color-bg-hover)";
      },
      onMouseLeave: e => {
        if (!on) e.currentTarget.style.background = "transparent";
      }
    }, it.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex",
        flexShrink: 0
      }
    }, it.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap"
      }
    }, it.label), it.count != null && /*#__PURE__*/React.createElement("span", {
      style: {
        font: "700 11px/1 var(--font-sans)",
        padding: "3px 7px",
        borderRadius: "var(--radius-pill)",
        background: on ? "var(--color-bg-accent-subtle)" : "var(--color-bg-muted)",
        color: on ? "var(--color-fg-accent)" : "var(--color-fg-muted)",
        fontVariantNumeric: "var(--numeric-tabular)"
      }
    }, it.count)));
  })))), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "auto"
    }
  }, footer));
}
Object.assign(__ds_scope, { SideNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/SideNav.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Stepper.jsx
try { (() => {
/**
 * Stepper — linear multi-step progress (wizards, applications, claims).
 * States: done (check, revisitable via onStepClick) · current
 * (aria-current="step") · upcoming. Long flows must pair this with
 * per-step "Guardar y salir". Progress is honest — no fake %.
 */
function Stepper({
  steps = [],
  current = 0,
  onStepClick,
  orientation = "horizontal",
  style
}) {
  const horiz = orientation === "horizontal";
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      display: "flex",
      flexDirection: horiz ? "row" : "column",
      gap: horiz ? 0 : "var(--space-2)",
      listStyle: "none",
      margin: 0,
      padding: 0,
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, steps.map((s, i) => {
    const done = i < current;
    const now = i === current;
    const clickable = done && onStepClick;
    const dot = /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: 28,
        height: 28,
        flexShrink: 0,
        display: "inline-flex",
        alignItems: "center",
        justifyContent: "center",
        borderRadius: "50%",
        boxSizing: "border-box",
        font: "700 13px/1 var(--font-sans)",
        fontVariantNumeric: "var(--numeric-tabular)",
        background: done ? "var(--color-bg-brand-solid)" : now ? "var(--color-bg-accent-solid)" : "var(--color-bg-muted)",
        color: done || now ? "var(--color-fg-on-solid)" : "var(--color-fg-subtle)",
        border: now ? "none" : done ? "none" : "1px solid var(--color-border-default)"
      }
    }, done ? /*#__PURE__*/React.createElement("svg", {
      width: "13",
      height: "13",
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: "3",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }, /*#__PURE__*/React.createElement("path", {
      d: "M20 6 9 17l-5-5"
    })) : i + 1);
    const text = /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: `${now ? 700 : 500} 13px/1.35 var(--font-sans)`,
        color: now ? "var(--color-fg-default)" : done ? "var(--color-fg-muted)" : "var(--color-fg-subtle)",
        whiteSpace: "nowrap"
      }
    }, s.label), s.description && /*#__PURE__*/React.createElement("span", {
      style: {
        font: "var(--text-xs)",
        color: "var(--color-fg-subtle)"
      }
    }, s.description));
    return /*#__PURE__*/React.createElement("li", {
      key: `${s.label}-${i}`,
      "aria-current": now ? "step" : undefined,
      style: {
        display: "flex",
        alignItems: "center",
        flex: horiz && i < steps.length - 1 ? 1 : "0 0 auto",
        minWidth: 0
      }
    }, clickable ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: () => onStepClick(i),
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-2)",
        background: "transparent",
        border: "none",
        padding: 0,
        cursor: "pointer",
        fontFamily: "inherit"
      }
    }, dot, text) : /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: "var(--space-2)"
      }
    }, dot, text), horiz && i < steps.length - 1 && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        flex: 1,
        height: 2,
        margin: "0 var(--space-3)",
        borderRadius: 1,
        background: done ? "var(--color-bg-brand-solid)" : "var(--color-border-default)",
        minWidth: 24
      }
    }));
  }));
}
Object.assign(__ds_scope, { Stepper });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Stepper.jsx", error: String((e && e.message) || e) }); }

// components/navigation/Tabs.jsx
try { (() => {
/**
 * Tabs — in-page switching among PEER content (route navigation
 * belongs to SideNav/AppBar). APG: one tab stop on the tablist,
 * ←→/Home/End move focus and auto-activate, Tab moves into the panel.
 * Overflow scrolls horizontally — never wraps to two rows.
 */
function Tabs({
  items = [],
  value,
  onChange,
  variant = "line",
  children,
  style
}) {
  const auto = React.useId();
  const refs = React.useRef([]);
  const idx = Math.max(0, items.findIndex(t => t.id === value));
  const onKeyDown = e => {
    const dir = e.key === "ArrowRight" ? 1 : e.key === "ArrowLeft" ? -1 : 0;
    let next = null;
    if (dir) next = (idx + dir + items.length) % items.length;else if (e.key === "Home") next = 0;else if (e.key === "End") next = items.length - 1;
    if (next == null) return;
    e.preventDefault();
    onChange && onChange(items[next].id);
    const el = refs.current[next];
    if (el) el.focus();
  };
  const line = variant === "line";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: "var(--font-sans)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    onKeyDown: onKeyDown,
    style: {
      display: "flex",
      gap: line ? "var(--space-5)" : 4,
      borderBottom: line ? "1px solid var(--color-border-subtle)" : "none",
      background: line ? "transparent" : "var(--color-bg-muted)",
      padding: line ? 0 : 3,
      borderRadius: line ? 0 : "var(--radius-md)",
      overflowX: "auto",
      maxWidth: "100%"
    }
  }, items.map((t, i) => {
    const on = i === idx;
    return /*#__PURE__*/React.createElement("button", {
      key: t.id,
      ref: el => refs.current[i] = el,
      type: "button",
      role: "tab",
      id: `${auto}-tab-${t.id}`,
      "aria-selected": on,
      "aria-controls": `${auto}-panel-${t.id}`,
      tabIndex: on ? 0 : -1,
      onClick: () => onChange && onChange(t.id),
      style: {
        display: "inline-flex",
        alignItems: "center",
        gap: "var(--space-2)",
        padding: line ? "10px 2px 12px" : "8px 14px",
        font: "600 14px/1.2 var(--font-sans)",
        color: on ? "var(--color-fg-brand)" : "var(--color-fg-muted)",
        background: !line && on ? "var(--color-bg-surface)" : "transparent",
        border: "none",
        borderRadius: line ? 0 : "calc(var(--radius-md) - 2px)",
        boxShadow: line ? `inset 0 -2px 0 ${on ? "var(--tm-red-600)" : "transparent"}` : on ? "var(--elevation-100)" : "none",
        cursor: "pointer",
        whiteSpace: "nowrap",
        transition: "color var(--duration-fast-2) var(--ease-standard), box-shadow var(--duration-fast-2) var(--ease-standard)"
      }
    }, t.icon && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        display: "inline-flex"
      }
    }, t.icon), t.label, t.count != null && /*#__PURE__*/React.createElement("span", {
      style: {
        font: "700 11px/1 var(--font-sans)",
        padding: "3px 7px",
        borderRadius: "var(--radius-pill)",
        background: on ? "var(--color-bg-accent-subtle)" : "var(--color-bg-muted)",
        color: on ? "var(--color-fg-accent)" : "var(--color-fg-muted)",
        fontVariantNumeric: "var(--numeric-tabular)"
      }
    }, t.count));
  })), children !== undefined && /*#__PURE__*/React.createElement("div", {
    role: "tabpanel",
    id: `${auto}-panel-${items[idx] ? items[idx].id : ""}`,
    "aria-labelledby": `${auto}-tab-${items[idx] ? items[idx].id : ""}`,
    tabIndex: 0,
    style: {
      paddingTop: "var(--space-4)",
      outline: "none"
    }
  }, children));
}
Object.assign(__ds_scope, { Tabs });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/Tabs.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Coachmark.jsx
try { (() => {
/**
 * Coachmark — walktour of 2–5 steps: spotlight on a target + anchored
 * card with title, body, "n de N" counter and Anterior/Siguiente.
 * role="dialog", focus moves to the card each step, Esc closes, ←→
 * navigate. First-run education only — never hide critical info here.
 * Always dismissible and re-openable from Ayuda.
 */
function Coachmark({
  steps = [],
  open = false,
  onClose,
  onFinish
}) {
  const [i, setI] = React.useState(0);
  const [rect, setRect] = React.useState(null);
  const cardRef = React.useRef(null);
  React.useEffect(() => {
    if (open) setI(0);
  }, [open]);
  const step = steps[i];
  React.useEffect(() => {
    if (!open || !step) return;
    const measure = () => {
      const el = typeof step.target === "string" ? document.querySelector(step.target) : step.target && step.target.current;
      setRect(el ? el.getBoundingClientRect() : null);
    };
    measure();
    window.addEventListener("resize", measure);
    window.addEventListener("scroll", measure, true);
    return () => {
      window.removeEventListener("resize", measure);
      window.removeEventListener("scroll", measure, true);
    };
  }, [open, i, step]);
  React.useEffect(() => {
    if (open && cardRef.current) cardRef.current.focus();
  }, [open, i]);
  React.useEffect(() => {
    if (!open) return;
    const onKey = e => {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose && onClose();
      } else if (e.key === "ArrowRight" && i < steps.length - 1) setI(i + 1);else if (e.key === "ArrowLeft" && i > 0) setI(i - 1);
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [open, i, steps.length, onClose]);
  if (!open || !step) return null;
  const last = i === steps.length - 1;
  const pad = 6;
  const vw = typeof window !== "undefined" ? window.innerWidth : 1280;
  const vh = typeof window !== "undefined" ? window.innerHeight : 800;
  const cardW = 300;
  const below = rect ? rect.bottom + pad + 12 : vh / 2;
  const flip = rect && below + 170 > vh && rect.top > 200;
  const left = rect ? Math.max(12, Math.min(vw - cardW - 12, rect.left + rect.width / 2 - cardW / 2)) : vw / 2 - cardW / 2;
  return /*#__PURE__*/React.createElement(React.Fragment, null, rect ? /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      top: rect.top - pad,
      left: rect.left - pad,
      width: rect.width + pad * 2,
      height: rect.height + pad * 2,
      borderRadius: "var(--radius-md)",
      boxShadow: "0 0 0 9999px var(--color-bg-overlay)",
      outline: "2px solid var(--color-border-focus)",
      outlineOffset: 2,
      zIndex: "var(--z-overlay)",
      pointerEvents: "none"
    }
  }) : /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      inset: 0,
      background: "var(--color-bg-overlay)",
      zIndex: "var(--z-overlay)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    ref: cardRef,
    role: "dialog",
    "aria-label": typeof step.title === "string" ? step.title : "Recorrido",
    tabIndex: -1,
    style: {
      position: "fixed",
      top: flip ? undefined : below,
      bottom: flip ? vh - rect.top + pad + 12 : undefined,
      left,
      width: cardW,
      boxSizing: "border-box",
      background: "var(--color-bg-surface-raised)",
      borderRadius: "var(--radius-lg)",
      boxShadow: "var(--elevation-400)",
      padding: "var(--space-4)",
      zIndex: "var(--z-popover)",
      fontFamily: "var(--font-sans)",
      outline: "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      justifyContent: "space-between",
      gap: "var(--space-2)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 15px/1.35 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, step.title), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar recorrido",
    onClick: onClose,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 26,
      height: 26,
      flexShrink: 0,
      border: "none",
      background: "transparent",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-xs)",
      margin: "-4px -6px 0 0"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "14",
    height: "14",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })))), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)"
    }
  }, step.content), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      gap: "var(--space-2)",
      marginTop: "var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, i + 1, " de ", steps.length), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      gap: "var(--space-2)"
    }
  }, i > 0 && /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => setI(i - 1),
    style: {
      height: 30,
      paddingInline: 12,
      font: "600 12.5px/1 var(--font-sans)",
      color: "var(--color-fg-brand)",
      background: "transparent",
      border: "1.5px solid currentColor",
      borderRadius: "var(--control-radius)",
      cursor: "pointer"
    }
  }, "Anterior"), /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => {
      if (last) {
        onFinish && onFinish();
        onClose && onClose();
      } else setI(i + 1);
    },
    style: {
      height: 30,
      paddingInline: 14,
      font: "600 12.5px/1 var(--font-sans)",
      color: "var(--color-fg-on-solid)",
      background: "var(--color-bg-brand-solid)",
      border: "none",
      borderRadius: "var(--control-radius)",
      cursor: "pointer"
    }
  }, last ? "Entendido" : "Siguiente")))));
}
Object.assign(__ds_scope, { Coachmark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Coachmark.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Drawer.jsx
try { (() => {
const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';

/**
 * Drawer — side-anchored surface for contextual detail/edit without
 * losing list context (the ops-console workhorse). Modal mode: scrim,
 * focus trap, Esc; focus returns to the invoker on close. Make it
 * URL-addressable when it shows an entity.
 */
function Drawer({
  open = false,
  onClose,
  title,
  children,
  footer,
  size = "md",
  side = "right",
  style
}) {
  const panelRef = React.useRef(null);
  const titleId = React.useId();
  React.useEffect(() => {
    if (!open) return;
    const invoker = document.activeElement;
    const prevOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    const t = setTimeout(() => {
      if (!panelRef.current) return;
      const first = panelRef.current.querySelector("[data-autofocus]") || panelRef.current.querySelector(FOCUSABLE) || panelRef.current;
      first.focus();
    }, 10);
    const onKey = e => {
      if (e.key === "Escape") {
        e.stopPropagation();
        onClose && onClose();
      }
      if (e.key === "Tab" && panelRef.current) {
        const els = Array.from(panelRef.current.querySelectorAll(FOCUSABLE));
        if (els.length === 0) return;
        const first = els[0],
          last = els[els.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    };
    document.addEventListener("keydown", onKey, true);
    return () => {
      clearTimeout(t);
      document.removeEventListener("keydown", onKey, true);
      document.body.style.overflow = prevOverflow;
      if (invoker && invoker.focus) invoker.focus();
    };
  }, [open, onClose]);
  if (!open) return null;
  const widths = {
    sm: 320,
    md: 400,
    lg: 520
  };
  return /*#__PURE__*/React.createElement("div", {
    onMouseDown: e => {
      if (e.target === e.currentTarget) onClose && onClose();
    },
    style: {
      position: "fixed",
      inset: 0,
      background: "var(--color-bg-overlay)",
      display: "flex",
      justifyContent: side === "right" ? "flex-end" : "flex-start",
      zIndex: "var(--z-overlay)",
      overscrollBehavior: "contain"
    }
  }, /*#__PURE__*/React.createElement("div", {
    ref: panelRef,
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": title ? titleId : undefined,
    tabIndex: -1,
    style: {
      width: "100%",
      maxWidth: widths[size] || widths.md,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      background: "var(--color-bg-surface-raised)",
      boxShadow: "var(--elevation-400)",
      fontFamily: "var(--font-sans)",
      outline: "none",
      boxSizing: "border-box",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: "var(--space-3)",
      padding: "var(--space-5) var(--space-5) var(--space-3)",
      borderBottom: "1px solid var(--color-border-subtle)"
    }
  }, /*#__PURE__*/React.createElement("h2", {
    id: titleId,
    style: {
      flex: 1,
      margin: 0,
      font: "700 17px/1.35 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar panel",
    onClick: onClose,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 28,
      height: 28,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-sm)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: "auto",
      padding: "var(--space-4) var(--space-5)"
    }
  }, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "flex-end",
      gap: "var(--space-2)",
      padding: "var(--space-4) var(--space-5)",
      borderTop: "1px solid var(--color-border-subtle)"
    }
  }, footer)));
}
Object.assign(__ds_scope, { Drawer });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Drawer.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Lightbox.jsx
try { (() => {
/**
 * Lightbox — fullscreen viewer for documents-as-images (INE,
 * comprobantes) and photography. Esc closes (returns focus), ←→
 * navigate, +/− zoom (keyboard path for the zoom drag), counter
 * announced politely. Focus is trapped while open.
 */
function Lightbox({
  open = false,
  images = [],
  index = 0,
  onClose,
  onIndexChange
}) {
  const [zoom, setZoom] = React.useState(1);
  const dialogRef = React.useRef(null);
  const img = images[index];
  React.useEffect(() => {
    setZoom(1);
  }, [index, open]);
  React.useEffect(() => {
    if (!open) return;
    const prev = document.activeElement;
    const t = setTimeout(() => {
      if (dialogRef.current) dialogRef.current.focus();
    }, 10);
    const onKey = e => {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose && onClose();
      } else if (e.key === "ArrowRight" && index < images.length - 1) onIndexChange && onIndexChange(index + 1);else if (e.key === "ArrowLeft" && index > 0) onIndexChange && onIndexChange(index - 1);else if (e.key === "+" || e.key === "=") setZoom(z => Math.min(3, z + 0.5));else if (e.key === "-") setZoom(z => Math.max(1, z - 0.5));else if (e.key === "Tab" && dialogRef.current) {
        const f = dialogRef.current.querySelectorAll("button");
        if (!f.length) return;
        const first = f[0],
          last = f[f.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    };
    document.addEventListener("keydown", onKey);
    return () => {
      clearTimeout(t);
      document.removeEventListener("keydown", onKey);
      if (prev && prev.focus) prev.focus();
    };
  }, [open, index, images.length, onClose, onIndexChange]);
  if (!open || !img) return null;
  const ctl = dis => ({
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    width: 40,
    height: 40,
    borderRadius: "50%",
    border: "none",
    background: "var(--tm-alpha-white-200)",
    color: dis ? "rgba(255,255,255,.35)" : "#fff",
    cursor: dis ? "default" : "pointer",
    flexShrink: 0
  });
  return /*#__PURE__*/React.createElement("div", {
    ref: dialogRef,
    role: "dialog",
    "aria-modal": "true",
    "aria-label": img.alt || "Visor de imagen",
    tabIndex: -1,
    style: {
      position: "fixed",
      inset: 0,
      zIndex: "var(--z-modal)",
      background: "rgba(3, 6, 16, 0.88)",
      display: "flex",
      flexDirection: "column",
      fontFamily: "var(--font-sans)",
      outline: "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "space-between",
      padding: "14px 18px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-live": "polite",
    style: {
      font: "600 13px/1.2 var(--font-sans)",
      color: "#fff",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, index + 1, " de ", images.length, img.caption ? ` · ${img.caption}` : ""), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Reducir",
    disabled: zoom <= 1,
    onClick: () => setZoom(z => Math.max(1, z - 0.5)),
    style: ctl(zoom <= 1)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "17",
    height: "17",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "8"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m21 21-4.3-4.3M8 11h6"
  }))), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Ampliar",
    disabled: zoom >= 3,
    onClick: () => setZoom(z => Math.min(3, z + 0.5)),
    style: ctl(zoom >= 3)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "17",
    height: "17",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "8"
  }), /*#__PURE__*/React.createElement("path", {
    d: "m21 21-4.3-4.3M11 8v6M8 11h6"
  }))), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar visor",
    onClick: onClose,
    style: ctl(false)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "17",
    height: "17",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  }))))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 12,
      padding: "0 12px 18px"
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Anterior",
    disabled: index === 0,
    onClick: () => onIndexChange && onIndexChange(index - 1),
    style: ctl(index === 0)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m15 18-6-6 6-6"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      height: "100%",
      overflow: "auto",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: img.src,
    alt: img.alt || "",
    style: {
      maxWidth: zoom === 1 ? "100%" : "none",
      maxHeight: zoom === 1 ? "100%" : "none",
      width: zoom === 1 ? undefined : `${zoom * 100}%`,
      borderRadius: "var(--radius-sm)",
      transition: "width var(--duration-base) var(--ease-standard)"
    }
  })), /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Siguiente",
    disabled: index === images.length - 1,
    onClick: () => onIndexChange && onIndexChange(index + 1),
    style: ctl(index === images.length - 1)
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "m9 18 6-6-6-6"
  })))));
}
Object.assign(__ds_scope, { Lightbox });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Lightbox.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Modal.jsx
try { (() => {
const FOCUSABLE = 'a[href], button:not([disabled]), input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])';

/**
 * Modal / Dialog — interrupts the flow until completed or cancelled.
 * Don't use for content that deserves a page, non-blocking info
 * (→ Toast/Alert), or nesting (max depth 1).
 *
 * Focus contract: on open, focus moves in (data-autofocus or first
 * control; alertdialog → least-destructive action); trapped inside;
 * Esc closes; on close, focus RETURNS to the invoker. Background
 * scroll locked. role="alertdialog" for destructive confirmations —
 * label the primary with the explicit verb ("Cancelar póliza"),
 * never "OK", and never pre-focus the destructive button.
 */
function Modal({
  open = false,
  onClose,
  title,
  children,
  footer,
  size = "md",
  role = "dialog",
  dismissible = true,
  style
}) {
  const panelRef = React.useRef(null);
  const titleId = React.useId();
  React.useEffect(() => {
    if (!open) return;
    const invoker = document.activeElement;
    const prevOverflow = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    const t = setTimeout(() => {
      if (!panelRef.current) return;
      const auto = panelRef.current.querySelector("[data-autofocus]");
      const first = auto || panelRef.current.querySelector(FOCUSABLE) || panelRef.current;
      first.focus();
    }, 10);
    const onKey = e => {
      if (e.key === "Escape" && dismissible) {
        e.stopPropagation();
        onClose && onClose();
      }
      if (e.key === "Tab" && panelRef.current) {
        const els = Array.from(panelRef.current.querySelectorAll(FOCUSABLE));
        if (els.length === 0) return;
        const first = els[0],
          last = els[els.length - 1];
        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      }
    };
    document.addEventListener("keydown", onKey, true);
    return () => {
      clearTimeout(t);
      document.removeEventListener("keydown", onKey, true);
      document.body.style.overflow = prevOverflow;
      if (invoker && invoker.focus) invoker.focus();
    };
  }, [open, dismissible, onClose]);
  if (!open) return null;
  const widths = {
    sm: 400,
    md: 560,
    lg: 720,
    xl: 920
  };
  return /*#__PURE__*/React.createElement("div", {
    onMouseDown: e => {
      if (dismissible && e.target === e.currentTarget) onClose && onClose();
    },
    style: {
      position: "fixed",
      inset: 0,
      background: "var(--color-bg-overlay)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      padding: "var(--space-5)",
      zIndex: "var(--z-modal)",
      overscrollBehavior: "contain"
    }
  }, /*#__PURE__*/React.createElement("div", {
    ref: panelRef,
    role: role,
    "aria-modal": "true",
    "aria-labelledby": title ? titleId : undefined,
    tabIndex: -1,
    style: {
      width: "100%",
      maxWidth: widths[size] || widths.md,
      maxHeight: "min(85vh, 900px)",
      display: "flex",
      flexDirection: "column",
      background: "var(--color-bg-surface-raised)",
      borderRadius: "var(--radius-xl)",
      boxShadow: "var(--elevation-500)",
      fontFamily: "var(--font-sans)",
      outline: "none",
      overflow: "hidden",
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: "var(--space-3)",
      padding: "var(--space-5) var(--space-5) var(--space-3)"
    }
  }, /*#__PURE__*/React.createElement("h2", {
    id: titleId,
    style: {
      flex: 1,
      margin: 0,
      font: "700 18px/1.35 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), dismissible && /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": "Cerrar di\xE1logo",
    onClick: onClose,
    style: {
      display: "inline-flex",
      alignItems: "center",
      justifyContent: "center",
      width: 28,
      height: 28,
      flexShrink: 0,
      background: "transparent",
      border: "none",
      color: "var(--color-fg-subtle)",
      cursor: "pointer",
      borderRadius: "var(--radius-sm)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    "aria-hidden": "true",
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M18 6 6 18M6 6l12 12"
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "0 var(--space-5)",
      overflowY: "auto",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)",
      flex: "1 1 auto"
    }
  }, children), footer && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "flex-end",
      gap: "var(--space-2)",
      padding: "var(--space-4) var(--space-5) var(--space-5)"
    }
  }, footer)));
}
Object.assign(__ds_scope, { Modal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Modal.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Popover.jsx
try { (() => {
/**
 * Popover — interactive mini-surface (filters, quick help with links).
 * Opens on click/Enter; Esc and outside-click close; focus returns to
 * the trigger. Trigger gets aria-expanded + aria-haspopup.
 */
function Popover({
  trigger,
  children,
  align = "start",
  width = 280,
  style
}) {
  const [open, setOpen] = React.useState(false);
  const rootRef = React.useRef(null);
  const triggerRef = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = e => {
      if (rootRef.current && !rootRef.current.contains(e.target)) setOpen(false);
    };
    const onKey = e => {
      if (e.key === "Escape") {
        setOpen(false);
        if (triggerRef.current) triggerRef.current.focus();
      }
    };
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onKey);
    return () => {
      document.removeEventListener("mousedown", onDoc);
      document.removeEventListener("keydown", onKey);
    };
  }, [open]);
  const triggerEl = React.cloneElement(trigger, {
    ref: triggerRef,
    "aria-haspopup": "dialog",
    "aria-expanded": open,
    onClick: e => {
      trigger.props.onClick && trigger.props.onClick(e);
      setOpen(o => !o);
    }
  });
  return /*#__PURE__*/React.createElement("span", {
    ref: rootRef,
    style: {
      position: "relative",
      display: "inline-block",
      ...style
    }
  }, triggerEl, open && /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    style: {
      position: "absolute",
      top: "calc(100% + 6px)",
      [align === "end" ? "right" : "left"]: 0,
      width,
      padding: "var(--space-4)",
      background: "var(--color-bg-surface-raised)",
      border: "1px solid var(--color-border-subtle)",
      borderRadius: "var(--radius-md)",
      boxShadow: "var(--elevation-300)",
      zIndex: "var(--z-popover)",
      fontFamily: "var(--font-sans)",
      font: "var(--text-sm)",
      color: "var(--color-fg-default)",
      boxSizing: "border-box"
    }
  }, children));
}
Object.assign(__ds_scope, { Popover });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Popover.jsx", error: String((e && e.message) || e) }); }

// components/overlays/Tooltip.jsx
try { (() => {
/**
 * Tooltip — non-interactive supplementary text (≤10 words). Appears
 * on hover AND focus after a 300ms delay; stays while hovered; Esc
 * dismisses (WCAG 1.4.13). Never put required info, links, or
 * buttons here — that's a Popover. Mandatory on icon-only buttons.
 */
function Tooltip({
  content,
  children,
  placement = "top",
  delay = 300
}) {
  const [show, setShow] = React.useState(false);
  const timer = React.useRef(null);
  const id = React.useId();
  const openSoon = () => {
    timer.current = setTimeout(() => setShow(true), delay);
  };
  const cancel = () => {
    clearTimeout(timer.current);
    setShow(false);
  };
  React.useEffect(() => {
    if (!show) return;
    const onKey = e => {
      if (e.key === "Escape") setShow(false);
    };
    document.addEventListener("keydown", onKey);
    return () => document.removeEventListener("keydown", onKey);
  }, [show]);
  const child = React.Children.only(children);
  const trigger = React.cloneElement(child, {
    "aria-describedby": show ? id : undefined,
    onMouseEnter: e => {
      child.props.onMouseEnter && child.props.onMouseEnter(e);
      openSoon();
    },
    onMouseLeave: e => {
      child.props.onMouseLeave && child.props.onMouseLeave(e);
      cancel();
    },
    onFocus: e => {
      child.props.onFocus && child.props.onFocus(e);
      openSoon();
    },
    onBlur: e => {
      child.props.onBlur && child.props.onBlur(e);
      cancel();
    }
  });
  const pos = {
    top: {
      bottom: "calc(100% + 6px)",
      left: "50%",
      transform: "translateX(-50%)"
    },
    bottom: {
      top: "calc(100% + 6px)",
      left: "50%",
      transform: "translateX(-50%)"
    },
    right: {
      left: "calc(100% + 6px)",
      top: "50%",
      transform: "translateY(-50%)"
    },
    left: {
      right: "calc(100% + 6px)",
      top: "50%",
      transform: "translateY(-50%)"
    }
  }[placement];
  return /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-flex"
    },
    onMouseEnter: openSoon,
    onMouseLeave: cancel
  }, trigger, show && /*#__PURE__*/React.createElement("span", {
    role: "tooltip",
    id: id,
    style: {
      position: "absolute",
      ...pos,
      padding: "6px 10px",
      font: "500 12px/1.35 var(--font-sans)",
      color: "var(--tm-neutral-0)",
      background: "var(--tm-navy-800)",
      borderRadius: "var(--radius-sm)",
      whiteSpace: "nowrap",
      boxShadow: "var(--elevation-200)",
      zIndex: "var(--z-tooltip)",
      pointerEvents: "none"
    }
  }, content));
}
Object.assign(__ds_scope, { Tooltip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/overlays/Tooltip.jsx", error: String((e && e.message) || e) }); }

// showcase/app.jsx
try { (() => {
const NS = window.TandaMirandaDesignSystem_a4a6f5;
const {
  AppBar,
  SideNav,
  SegmentedControl,
  Typography
} = NS;
const {
  SecFoundations,
  SecActions,
  SecForms
} = window.SC_A;
const R = window.__resources || {};
const {
  SecNav,
  SecFeedback,
  SecOverlays,
  SecData,
  SecDomain
} = window.SC_B;
const SECTIONS = [{
  id: "fundamentos",
  label: "Fundamentos"
}, {
  id: "acciones",
  label: "Acciones"
}, {
  id: "formularios",
  label: "Formularios"
}, {
  id: "navegacion",
  label: "Navegación"
}, {
  id: "feedback",
  label: "Retroalimentación"
}, {
  id: "overlays",
  label: "Superposiciones"
}, {
  id: "datos",
  label: "Datos"
}, {
  id: "dominio",
  label: "Dominio fintech"
}];
function Showcase() {
  const [theme, setTheme] = React.useState(() => {
    try {
      return localStorage.getItem("tm-showcase-theme") || "light";
    } catch (e) {
      return "light";
    }
  });
  const [density, setDensity] = React.useState("comfortable");
  const [active, setActive] = React.useState("fundamentos");
  React.useEffect(() => {
    document.documentElement.setAttribute("data-theme", theme);
    try {
      localStorage.setItem("tm-showcase-theme", theme);
    } catch (e) {}
  }, [theme]);
  React.useEffect(() => {
    const els = SECTIONS.map(s => document.getElementById(s.id)).filter(Boolean);
    const obs = new IntersectionObserver(entries => {
      const vis = entries.filter(e => e.isIntersecting).sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
      if (vis[0]) setActive(vis[0].target.id);
    }, {
      rootMargin: "-80px 0px -60% 0px"
    });
    els.forEach(el => obs.observe(el));
    return () => obs.disconnect();
  }, []);
  const goTo = id => {
    const el = document.getElementById(id);
    if (!el) return;
    const top = el.getBoundingClientRect().top + window.scrollY - 76;
    window.scrollTo({
      top,
      behavior: "smooth"
    });
  };
  return /*#__PURE__*/React.createElement("div", {
    "data-density": density,
    style: {
      minHeight: "100vh",
      fontFamily: "var(--font-sans)"
    }
  }, /*#__PURE__*/React.createElement(AppBar, {
    sticky: true,
    brand: /*#__PURE__*/React.createElement("img", {
      src: R.logoTm || "assets/logo/tm-logo-horizontal-blanco-rojo.svg",
      alt: "Tanda Miranda",
      style: {
        height: 26
      }
    }),
    title: "Showcase de componentes",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SegmentedControl, {
      label: "Densidad",
      value: density,
      onChange: setDensity,
      options: [{
        value: "comfortable",
        label: "Cómoda"
      }, {
        value: "compact",
        label: "Compacta"
      }],
      style: {
        background: "var(--tm-alpha-white-200)"
      }
    }), /*#__PURE__*/React.createElement(SegmentedControl, {
      label: "Tema",
      value: theme,
      onChange: setTheme,
      options: [{
        value: "light",
        label: "Claro"
      }, {
        value: "dark",
        label: "Oscuro"
      }],
      style: {
        background: "var(--tm-alpha-white-200)"
      }
    }))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "stretch"
    }
  }, /*#__PURE__*/React.createElement(SideNav, {
    width: 220,
    activeId: active,
    onSelect: goTo,
    style: {
      position: "sticky",
      top: 60,
      alignSelf: "flex-start",
      height: "calc(100vh - 60px)",
      overflowY: "auto"
    },
    sections: [{
      header: "Secciones",
      items: SECTIONS.map(s => ({
        id: s.id,
        label: s.label
      }))
    }],
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        padding: "var(--space-3)",
        font: "var(--text-xs)",
        color: "var(--color-fg-subtle)"
      }
    }, "84 componentes \xB7 tokens sem\xE1nticos \xB7 WCAG 2.2 AA")
  }), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      minWidth: 0,
      padding: "30px 36px 60px",
      maxWidth: 1120,
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      marginBottom: "var(--space-6)"
    }
  }, /*#__PURE__*/React.createElement(Typography, {
    variant: "overline",
    color: "accent"
  }, "Sistema de dise\xF1o"), /*#__PURE__*/React.createElement(Typography, {
    variant: "h1",
    style: {
      marginTop: 8
    }
  }, "Todos los componentes, en vivo"), /*#__PURE__*/React.createElement(Typography, {
    variant: "body",
    color: "muted",
    style: {
      marginTop: 10,
      maxWidth: "72ch"
    }
  }, "Inventario interactivo del sistema: cambia tema y densidad arriba \u2014 cada pieza se re-mapea por tokens, sin cambios de c\xF3digo. Registro app por defecto; los bloques punteados muestran el registro marketing.")), /*#__PURE__*/React.createElement(SecFoundations, null), /*#__PURE__*/React.createElement(SecActions, null), /*#__PURE__*/React.createElement(SecForms, null), /*#__PURE__*/React.createElement(SecNav, null), /*#__PURE__*/React.createElement(SecFeedback, null), /*#__PURE__*/React.createElement(SecOverlays, null), /*#__PURE__*/React.createElement(SecData, null), /*#__PURE__*/React.createElement(SecDomain, null))));
}
ReactDOM.createRoot(document.getElementById("root")).render(/*#__PURE__*/React.createElement(Showcase, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "showcase/app.jsx", error: String((e && e.message) || e) }); }

// showcase/sections-a.jsx
try { (() => {
const NS = window.TandaMirandaDesignSystem_a4a6f5;
const {
  Button,
  IconButton,
  SegmentedControl,
  ButtonGroup,
  ToggleGroup,
  Fab,
  SpeedDial,
  CopyButton,
  Field,
  Input,
  Textarea,
  Select,
  Combobox,
  Checkbox,
  RadioGroup,
  Switch,
  Slider,
  DateInput,
  DatePicker,
  TimePicker,
  NumberInput,
  Rating,
  TransferList,
  FileUpload,
  Typography,
  Divider,
  Card,
  Tag,
  Badge,
  StatusIndicator,
  Avatar,
  AnchorBadge
} = NS;
const Ic = ({
  d,
  s = 16
}) => /*#__PURE__*/React.createElement("svg", {
  width: s,
  height: s,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "1.8",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}, /*#__PURE__*/React.createElement("path", {
  d: d
}));
const ICONS = {
  gear: "M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6ZM19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 1 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 1 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 1 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 1 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1Z",
  plus: "M12 5v14M5 12h14",
  down: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M7 10l5 5 5-5M12 15V3",
  bell: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9M10.3 21a1.94 1.94 0 0 0 3.4 0",
  money: "M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6",
  doc: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8zM14 2v6h6",
  search: "M11 19a8 8 0 1 0 0-16 8 8 0 0 0 0 16ZM21 21l-4.3-4.3",
  home: "m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"
};
const mxn = n => "MXN " + new Intl.NumberFormat("es-MX", {
  minimumFractionDigits: 2
}).format(n);
function Spec({
  title,
  note,
  children,
  cols
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: "var(--space-5)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: 10,
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "700 13px/1.3 var(--font-sans)",
      color: "var(--color-fg-default)"
    }
  }, title), note && /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, note)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: cols ? "grid" : "flex",
      gridTemplateColumns: cols ? `repeat(${cols}, minmax(0, 1fr))` : undefined,
      flexWrap: "wrap",
      alignItems: cols ? "start" : "center",
      gap: "var(--space-4)"
    }
  }, cols ? React.Children.map(children, c => c && /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, c)) : children));
}
function Section({
  id,
  title,
  lead,
  children
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: "sc-section",
    "aria-labelledby": `${id}-h`,
    style: {
      marginBottom: "var(--space-7)"
    }
  }, /*#__PURE__*/React.createElement("h2", {
    id: `${id}-h`,
    style: {
      margin: 0,
      font: "var(--text-h3)",
      color: "var(--color-fg-default)"
    }
  }, title), lead && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      font: "var(--text-sm)",
      color: "var(--color-fg-muted)",
      maxWidth: "70ch"
    }
  }, lead), /*#__PURE__*/React.createElement(Divider, {
    spacing: "16px"
  }), children);
}

/* ---------- Fundamentos ---------- */
function SecFoundations() {
  const roles = [["bg-brand-solid", "var(--color-bg-brand-solid)", "#fff"], ["bg-accent-solid", "var(--color-bg-accent-solid)", "#fff"], ["bg-success-solid", "var(--color-bg-success-solid)", "#fff"], ["bg-warning-solid", "var(--color-bg-warning-solid)", "var(--tm-navy-900)"], ["bg-info-solid", "var(--color-bg-info-solid)", "#fff"], ["bg-selected", "var(--color-bg-selected)", "var(--color-fg-brand)"], ["bg-subtle", "var(--color-bg-subtle)", "var(--color-fg-default)"], ["bg-muted", "var(--color-bg-muted)", "var(--color-fg-default)"]];
  return /*#__PURE__*/React.createElement(Section, {
    id: "fundamentos",
    title: "Fundamentos",
    lead: "Tokens sem\xE1nticos (los componentes nunca tocan las rampas primitivas), tipograf\xEDa de la marca y figuras tabulares para todo lo num\xE9rico."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Roles de color (tier 2)"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(8, minmax(0, 1fr))",
      gap: 6,
      width: "100%"
    }
  }, roles.map(([n, bg, fg]) => /*#__PURE__*/React.createElement("div", {
    key: n,
    style: {
      background: bg,
      color: fg,
      borderRadius: 8,
      border: "1px solid var(--color-border-subtle)",
      padding: "16px 8px 6px",
      font: "600 9.5px/1.3 var(--font-mono)",
      wordBreak: "break-all"
    }
  }, n)))), /*#__PURE__*/React.createElement(Spec, {
    title: "Tipograf\xEDa",
    note: "Serif = s\xF3lo it\xE1lica, titulares emocionales; Noto Sans para todo lo dem\xE1s"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Typography, {
    variant: "accent-lg"
  }, "\xA1Metas que s\xED se cumplen!"), /*#__PURE__*/React.createElement(Typography, {
    variant: "h2",
    style: {
      marginTop: 8
    }
  }, "T\xEDtulo estructural"), /*#__PURE__*/React.createElement(Typography, {
    variant: "subtitle-sm",
    color: "muted",
    style: {
      marginTop: 4
    }
  }, "Subt\xEDtulo SemiBold Italic"), /*#__PURE__*/React.createElement(Typography, {
    variant: "body",
    color: "muted",
    style: {
      marginTop: 8,
      maxWidth: "56ch"
    }
  }, "Cuerpo de lectura a 16px m\xEDnimo. Los identificadores usan mono: ", /*#__PURE__*/React.createElement(Typography, {
    variant: "code",
    as: "code"
  }, "TM-2026-04471"), "."), /*#__PURE__*/React.createElement(Typography, {
    variant: "sm",
    tabular: true,
    style: {
      marginTop: 8
    }
  }, "Tabular: MXN 62,340.00 \xB7 24.9% \xB7 15/07/2026"))), /*#__PURE__*/React.createElement(Spec, {
    title: "Registros",
    note: "mismo componente, un atributo cambia el registro"
  }, /*#__PURE__*/React.createElement("div", {
    "data-surface": "marketing",
    style: {
      display: "flex",
      gap: 12,
      alignItems: "center",
      padding: "14px 16px",
      border: "1px dashed var(--color-border-default)",
      borderRadius: 12
    }
  }, /*#__PURE__*/React.createElement(Button, null, "Aplica hoy"), /*#__PURE__*/React.createElement(Input, {
    placeholder: "Correo electr\xF3nico",
    style: {
      minWidth: 190
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, "marketing: pill \xB7 MAY\xDASCULAS \xB7 rojo oficial")), /*#__PURE__*/React.createElement("div", {
    "data-surface": "app",
    style: {
      display: "flex",
      gap: 12,
      alignItems: "center",
      padding: "14px 16px",
      border: "1px dashed var(--color-border-default)",
      borderRadius: 12
    }
  }, /*#__PURE__*/React.createElement(Button, null, "Solicitar cr\xE9dito"), /*#__PURE__*/React.createElement(Input, {
    placeholder: "Correo electr\xF3nico",
    style: {
      minWidth: 190
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)"
    }
  }, "app: radio md \xB7 oraci\xF3n \xB7 rojo-650 AA"))));
}

/* ---------- Acciones ---------- */
function SecActions() {
  const [seg, setSeg] = React.useState("mensual");
  const [view, setView] = React.useState("tabla");
  const [fmt, setFmt] = React.useState(["pdf"]);
  return /*#__PURE__*/React.createElement(Section, {
    id: "acciones",
    title: "Acciones",
    lead: "Un primario por vista; etiquetas verbo-primero de \u22643 palabras; loading conserva el ancho y bloquea duplicados."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Button \u2014 variantes"
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "primary"
  }, "Solicitar cr\xE9dito"), /*#__PURE__*/React.createElement(Button, {
    variant: "navy"
  }, "Ver detalle"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary"
  }, "Cancelar"), /*#__PURE__*/React.createElement(Button, {
    variant: "tertiary"
  }, "Descargar"), /*#__PURE__*/React.createElement(Button, {
    variant: "destructive"
  }, "Cancelar cr\xE9dito"), /*#__PURE__*/React.createElement(Button, {
    variant: "link"
  }, "Ver contrato")), /*#__PURE__*/React.createElement(Spec, {
    title: "Tama\xF1os y estados"
  }, /*#__PURE__*/React.createElement(Button, {
    size: "sm"
  }, "Guardar"), /*#__PURE__*/React.createElement(Button, {
    size: "md"
  }, "Guardar"), /*#__PURE__*/React.createElement(Button, {
    size: "lg"
  }, "Guardar"), /*#__PURE__*/React.createElement(Button, {
    loading: true
  }, "Enviando"), /*#__PURE__*/React.createElement(Button, {
    disabled: true
  }, "Enviar"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    iconStart: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.down,
      s: 14
    })
  }, "Exportar")), /*#__PURE__*/React.createElement(Spec, {
    title: "IconButton \xB7 CopyButton \xB7 SegmentedControl"
  }, /*#__PURE__*/React.createElement(IconButton, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.gear
    }),
    label: "Configuraci\xF3n",
    variant: "ghost"
  }), /*#__PURE__*/React.createElement(IconButton, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.gear
    }),
    label: "Configuraci\xF3n",
    variant: "outline"
  }), /*#__PURE__*/React.createElement(IconButton, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.gear
    }),
    label: "Configuraci\xF3n",
    variant: "solid"
  }), /*#__PURE__*/React.createElement(CopyButton, {
    value: "TM-P-88214",
    label: "Copiar folio",
    showLabel: true
  }), /*#__PURE__*/React.createElement(SegmentedControl, {
    label: "Frecuencia",
    value: seg,
    onChange: setSeg,
    options: [{
      value: "quincenal",
      label: "Quincenal"
    }, {
      value: "mensual",
      label: "Mensual"
    }]
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "ButtonGroup \xB7 ToggleGroup"
  }, /*#__PURE__*/React.createElement(ButtonGroup, {
    label: "Vista",
    size: "sm",
    items: [{
      label: "Tabla",
      selected: view === "tabla",
      onClick: () => setView("tabla")
    }, {
      label: "Tarjetas",
      selected: view === "tarjetas",
      onClick: () => setView("tarjetas")
    }]
  }), /*#__PURE__*/React.createElement(ToggleGroup, {
    label: "Formatos",
    value: fmt,
    onChange: setFmt,
    options: [{
      value: "pdf",
      label: "PDF"
    }, {
      value: "csv",
      label: "CSV"
    }, {
      value: "xml",
      label: "XML"
    }]
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "Fab \xB7 SpeedDial",
    note: "m\xE1x. uno por pantalla m\xF3vil; acciones siempre etiquetadas"
  }, /*#__PURE__*/React.createElement(Fab, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.plus,
      s: 20
    }),
    label: "Nueva solicitud"
  }), /*#__PURE__*/React.createElement(Fab, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.plus,
      s: 18
    }),
    label: "Nueva solicitud",
    extended: true
  }), /*#__PURE__*/React.createElement(SpeedDial, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.plus,
      s: 20
    }),
    label: "Acciones r\xE1pidas",
    direction: "down",
    actions: [{
      icon: /*#__PURE__*/React.createElement(Ic, {
        d: ICONS.money,
        s: 15
      }),
      label: "Registrar pago"
    }, {
      icon: /*#__PURE__*/React.createElement(Ic, {
        d: ICONS.doc,
        s: 15
      }),
      label: "Nueva solicitud"
    }]
  })));
}

/* ---------- Formularios ---------- */
function SecForms() {
  const [banco, setBanco] = React.useState("012");
  const [tipo, setTipo] = React.useState("nomina");
  const [fecha, setFecha] = React.useState("15/08/2026");
  const [hora, setHora] = React.useState("14:00");
  const [plazoN, setPlazoN] = React.useState(24);
  const [monto, setMonto] = React.useState(85000);
  const [plazo, setPlazo] = React.useState("24");
  const [avisos, setAvisos] = React.useState(true);
  const [rate, setRate] = React.useState(4);
  const [suc, setSuc] = React.useState(["hmo"]);
  return /*#__PURE__*/React.createElement(Section, {
    id: "formularios",
    title: "Formularios",
    lead: "Etiqueta visible arriba, ayuda de una l\xEDnea, error = qu\xE9 pas\xF3 + c\xF3mo corregirlo. Formato al salir del campo, completitud al enviar; pegar nunca se bloquea."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Field + Input / Select / Combobox",
    cols: 3
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nombre completo",
    help: "Como aparece en tu identificaci\xF3n."
  }, /*#__PURE__*/React.createElement(Input, {
    placeholder: "Mar\xEDa Fernanda L\xF3pez",
    fullWidth: true
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Correo electr\xF3nico",
    error: "El correo debe incluir @ (escribiste maria.lopez)"
  }, /*#__PURE__*/React.createElement(Input, {
    defaultValue: "maria.lopez",
    fullWidth: true
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Tipo de ingreso"
  }, /*#__PURE__*/React.createElement(Select, {
    fullWidth: true,
    value: tipo,
    onChange: setTipo,
    options: [{
      value: "nomina",
      label: "Nómina",
      description: "Empleo formal con recibo"
    }, {
      value: "honorarios",
      label: "Honorarios"
    }]
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Banco de n\xF3mina",
    help: "Escribe para filtrar."
  }, /*#__PURE__*/React.createElement(Combobox, {
    fullWidth: true,
    value: banco,
    onChange: setBanco,
    placeholder: "Busca tu banco\u2026",
    options: [{
      value: "012",
      label: "BBVA México"
    }, {
      value: "072",
      label: "Banorte"
    }, {
      value: "014",
      label: "Santander"
    }, {
      value: "127",
      label: "Banco Azteca"
    }]
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Primer pago",
    help: "Teclado o calendario \u2014 dd/mm/aaaa."
  }, /*#__PURE__*/React.createElement(DatePicker, {
    fullWidth: true,
    value: fecha,
    onChange: setFecha,
    min: "15/07/2026",
    max: "31/12/2026"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Hora de contacto",
    help: "Horario CDMX."
  }, /*#__PURE__*/React.createElement(TimePicker, {
    fullWidth: true,
    value: hora,
    onChange: setHora
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "NumberInput \xB7 Slider",
    cols: 2
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Plazo",
    help: "Entre 6 y 48 quincenas."
  }, /*#__PURE__*/React.createElement(NumberInput, {
    fullWidth: true,
    value: plazoN,
    onChange: setPlazoN,
    min: 6,
    max: 48,
    step: 6,
    suffix: "quincenas",
    label: "Plazo"
  })), /*#__PURE__*/React.createElement(Slider, {
    label: "Monto del cr\xE9dito",
    min: 10000,
    max: 250000,
    step: 5000,
    value: monto,
    onChange: setMonto,
    format: v => new Intl.NumberFormat("es-MX", {
      style: "currency",
      currency: "MXN",
      maximumFractionDigits: 0
    }).format(v)
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "Selecci\xF3n",
    cols: 2
  }, /*#__PURE__*/React.createElement(RadioGroup, {
    legend: "Plazo",
    value: plazo,
    onChange: setPlazo,
    options: [{
      value: "12",
      label: "12 quincenas"
    }, {
      value: "24",
      label: "24 quincenas",
      description: "El más elegido"
    }, {
      value: "36",
      label: "36 quincenas"
    }]
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(Checkbox, {
    label: "Acepto el Aviso de Privacidad",
    defaultChecked: true
  }), /*#__PURE__*/React.createElement(Checkbox, {
    label: "Selecci\xF3n parcial",
    indeterminate: true
  }), /*#__PURE__*/React.createElement(Switch, {
    label: "Notificaciones de pago",
    description: "Aplica de inmediato",
    checked: avisos,
    onChange: setAvisos
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(Rating, {
    label: "Califica la atenci\xF3n",
    value: rate,
    onChange: setRate
  })))), /*#__PURE__*/React.createElement(Spec, {
    title: "TransferList \xB7 FileUpload",
    cols: 2
  }, /*#__PURE__*/React.createElement(TransferList, {
    value: suc,
    onChange: setSuc,
    height: 150,
    titles: {
      available: "Disponibles",
      selected: "En campaña"
    },
    options: [{
      value: "hmo",
      label: "Hermosillo"
    }, {
      value: "chih",
      label: "Chihuahua"
    }, {
      value: "mty",
      label: "Monterrey"
    }, {
      value: "tij",
      label: "Tijuana"
    }]
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Comprobante de ingresos",
    optional: true
  }, /*#__PURE__*/React.createElement(FileUpload, {
    accept: ".pdf,.jpg,.png",
    maxSizeMb: 10
  }))));
}
window.SC_A = {
  Spec,
  Section,
  Ic,
  ICONS,
  mxn,
  SecFoundations,
  SecActions,
  SecForms
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "showcase/sections-a.jsx", error: String((e && e.message) || e) }); }

// showcase/sections-b.jsx
try { (() => {
const NS = window.TandaMirandaDesignSystem_a4a6f5;
const {
  Button,
  IconButton,
  Tabs,
  Breadcrumbs,
  Stepper,
  Pagination,
  Menu,
  Link,
  BottomNav,
  MegaMenu,
  Alert,
  Toast,
  ProgressBar,
  ProgressRing,
  Spinner,
  Skeleton,
  EmptyState,
  Backdrop,
  Modal,
  Drawer,
  Tooltip,
  Popover,
  Lightbox,
  Coachmark,
  Table,
  DataGrid,
  StatTile,
  Tag,
  Badge,
  StatusIndicator,
  Avatar,
  AnchorBadge,
  Timeline,
  DescriptionList,
  Accordion,
  Card,
  PageHeader,
  Typography,
  Divider,
  List,
  ListItem,
  Clamp,
  TreeView,
  OrgChart,
  Carousel,
  BarChart,
  LineChart,
  DonutChart,
  AmountInput,
  ClabeInput,
  OtpInput,
  MaskedValue,
  CatDisclosure,
  Field,
  CommandPalette
} = NS;
const TmImage = NS.Image;
const {
  Spec,
  Section,
  Ic,
  ICONS,
  mxn
} = window.SC_A;
const R = window.__resources || {};

/* ---------- Navegación ---------- */
function SecNav() {
  const [tab, setTab] = React.useState("resumen");
  const [page, setPage] = React.useState(3);
  const [bnav, setBnav] = React.useState("inicio");
  return /*#__PURE__*/React.createElement(Section, {
    id: "navegacion",
    title: "Navegaci\xF3n",
    lead: "Tabs para contenido par en la p\xE1gina; SideNav/AppBar para rutas; el CommandPalette (\u2318K) nunca es el \xFAnico camino."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Breadcrumbs \xB7 Stepper"
  }, /*#__PURE__*/React.createElement(Breadcrumbs, {
    items: [{
      label: "Inicio",
      href: "#"
    }, {
      label: "Créditos",
      href: "#"
    }, {
      label: "TM-2026-04471"
    }]
  }), /*#__PURE__*/React.createElement(Stepper, {
    current: 2,
    steps: [{
      label: "Datos"
    }, {
      label: "Documentos"
    }, {
      label: "Revisión"
    }, {
      label: "Firma"
    }]
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "Tabs"
  }, /*#__PURE__*/React.createElement(Tabs, {
    value: tab,
    onChange: setTab,
    items: [{
      id: "resumen",
      label: "Resumen"
    }, {
      id: "actividad",
      label: "Actividad",
      count: 4
    }, {
      id: "documentos",
      label: "Documentos"
    }],
    style: {
      width: "100%"
    }
  }, /*#__PURE__*/React.createElement(Typography, {
    variant: "sm",
    color: "muted"
  }, tab === "resumen" ? "Panel de resumen del crédito." : tab === "actividad" ? "Bitácora de eventos." : "CFDI, contrato y comprobantes."))), /*#__PURE__*/React.createElement(Spec, {
    title: "Menu \xB7 Pagination \xB7 Link"
  }, /*#__PURE__*/React.createElement(Menu, {
    trigger: /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm"
    }, "Acciones"),
    items: [{
      label: "Descargar estado de cuenta"
    }, {
      label: "Reenviar contrato"
    }, {
      divider: true
    }, {
      label: "Cancelar crédito",
      destructive: true
    }]
  }), /*#__PURE__*/React.createElement(Pagination, {
    page: page,
    pageSize: 25,
    total: 3412,
    onPageChange: setPage
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-md)"
    }
  }, "Consulta el ", /*#__PURE__*/React.createElement(Link, {
    href: "#"
  }, "contrato"), " o el ", /*#__PURE__*/React.createElement(Link, {
    href: "https://www.condusef.gob.mx",
    external: true
  }, "portal CONDUSEF"), ".")), /*#__PURE__*/React.createElement(Spec, {
    title: "BottomNav \u2014 m\xF3vil, 3\u20135 destinos con etiqueta"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 330,
      border: "1px solid var(--color-border-default)",
      borderRadius: 16,
      overflow: "hidden",
      background: "var(--color-bg-surface)"
    }
  }, /*#__PURE__*/React.createElement(BottomNav, {
    activeId: bnav,
    onSelect: setBnav,
    items: [{
      id: "inicio",
      label: "Inicio",
      icon: /*#__PURE__*/React.createElement(Ic, {
        d: ICONS.home,
        s: 19
      })
    }, {
      id: "credito",
      label: "Mi crédito",
      icon: /*#__PURE__*/React.createElement(Ic, {
        d: ICONS.money,
        s: 19
      })
    }, {
      id: "pagos",
      label: "Pagos",
      icon: /*#__PURE__*/React.createElement(Ic, {
        d: ICONS.doc,
        s: 19
      }),
      count: 1
    }]
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "MegaMenu \u2014 nav de marketing"
  }, /*#__PURE__*/React.createElement("nav", {
    "data-surface": "marketing",
    style: {
      position: "relative",
      width: "100%",
      background: "var(--tm-navy-900)",
      color: "#fff",
      borderRadius: 10,
      padding: "0 16px",
      display: "flex",
      alignItems: "center",
      gap: 8,
      height: 50
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: R.logoTm || "assets/logo/tm-logo-horizontal-blanco-rojo.svg",
    alt: "Tanda Miranda",
    style: {
      height: 20,
      marginRight: 10
    }
  }), /*#__PURE__*/React.createElement(MegaMenu, {
    label: "Cr\xE9ditos",
    columns: [{
      header: "Para ti",
      links: [{
        label: "Crédito de nómina",
        description: "Descuento quincenal"
      }, {
        label: "Crédito de auto"
      }]
    }, {
      header: "Recursos",
      links: [{
        label: "Simulador"
      }, {
        label: "Preguntas frecuentes"
      }]
    }]
  }))));
}

/* ---------- Retroalimentación ---------- */
function SecFeedback() {
  const [toast, setToast] = React.useState(true);
  return /*#__PURE__*/React.createElement(Section, {
    id: "feedback",
    title: "Retroalimentaci\xF3n",
    lead: "El estado nunca viaja s\xF3lo en color: icono + etiqueta siempre (el rojo de marca tambi\xE9n es el rojo de peligro)."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Alert \u2014 severidades",
    cols: 2
  }, /*#__PURE__*/React.createElement(Alert, {
    severity: "info",
    title: "Mantenimiento programado"
  }, "SPEI no disponible el domingo 02:00\u201304:00 CDMX."), /*#__PURE__*/React.createElement(Alert, {
    severity: "success",
    title: "Pago aplicado",
    onDismiss: () => {}
  }, "Tu pago de ", mxn(3450), " se aplic\xF3 al cr\xE9dito."), /*#__PURE__*/React.createElement(Alert, {
    severity: "warning",
    title: "Documento por vencer"
  }, "Tu comprobante vence en 5 d\xEDas."), /*#__PURE__*/React.createElement(Alert, {
    severity: "danger",
    title: "No pudimos procesar el pago",
    action: /*#__PURE__*/React.createElement(Button, {
      size: "sm",
      variant: "secondary"
    }, "Reintentar")
  }, "Verifica los datos e int\xE9ntalo de nuevo.")), /*#__PURE__*/React.createElement(Spec, {
    title: "Toast \xB7 Spinner \xB7 Skeleton"
  }, toast && /*#__PURE__*/React.createElement(Toast, {
    severity: "success",
    action: "Deshacer",
    onDismiss: () => setToast(false)
  }, "Documento archivado"), /*#__PURE__*/React.createElement(Spinner, {
    delay: 0
  }), /*#__PURE__*/React.createElement(Skeleton, {
    variant: "circle",
    width: 34
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      width: 190
    }
  }, /*#__PURE__*/React.createElement(Skeleton, {
    lines: 2
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "Progreso \u2014 barra y anillo"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 260
    }
  }, /*#__PURE__*/React.createElement(ProgressBar, {
    label: "Generando estado de cuenta",
    value: 64
  })), /*#__PURE__*/React.createElement(ProgressRing, {
    value: 75,
    label: "Expediente al 75%"
  }), /*#__PURE__*/React.createElement(ProgressRing, {
    value: 9,
    max: 12,
    tone: "success",
    size: 62,
    format: (v, m) => `${v}/${m}`,
    label: "9 de 12 documentos"
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "EmptyState \xB7 Backdrop contenido",
    cols: 2
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px dashed var(--color-border-default)",
      borderRadius: 14
    }
  }, /*#__PURE__*/React.createElement(EmptyState, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.search,
      s: 22
    }),
    title: "Sin resultados para \xABG\xF3mez\xBB",
    action: /*#__PURE__*/React.createElement(Button, {
      size: "sm",
      variant: "secondary"
    }, "Limpiar filtros"),
    style: {
      padding: "22px 16px"
    }
  }, "Ning\xFAn cr\xE9dito coincide. Ajusta el nombre o el rango de fechas.")), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      borderRadius: 14,
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement(Card, {
    title: "Generando reporte"
  }, /*#__PURE__*/React.createElement(Typography, {
    variant: "sm",
    color: "muted"
  }, "Cartera completa \xB7 jun 2026")), /*#__PURE__*/React.createElement(Backdrop, {
    open: true,
    contained: true
  }, /*#__PURE__*/React.createElement(Spinner, {
    delay: 0,
    inverse: true
  }), "Generando reporte\u2026"))));
}

/* ---------- Superposiciones ---------- */
function SecOverlays() {
  const [modal, setModal] = React.useState(false);
  const [drawer, setDrawer] = React.useState(false);
  const [lb, setLb] = React.useState(false);
  const [lbIdx, setLbIdx] = React.useState(0);
  const [tour, setTour] = React.useState(false);
  const [palette, setPalette] = React.useState(false);
  const FOTOS = [{
    src: R.photoDreamer || "assets/photography/dreamer-night-sky.jpg",
    alt: "Aspiracional",
    caption: "Aspiracional"
  }, {
    src: R.photoTeamwork || "assets/photography/teamwork-workers.jpg",
    alt: "Personas",
    caption: "Personas"
  }];
  return /*#__PURE__*/React.createElement(Section, {
    id: "overlays",
    title: "Superposiciones",
    lead: "Foco atrapado y devuelto al invocador; Esc cierra la capa superior; las destructivas piden confirmaci\xF3n con verbos espec\xEDficos."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "Lanzadores"
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => setModal(true)
  }, "Modal de confirmaci\xF3n"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => setDrawer(true)
  }, "Drawer"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => {
      setLbIdx(0);
      setLb(true);
    }
  }, "Lightbox"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    id: "sc-tour-target",
    onClick: () => setTour(true)
  }, "Coachmark"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    onClick: () => setPalette(true)
  }, "CommandPalette \u2318K"), /*#__PURE__*/React.createElement(Tooltip, {
    content: "Ayuda sobre pagos"
  }, /*#__PURE__*/React.createElement(IconButton, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.bell
    }),
    label: "Ayuda",
    variant: "outline"
  })), /*#__PURE__*/React.createElement(Popover, {
    trigger: /*#__PURE__*/React.createElement(Button, {
      variant: "tertiary"
    }, "\xBFQu\xE9 es el CAT?"),
    width: 280
  }, /*#__PURE__*/React.createElement("strong", null, "CAT"), " \u2014 Costo Anual Total: tasa, comisiones y seguros.")), /*#__PURE__*/React.createElement(Modal, {
    open: modal,
    onClose: () => setModal(false),
    role: "alertdialog",
    size: "sm",
    title: "\xBFCancelar el cr\xE9dito TM-2026-04471?",
    footer: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      "data-autofocus": true,
      onClick: () => setModal(false)
    }, "Conservar cr\xE9dito"), /*#__PURE__*/React.createElement(Button, {
      variant: "destructive",
      onClick: () => setModal(false)
    }, "Cancelar cr\xE9dito"))
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      marginTop: 0
    }
  }, "Se notificar\xE1 al cliente. ", /*#__PURE__*/React.createElement("strong", null, "Esta acci\xF3n no se puede deshacer."))), /*#__PURE__*/React.createElement(Drawer, {
    open: drawer,
    onClose: () => setDrawer(false),
    title: "Cr\xE9dito TM-2026-04471",
    footer: /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      onClick: () => setDrawer(false)
    }, "Cerrar")
  }, /*#__PURE__*/React.createElement(DescriptionList, {
    items: [{
      term: "Cliente",
      detail: "María Fernanda López"
    }, {
      term: "CLABE",
      detail: /*#__PURE__*/React.createElement(MaskedValue, {
        value: "012180012345678912",
        label: "CLABE"
      })
    }, {
      term: "Saldo",
      detail: mxn(62340)
    }]
  })), /*#__PURE__*/React.createElement(Lightbox, {
    open: lb,
    images: FOTOS,
    index: lbIdx,
    onClose: () => setLb(false),
    onIndexChange: setLbIdx
  }), /*#__PURE__*/React.createElement(Coachmark, {
    open: tour,
    onClose: () => setTour(false),
    steps: [{
      target: "#sc-tour-target",
      title: "Recorridos de primera vez",
      content: "Spotlight + tarjeta con contador; siempre se puede cerrar y reabrir desde Ayuda."
    }]
  }), /*#__PURE__*/React.createElement(CommandPalette, {
    open: palette,
    onClose: () => setPalette(false),
    onOpen: () => setPalette(true),
    groups: [{
      header: "Acciones",
      items: [{
        id: "pago",
        label: "Registrar pago",
        hint: "P"
      }, {
        id: "sol",
        label: "Crear solicitud de crédito"
      }]
    }, {
      header: "Navegación",
      items: [{
        id: "cred",
        label: "Ir a Créditos",
        hint: "G C"
      }]
    }]
  }));
}

/* ---------- Datos ---------- */
function SecData() {
  const [sel, setSel] = React.useState(["TM-04471"]);
  const [tree, setTree] = React.useState("ine");
  const rows = [{
    folio: "TM-04471",
    cliente: "María F. López",
    saldo: 62340,
    estado: "ok"
  }, {
    folio: "TM-04456",
    cliente: "Jorge Anaya",
    saldo: 118200,
    estado: "rev"
  }, {
    folio: "TM-04392",
    cliente: "Lucía Serna",
    saldo: 8420,
    estado: "ven"
  }];
  const EST = {
    ok: ["success", "Al corriente"],
    rev: ["pending", "En revisión"],
    ven: ["danger", "Vencido"]
  };
  return /*#__PURE__*/React.createElement(Section, {
    id: "datos",
    title: "Datos",
    lead: "Figuras tabulares alineadas a la derecha, c\xF3digo ISO cuando puede aparecer m\xE1s de una divisa, y sello \xABal dd/mm/aaaa\xBB en cada cifra."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "StatTile \xB7 m\xE9tricas"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(3, minmax(0, 1fr))",
      gap: 12,
      width: "100%"
    }
  }, /*#__PURE__*/React.createElement(StatTile, {
    label: "Cartera activa",
    value: mxn(4823400),
    delta: {
      value: "3.2%",
      direction: "up",
      vs: "mes anterior"
    }
  }), /*#__PURE__*/React.createElement(StatTile, {
    label: "Cartera vencida",
    value: mxn(112050),
    delta: {
      value: "0.4%",
      direction: "up",
      positive: false,
      vs: "mes anterior"
    }
  }), /*#__PURE__*/React.createElement(StatTile, {
    label: "Pagos hoy",
    value: "47",
    caption: "al 14 jul 2026, 09:12 CDMX"
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "Table \u2014 selecci\xF3n tri-estado, orden, densidad"
  }, /*#__PURE__*/React.createElement(Card, {
    padding: 0,
    style: {
      width: "100%"
    }
  }, /*#__PURE__*/React.createElement(Table, {
    caption: "Cr\xE9ditos activos",
    selectable: true,
    selected: sel,
    onSelectionChange: setSel,
    rowKey: r => r.folio,
    columns: [{
      key: "folio",
      header: "Folio",
      render: r => /*#__PURE__*/React.createElement("span", {
        style: {
          font: "var(--text-code-sm)"
        }
      }, r.folio),
      width: 110
    }, {
      key: "cliente",
      header: "Cliente"
    }, {
      key: "estado",
      header: "Estado",
      render: r => /*#__PURE__*/React.createElement(StatusIndicator, {
        status: EST[r.estado][0],
        label: EST[r.estado][1],
        size: "sm"
      })
    }, {
      key: "saldo",
      header: "Saldo",
      numeric: true,
      render: r => mxn(r.saldo)
    }],
    rows: rows
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "Charts \u2014 tokens --chart-*",
    cols: 3
  }, /*#__PURE__*/React.createElement(BarChart, {
    height: 130,
    formatValue: v => new Intl.NumberFormat("es-MX", {
      notation: "compact"
    }).format(v),
    data: [{
      label: "Abr",
      value: 38200
    }, {
      label: "May",
      value: 51400
    }, {
      label: "Jun",
      value: 47900
    }]
  }), /*#__PURE__*/React.createElement(LineChart, {
    height: 130,
    color: "var(--chart-negative)",
    formatValue: v => v.toFixed(1) + "%",
    data: [{
      label: "Abr",
      value: 3.4
    }, {
      label: "May",
      value: 2.6
    }, {
      label: "Jun",
      value: 2.3
    }]
  }), /*#__PURE__*/React.createElement(DonutChart, {
    size: 120,
    centerLabel: "834",
    data: [{
      label: "Nómina",
      value: 512
    }, {
      label: "Auto",
      value: 186
    }, {
      label: "Personal",
      value: 136
    }]
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "Timeline \xB7 TreeView \xB7 OrgChart",
    cols: 3
  }, /*#__PURE__*/React.createElement(Card, {
    title: "Actividad"
  }, /*#__PURE__*/React.createElement(Timeline, {
    items: [{
      time: "hace 2 h",
      actor: "K. Ríos",
      action: "aprobó el desembolso de",
      object: mxn(85000),
      tone: "success"
    }, {
      time: "ayer",
      actor: "Sistema",
      action: "validó la CLABE",
      tone: "info"
    }]
  })), /*#__PURE__*/React.createElement(Card, {
    title: "Expediente"
  }, /*#__PURE__*/React.createElement(TreeView, {
    label: "Expediente",
    selectedId: tree,
    onSelect: setTree,
    defaultExpanded: ["id"],
    items: [{
      id: "id",
      label: "Identidad",
      children: [{
        id: "ine",
        label: "INE",
        count: 2
      }, {
        id: "curp",
        label: "CURP"
      }]
    }, {
      id: "ing",
      label: "Ingresos",
      children: [{
        id: "nom",
        label: "Recibos de nómina",
        count: 3
      }]
    }]
  })), /*#__PURE__*/React.createElement(Card, {
    title: "Equipo"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      maxWidth: "100%"
    }
  }, /*#__PURE__*/React.createElement(OrgChart, {
    label: "K. R\xEDos, Cr\xE9dito, con dos analistas",
    data: {
      name: "K. Ríos",
      role: "Crédito",
      children: [{
        name: "J. Duarte",
        role: "Analista"
      }, {
        name: "A. Vega",
        role: "Analista"
      }]
    }
  })))), /*#__PURE__*/React.createElement(Spec, {
    title: "List \xB7 Accordion \xB7 Carousel",
    cols: 3
  }, /*#__PURE__*/React.createElement(List, {
    inset: true
  }, /*#__PURE__*/React.createElement(ListItem, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.doc
    }),
    primary: "Estado de cuenta \u2014 junio",
    secondary: "PDF \xB7 240 KB",
    meta: "01/07/2026",
    onClick: () => {}
  }), /*#__PURE__*/React.createElement(ListItem, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.money
    }),
    primary: "Pago quincena 15",
    secondary: "Aplicado",
    meta: mxn(3450),
    selected: true,
    onClick: () => {}
  })), /*#__PURE__*/React.createElement(Accordion, {
    defaultOpen: ["cat"],
    items: [{
      id: "cat",
      title: "Costo total",
      content: "CAT promedio 32.4% sin IVA. Informativo."
    }, {
      id: "req",
      title: "Requisitos",
      content: "Identificación, comprobante de nómina y CLABE."
    }]
  }), /*#__PURE__*/React.createElement(Carousel, {
    label: "Fotograf\xEDa",
    height: 120
  }, /*#__PURE__*/React.createElement(TmImage, {
    src: R.photoHands || "assets/photography/hands-car-protection.jpg",
    alt: "Producto",
    ratio: "auto",
    radius: "none",
    style: {
      height: "100%",
      aspectRatio: "auto"
    }
  }), /*#__PURE__*/React.createElement(TmImage, {
    src: R.photoOffice || "assets/photography/office-meeting.jpg",
    alt: "Personas",
    ratio: "auto",
    radius: "none",
    style: {
      height: "100%",
      aspectRatio: "auto"
    }
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "Etiquetas y estado"
  }, /*#__PURE__*/React.createElement(Badge, {
    tone: "navy"
  }, "234 activos"), /*#__PURE__*/React.createElement(Tag, {
    tone: "brand"
  }, "N\xF3mina"), /*#__PURE__*/React.createElement(Tag, {
    tone: "success"
  }, "Aprobado"), /*#__PURE__*/React.createElement(Tag, {
    onRemove: () => {}
  }, "Sucursal: Hermosillo"), /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "success",
    label: "Al corriente"
  }), /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "danger",
    label: "Vencido"
  }), /*#__PURE__*/React.createElement(Avatar, {
    name: "Mar\xEDa Fernanda L\xF3pez",
    size: 32
  }), /*#__PURE__*/React.createElement(AnchorBadge, {
    count: 3,
    srLabel: "3 notificaciones sin leer"
  }, /*#__PURE__*/React.createElement(IconButton, {
    icon: /*#__PURE__*/React.createElement(Ic, {
      d: ICONS.bell
    }),
    label: "Notificaciones",
    variant: "outline"
  }))));
}

/* ---------- Dominio fintech ---------- */
function SecDomain() {
  const [monto, setMonto] = React.useState(85000);
  const [clabe, setClabe] = React.useState("012180012345678912");
  const [otp, setOtp] = React.useState("48");
  return /*#__PURE__*/React.createElement(Section, {
    id: "dominio",
    title: "Dominio fintech",
    lead: "Dinero sin UI optimista: revisi\xF3n \u2192 c\xF3digo \u2192 recibo con folio. Identificadores en mono, enmascarados por defecto; revelar es un evento auditable."
  }, /*#__PURE__*/React.createElement(Spec, {
    title: "AmountInput \xB7 ClabeInput",
    cols: 2
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Monto a transferir",
    help: "M\xEDnimo MXN 500.00."
  }, /*#__PURE__*/React.createElement(AmountInput, {
    value: monto,
    onChange: setMonto,
    fullWidth: true
  })), /*#__PURE__*/React.createElement(Field, {
    label: "CLABE del beneficiario",
    help: "18 d\xEDgitos; el banco se identifica solo."
  }, /*#__PURE__*/React.createElement(ClabeInput, {
    value: clabe,
    onChange: setClabe,
    fullWidth: true
  }))), /*#__PURE__*/React.createElement(Spec, {
    title: "OtpInput \xB7 MaskedValue"
  }, /*#__PURE__*/React.createElement(OtpInput, {
    value: otp,
    onChange: setOtp
  }), /*#__PURE__*/React.createElement(MaskedValue, {
    value: "5204165912344587",
    label: "tarjeta"
  })), /*#__PURE__*/React.createElement(Spec, {
    title: "CatDisclosure \u2014 divulgaci\xF3n versionada"
  }, /*#__PURE__*/React.createElement(CatDisclosure, {
    value: "32.4%",
    date: "14 de julio de 2026",
    version: "2026.2",
    style: {
      maxWidth: 560
    }
  }, "Cr\xE9dito de n\xF3mina. Tasa fija anual 24.9%. Comisi\xF3n por apertura 2%.")));
}
window.SC_B = {
  SecNav,
  SecFeedback,
  SecOverlays,
  SecData,
  SecDomain
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "showcase/sections-b.jsx", error: String((e && e.message) || e) }); }

// ui_kits/app/Dashboard.jsx
try { (() => {
const {
  AppBar,
  SideNav,
  PageHeader,
  StatTile,
  Card,
  Table,
  Timeline,
  Button,
  IconButton,
  Switch,
  StatusIndicator,
  Tag,
  Avatar,
  MaskedValue,
  CatDisclosure,
  ProgressBar,
  DescriptionList,
  Alert,
  SegmentedControl,
  Modal,
  AmountInput,
  OtpInput,
  Badge
} = window.TandaMirandaDesignSystem_a4a6f5;
const {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRadio,
  TweakSelect,
  TweakToggle
} = window;
const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
  "tema": "light",
  "logoOscuro": "color",
  "privacidad": false,
  "vaquita": true
} /*EDITMODE-END*/;

/* In dark mode the AppBar surface goes light (bg-inverse re-maps), so the
   white wordmark loses contrast — swap to a dark-ink lockup there. */
const LOGOS = {
  "blanco-rojo": "tm-logo-horizontal-blanco-rojo.svg",
  "color": "tm-logo-horizontal-color.svg",
  "azul": "tm-logo-horizontal-azul.svg"
};
const mxn = n => "MXN " + new Intl.NumberFormat("es-MX", {
  minimumFractionDigits: 2
}).format(n);
const Icon = ({
  d,
  s = 17
}) => /*#__PURE__*/React.createElement("svg", {
  width: s,
  height: s,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "1.8",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}, /*#__PURE__*/React.createElement("path", {
  d: d
}));
const I = {
  bell: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9M10.3 21a1.94 1.94 0 0 0 3.4 0",
  home: "m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z",
  credit: "M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6",
  doc: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8zM14 2v6h6",
  help: "M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3M12 17h.01M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z",
  cal: "M8 2v4M16 2v4M3 10h18M5 4h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2Z",
  down: "M12 5v14M19 12l-7 7-7-7"
};
const SCHEDULE = [{
  n: 14,
  fecha: "15/06/2026",
  capital: 2637.6,
  interes: 812.4,
  pago: 3450,
  estado: "pagado"
}, {
  n: 15,
  fecha: "30/06/2026",
  capital: 2664.0,
  interes: 786.0,
  pago: 3450,
  estado: "pagado"
}, {
  n: 16,
  fecha: "15/07/2026",
  capital: 2690.7,
  interes: 759.3,
  pago: 3450,
  estado: "proximo"
}, {
  n: 17,
  fecha: "31/07/2026",
  capital: 2717.6,
  interes: 732.4,
  pago: 3450,
  estado: "futuro"
}, {
  n: 18,
  fecha: "15/08/2026",
  capital: 2744.8,
  interes: 705.2,
  pago: 3450,
  estado: "futuro"
}];
const ESTADO = s => ({
  pagado: /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "success",
    label: "Pagado",
    size: "sm"
  }),
  proximo: /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "pending",
    label: "Pr\xF3ximo",
    size: "sm"
  }),
  futuro: /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "neutral",
    label: "Programado",
    size: "sm"
  })
})[s];
function PayFlow({
  open,
  onClose
}) {
  const [step, setStep] = React.useState(0);
  const [monto, setMonto] = React.useState(3450);
  const [otp, setOtp] = React.useState("");
  React.useEffect(() => {
    if (open) {
      setStep(0);
      setOtp("");
      setMonto(3450);
    }
  }, [open]);
  return /*#__PURE__*/React.createElement(Modal, {
    open: open,
    onClose: onClose,
    size: "sm",
    title: step === 0 ? "Pagar quincena 16" : step === 1 ? "Confirma tu pago" : "Pago aplicado",
    footer: step === 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: onClose
    }, "Cancelar"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      onClick: () => setStep(1)
    }, "Revisar pago")) : step === 1 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: () => setStep(0)
    }, "Regresar"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      disabled: otp.length < 6,
      onClick: () => setStep(2)
    }, "Confirmar pago")) : /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      onClick: onClose
    }, "Listo")
  }, step === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 14,
      paddingBottom: 4
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "600 13px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)",
      marginBottom: 6
    }
  }, "Monto"), /*#__PURE__*/React.createElement(AmountInput, {
    value: monto,
    onChange: setMonto,
    fullWidth: true
  })), /*#__PURE__*/React.createElement(DescriptionList, {
    items: [{
      term: "Cargo a",
      detail: "BBVA México •••• 4587"
    }, {
      term: "Fecha límite",
      detail: "15/07/2026"
    }],
    columns: 2
  })), step === 1 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 14,
      paddingBottom: 4
    }
  }, /*#__PURE__*/React.createElement(DescriptionList, {
    columns: 2,
    items: [{
      term: "Monto",
      detail: mxn(monto || 0)
    }, {
      term: "Comisión",
      detail: "MXN 0.00"
    }, {
      term: "Total",
      detail: mxn(monto || 0)
    }, {
      term: "Aplicación",
      detail: "Hoy, antes de 18:00 CDMX"
    }]
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "600 13px/1.4 var(--font-sans)",
      color: "var(--color-fg-default)",
      marginBottom: 6
    }
  }, "C\xF3digo enviado al \u2022\u2022\u2022\u2022 1123"), /*#__PURE__*/React.createElement(OtpInput, {
    value: otp,
    onChange: setOtp
  }))), step === 2 && /*#__PURE__*/React.createElement("div", {
    style: {
      paddingBottom: 4
    }
  }, /*#__PURE__*/React.createElement(Alert, {
    severity: "success",
    title: "Transferencia enviada"
  }, "Folio ", /*#__PURE__*/React.createElement("span", {
    style: {
      font: "var(--text-code-sm)"
    }
  }, "TM-P-88214"), " \xB7 Recibo CEP disponible en Documentos.")));
}
function Dashboard() {
  const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
  const theme = t.tema;
  const [nav, setNav] = React.useState("inicio");
  const [pay, setPay] = React.useState(false);
  const [domiciliado, setDomiciliado] = React.useState(true);
  React.useEffect(() => {
    document.documentElement.setAttribute("data-theme", theme);
  }, [theme]);
  const money = n => t.privacidad ? "MXN ••••••" : mxn(n);
  const logoSrc = `../../assets/logo/${theme === "dark" ? LOGOS[t.logoOscuro] || LOGOS.color : LOGOS["blanco-rojo"]}`;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100vh",
      background: "var(--color-bg-subtle)",
      fontFamily: "var(--font-sans)"
    }
  }, /*#__PURE__*/React.createElement(AppBar, {
    sticky: true,
    brand: /*#__PURE__*/React.createElement("img", {
      src: logoSrc,
      alt: "Tanda Miranda",
      style: {
        height: 28
      }
    }),
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(SegmentedControl, {
      label: "Tema",
      value: theme,
      onChange: v => setTweak("tema", v),
      options: [{
        value: "light",
        label: "Claro"
      }, {
        value: "dark",
        label: "Oscuro"
      }],
      style: {
        background: "var(--tm-alpha-white-200)"
      }
    }), /*#__PURE__*/React.createElement(IconButton, {
      icon: /*#__PURE__*/React.createElement(Icon, {
        d: I.bell
      }),
      label: "Notificaciones",
      variant: "inverse"
    })),
    user: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Avatar, {
      name: "Mar\xEDa Fernanda L\xF3pez",
      size: 30
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        font: "600 13px/1.2 var(--font-sans)"
      }
    }, "Mar\xEDa F."))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "stretch"
    }
  }, /*#__PURE__*/React.createElement(SideNav, {
    activeId: nav,
    onSelect: setNav,
    style: {
      minHeight: "calc(100vh - 60px)"
    },
    sections: [{
      items: [{
        id: "inicio",
        label: "Inicio",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.home
        })
      }, {
        id: "credito",
        label: "Mi crédito",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.credit
        })
      }, {
        id: "documentos",
        label: "Documentos",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.doc
        }),
        count: 2
      }, {
        id: "ayuda",
        label: "Ayuda",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.help
        })
      }]
    }],
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        padding: "var(--space-3)",
        font: "var(--text-xs)",
        color: "var(--color-fg-subtle)"
      }
    }, "UNE \xB7 CONDUSEF 55 5340 0999")
  }), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      minWidth: 0,
      padding: "28px 32px 40px",
      maxWidth: 1180,
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/React.createElement(PageHeader, {
    title: "Hola, Mar\xEDa",
    status: /*#__PURE__*/React.createElement(StatusIndicator, {
      status: "success",
      label: "Al corriente"
    }),
    meta: /*#__PURE__*/React.createElement(React.Fragment, null, "Cr\xE9dito de n\xF3mina ", /*#__PURE__*/React.createElement("span", {
      style: {
        font: "var(--text-code-sm)"
      }
    }, "TM-2026-04471"), " \xB7 Tanda Miranda SOFOM"),
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      iconStart: /*#__PURE__*/React.createElement(Icon, {
        d: I.doc,
        s: 15
      })
    }, "Estado de cuenta"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      onClick: () => setPay(true)
    }, "Pagar quincena"))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
      gap: 14,
      marginTop: 22
    }
  }, /*#__PURE__*/React.createElement(StatTile, {
    label: "Saldo por pagar",
    value: money(62340),
    caption: "al 10 jul 2026, 09:12 CDMX"
  }), /*#__PURE__*/React.createElement(StatTile, {
    label: "Pr\xF3ximo pago",
    value: money(3450),
    caption: "vence 15/07/2026 \xB7 quincena 16 de 24",
    icon: /*#__PURE__*/React.createElement(Icon, {
      d: I.cal,
      s: 15
    })
  }), /*#__PURE__*/React.createElement(StatTile, {
    label: "Pagado a la fecha",
    value: money(51750),
    delta: {
      value: "3 pagos adelantados",
      direction: "up",
      vs: "plan original"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1.55fr 1fr",
      gap: 14,
      marginTop: 14,
      alignItems: "start"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 14,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(Card, {
    title: "Avance del cr\xE9dito",
    meta: "15 de 24 quincenas pagadas"
  }, /*#__PURE__*/React.createElement(ProgressBar, {
    value: 15,
    max: 24,
    showValue: false,
    tone: "brand",
    size: "lg"
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      marginTop: 8,
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, /*#__PURE__*/React.createElement("span", null, "Inicio 15/12/2025"), /*#__PURE__*/React.createElement("span", null, "62.5% completado"), /*#__PURE__*/React.createElement("span", null, "Fin 30/11/2026"))), /*#__PURE__*/React.createElement(Card, {
    title: "Tabla de amortizaci\xF3n",
    meta: "Montos en MXN \xB7 IVA incluido",
    padding: 0,
    actions: /*#__PURE__*/React.createElement(Button, {
      variant: "tertiary",
      size: "sm",
      iconStart: /*#__PURE__*/React.createElement(Icon, {
        d: I.down,
        s: 14
      })
    }, "Exportar")
  }, /*#__PURE__*/React.createElement(Table, {
    caption: "Tabla de amortizaci\xF3n del cr\xE9dito TM-2026-04471",
    columns: [{
      key: "n",
      header: "No.",
      numeric: true,
      width: 56
    }, {
      key: "fecha",
      header: "Fecha",
      align: "left"
    }, {
      key: "capital",
      header: "Capital",
      numeric: true,
      render: r => money(r.capital)
    }, {
      key: "interes",
      header: "Interés",
      numeric: true,
      render: r => money(r.interes)
    }, {
      key: "pago",
      header: "Pago",
      numeric: true,
      render: r => money(r.pago)
    }, {
      key: "estado",
      header: "Estado",
      render: r => ESTADO(r.estado)
    }],
    rows: SCHEDULE,
    rowKey: r => r.n
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "10px 16px",
      font: "var(--text-xs)",
      color: "var(--color-fg-subtle)",
      borderTop: "1px solid var(--color-border-subtle)"
    }
  }, "Mostrando quincenas 14\u201318 de 24 \xB7 Los totales concilian con tu contrato."))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 14,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(Card, {
    title: "Cuenta de cargo"
  }, /*#__PURE__*/React.createElement(DescriptionList, {
    items: [{
      term: "Banco",
      detail: "BBVA México"
    }, {
      term: "Tarjeta",
      detail: /*#__PURE__*/React.createElement(MaskedValue, {
        value: "5204165912344587",
        label: "tarjeta"
      })
    }]
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement(Switch, {
    label: "Pago domiciliado",
    description: "Se cobra cada quincena autom\xE1ticamente",
    checked: domiciliado,
    onChange: setDomiciliado
  }))), /*#__PURE__*/React.createElement(Card, {
    title: "Actividad reciente"
  }, /*#__PURE__*/React.createElement(Timeline, {
    items: [{
      time: "30/06/2026",
      actor: "Pago",
      action: "aplicado por",
      object: money(3450),
      tone: "success",
      timeExact: "30 jun 2026, 12:02 CDMX"
    }, {
      time: "15/06/2026",
      actor: "Pago",
      action: "aplicado por",
      object: money(3450),
      tone: "success"
    }, {
      time: "12/06/2026",
      actor: "Tú",
      action: "actualizaste tu comprobante de domicilio",
      tone: "info"
    }]
  }), /*#__PURE__*/React.createElement(Button, {
    variant: "link",
    size: "sm",
    style: {
      marginTop: 6
    }
  }, "Ver toda la actividad")), t.vaquita && /*#__PURE__*/React.createElement(Card, null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 14,
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/illustrations/tm-vaca-color.svg",
    alt: "",
    style: {
      width: 64,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "italic 400 17px/1.35 var(--font-serif-display)",
      color: "var(--color-fg-default)"
    }
  }, "\xA1Vas muy bien!"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-xs)",
      color: "var(--color-fg-muted)",
      marginTop: 4
    }
  }, "Adelanta capital sin penalizaci\xF3n y termina antes tu tanda.")))), /*#__PURE__*/React.createElement(CatDisclosure, {
    value: "32.4%",
    date: "10 de julio de 2026",
    version: "2026.2"
  }, "Cr\xE9dito de n\xF3mina. Tasa fija anual 24.9%. Comisi\xF3n por apertura 2%."))))), /*#__PURE__*/React.createElement(PayFlow, {
    open: pay,
    onClose: () => setPay(false)
  }), /*#__PURE__*/React.createElement(TweaksPanel, null, /*#__PURE__*/React.createElement(TweakSection, {
    label: "Apariencia"
  }), /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Tema",
    value: t.tema,
    options: [{
      value: "light",
      label: "Claro"
    }, {
      value: "dark",
      label: "Oscuro"
    }],
    onChange: v => setTweak("tema", v)
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Logo en modo oscuro",
    value: t.logoOscuro,
    options: [{
      value: "color",
      label: "Color (recomendado)"
    }, {
      value: "azul",
      label: "Azul Tanda"
    }, {
      value: "blanco-rojo",
      label: "Blanco y rojo (original)"
    }],
    onChange: v => setTweak("logoOscuro", v)
  }), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Contenido"
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Ocultar saldos",
    value: t.privacidad,
    onChange: v => setTweak("privacidad", v)
  }), /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Consejo de la vaquita",
    value: t.vaquita,
    onChange: v => setTweak("vaquita", v)
  })));
}
ReactDOM.createRoot(document.getElementById("root")).render(/*#__PURE__*/React.createElement(Dashboard, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/app/Dashboard.jsx", error: String((e && e.message) || e) }); }

// ui_kits/app/tweaks-panel.jsx
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)
// Copied omelette starter. Re-running copy_starter_component with this kind overwrites this file with the latest version (page content is unaffected).

/* BEGIN USAGE */
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
// Exports (to window): useTweaks, TweaksPanel, TweakSection, TweakRow, TweakSlider,
//   TweakToggle, TweakRadio, TweakSelect, TweakText, TweakNumber, TweakColor, TweakButton.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// TweakRadio is the segmented control for 2–3 short options (auto-falls-back to
// TweakSelect past ~16/~10 chars per label); reach for TweakSelect directly when
// options are many or long. For color tweaks always curate 3-4 options rather than
// a free picker; an option can also be a whole 2–5 color palette (the stored value
// is the array). The Tweak* controls are a floor, not a ceiling — build custom
// controls inside the panel if a tweak calls for UI they don't cover.
/* END USAGE */
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-omelette-chrome": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children)));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/app/tweaks-panel.jsx", error: String((e && e.message) || e) }); }

// ui_kits/ops/OpsConsole.jsx
try { (() => {
const {
  AppBar,
  SideNav,
  PageHeader,
  Button,
  IconButton,
  Input,
  Select,
  Combobox,
  Tag,
  DataGrid,
  StatusIndicator,
  Drawer,
  Tabs,
  DescriptionList,
  Timeline,
  MaskedValue,
  Alert,
  Modal,
  CommandPalette,
  SegmentedControl,
  Avatar,
  Badge,
  Toast,
  ToastRegion
} = window.TandaMirandaDesignSystem_a4a6f5;
const mxn = n => "MXN " + new Intl.NumberFormat("es-MX", {
  minimumFractionDigits: 2
}).format(n);
const dias = n => `${n} ${n === 1 ? "día" : "días"}`;
const Icon = ({
  d,
  s = 17
}) => /*#__PURE__*/React.createElement("svg", {
  width: s,
  height: s,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "1.8",
  strokeLinecap: "round",
  strokeLinejoin: "round"
}, /*#__PURE__*/React.createElement("path", {
  d: d
}));
const I = {
  bell: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9M10.3 21a1.94 1.94 0 0 0 3.4 0",
  home: "m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z",
  credit: "M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6",
  users: "M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2M23 21v-2a4 4 0 0 0-3-3.87M16 3.13a4 4 0 0 1 0 7.75M13 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z",
  clock: "M12 8v4l3 3M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0Z",
  check: "M22 11.08V12a10 10 0 1 1-5.93-9.14M22 4 12 14.01l-3-3",
  file: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8zM14 2v6h6",
  chevR: "m9 18 6-6-6-6",
  search: "M11 19a8 8 0 1 0 0-16 8 8 0 0 0 0 16ZM21 21l-4.3-4.3",
  download: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M7 10l5 5 5-5M12 15V3"
};
const NOMBRES = ["María F. López", "Jorge Anaya", "Lucía Serna", "Carlos Duarte", "Ana P. Vega", "Raúl Cantú", "Elena Ríos", "Pedro Salas", "Sofía Beltrán", "Iván Zamora"];
const SUCURSALES = ["Hermosillo", "Chihuahua", "Monterrey", "Tijuana"];
const ROWS = Array.from({
  length: 834
}, (_, i) => ({
  folio: `TM-${String(10000 + i)}`,
  cliente: NOMBRES[i % NOMBRES.length],
  sucursal: SUCURSALES[i % 4],
  saldo: 4000 + i * 7919 % 180000,
  proximo: `${String(i % 28 + 1).padStart(2, "0")}/0${i % 2 + 7}/2026`,
  atraso: i % 13 === 0 ? i % 45 + 1 : 0
}));
function OpsConsole() {
  const [nav, setNav] = React.useState("creditos");
  const [density, setDensity] = React.useState("compact");
  const [q, setQ] = React.useState("");
  const [estado, setEstado] = React.useState("todos");
  const [sucursal, setSucursal] = React.useState(null);
  const [sort, setSort] = React.useState({
    key: "folio",
    dir: "asc"
  });
  const [selected, setSelected] = React.useState([]);
  const [openRow, setOpenRow] = React.useState(null);
  const [confirm, setConfirm] = React.useState(false);
  const [palette, setPalette] = React.useState(false);
  const [toast, setToast] = React.useState(null);
  const [tab, setTab] = React.useState("resumen");
  const filtered = React.useMemo(() => {
    let r = ROWS;
    if (q) {
      const s = q.toLowerCase();
      r = r.filter(x => x.folio.toLowerCase().includes(s) || x.cliente.toLowerCase().includes(s));
    }
    if (estado === "vencidos") r = r.filter(x => x.atraso > 0);
    if (estado === "corriente") r = r.filter(x => x.atraso === 0);
    if (sucursal) r = r.filter(x => x.sucursal === sucursal);
    return [...r].sort((a, b) => (a[sort.key] > b[sort.key] ? 1 : -1) * (sort.dir === "asc" ? 1 : -1));
  }, [q, estado, sucursal, sort]);
  const chips = [estado !== "todos" && {
    label: `Estado: ${estado === "vencidos" ? "Vencidos" : "Al corriente"}`,
    clear: () => setEstado("todos")
  }, sucursal && {
    label: `Sucursal: ${sucursal}`,
    clear: () => setSucursal(null)
  }, q && {
    label: `Búsqueda: "${q}"`,
    clear: () => setQ("")
  }].filter(Boolean);
  const showToast = msg => {
    setToast(msg);
    setTimeout(() => setToast(null), 5000);
  };
  const row = openRow ? ROWS.find(r => r.folio === openRow) : null;
  return /*#__PURE__*/React.createElement("div", {
    "data-density": density,
    style: {
      minHeight: "100vh",
      background: "var(--color-bg-subtle)",
      fontFamily: "var(--font-sans)"
    }
  }, /*#__PURE__*/React.createElement(AppBar, {
    sticky: true,
    brand: /*#__PURE__*/React.createElement("img", {
      src: "../../assets/logo/tm-logo-horizontal-blanco-rojo.svg",
      alt: "Tanda Miranda",
      style: {
        height: 26
      }
    }),
    title: "Consola de operaci\xF3n",
    env: "producci\xF3n",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "tertiary",
      size: "sm",
      onClick: () => setPalette(true),
      style: {
        color: "var(--color-fg-inverse-muted)"
      },
      iconStart: /*#__PURE__*/React.createElement(Icon, {
        d: I.search,
        s: 14
      })
    }, "Buscar", /*#__PURE__*/React.createElement("kbd", {
      style: {
        font: "600 10px/1 var(--font-mono)",
        background: "var(--tm-alpha-white-200)",
        padding: "3px 5px",
        borderRadius: 4,
        marginLeft: 4
      }
    }, "\u2318K")), /*#__PURE__*/React.createElement(IconButton, {
      icon: /*#__PURE__*/React.createElement(Icon, {
        d: I.bell
      }),
      label: "Notificaciones",
      variant: "inverse"
    })),
    user: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Avatar, {
      name: "Karla R\xEDos",
      size: 28
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        font: "600 12.5px/1.3 var(--font-sans)"
      }
    }, "Karla R\xEDos", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("span", {
      style: {
        font: "400 10.5px/1 var(--font-sans)",
        color: "var(--color-fg-inverse-muted)"
      }
    }, "Analista de cr\xE9dito")))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "stretch"
    }
  }, /*#__PURE__*/React.createElement(SideNav, {
    width: 232,
    activeId: nav,
    onSelect: setNav,
    style: {
      minHeight: "calc(100vh - 60px)"
    },
    sections: [{
      header: "Operación",
      items: [{
        id: "inicio",
        label: "Inicio",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.home,
          s: 16
        })
      }, {
        id: "creditos",
        label: "Créditos",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.credit,
          s: 16
        }),
        count: 834
      }, {
        id: "solicitudes",
        label: "Solicitudes",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.file,
          s: 16
        }),
        count: 12
      }, {
        id: "clientes",
        label: "Clientes",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.users,
          s: 16
        })
      }]
    }, {
      header: "Gestión",
      items: [{
        id: "cobranza",
        label: "Cobranza",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.clock,
          s: 16
        }),
        count: 64
      }, {
        id: "aprobaciones",
        label: "Aprobaciones",
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.check,
          s: 16
        }),
        count: 3
      }]
    }],
    footer: /*#__PURE__*/React.createElement("div", {
      style: {
        padding: "var(--space-2) var(--space-3)",
        display: "flex",
        flexDirection: "column",
        gap: 6
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        font: "700 10px/1.4 var(--font-sans)",
        letterSpacing: ".06em",
        textTransform: "uppercase",
        color: "var(--color-fg-subtle)"
      }
    }, "Densidad"), /*#__PURE__*/React.createElement(SegmentedControl, {
      label: "Densidad",
      value: density,
      onChange: setDensity,
      options: [{
        value: "compact",
        label: "Compacta"
      }, {
        value: "comfortable",
        label: "Cómoda"
      }]
    }))
  }), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      minWidth: 0,
      padding: "22px 26px 36px",
      boxSizing: "border-box"
    }
  }, /*#__PURE__*/React.createElement(PageHeader, {
    title: "Cr\xE9ditos",
    status: /*#__PURE__*/React.createElement(Badge, {
      tone: "navy"
    }, new Intl.NumberFormat("es-MX").format(filtered.length), " resultados"),
    meta: "Cartera activa \xB7 datos al 10 jul 2026, 09:12 CDMX",
    actions: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      size: "sm",
      iconStart: /*#__PURE__*/React.createElement(Icon, {
        d: I.download,
        s: 14
      }),
      onClick: () => showToast("Exportación iniciada · te avisamos al terminar")
    }, "Exportar"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      size: "sm"
    }, "Nueva solicitud"))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      marginTop: 16,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement(Input, {
    placeholder: "Folio o cliente\u2026",
    value: q,
    onChange: e => setQ(e.target.value),
    iconStart: /*#__PURE__*/React.createElement(Icon, {
      d: I.search,
      s: 14
    }),
    size: "sm",
    style: {
      minWidth: 240
    }
  }), /*#__PURE__*/React.createElement(Select, {
    size: "sm",
    value: estado,
    onChange: setEstado,
    options: [{
      value: "todos",
      label: "Todos los estados"
    }, {
      value: "corriente",
      label: "Al corriente"
    }, {
      value: "vencidos",
      label: "Vencidos"
    }],
    style: {
      minWidth: 170
    }
  }), /*#__PURE__*/React.createElement(Combobox, {
    size: "sm",
    value: sucursal,
    onChange: setSucursal,
    options: SUCURSALES.map(s => ({
      value: s,
      label: s
    })),
    placeholder: "Sucursal\u2026",
    style: {
      minWidth: 170
    }
  })), chips.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-2)",
      marginTop: 10,
      flexWrap: "wrap"
    }
  }, chips.map(c => /*#__PURE__*/React.createElement(Tag, {
    key: c.label,
    tone: "brand",
    onRemove: c.clear
  }, c.label)), /*#__PURE__*/React.createElement(Button, {
    variant: "link",
    size: "sm",
    onClick: () => {
      setQ("");
      setEstado("todos");
      setSucursal(null);
    }
  }, "Limpiar todo")), selected.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: "var(--space-3)",
      marginTop: 12,
      padding: "8px 14px",
      background: "var(--color-bg-selected)",
      border: "1px solid var(--color-border-default)",
      borderRadius: "var(--radius-md)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      font: "600 13px/1.3 var(--font-sans)",
      color: "var(--color-fg-brand)",
      fontVariantNumeric: "var(--numeric-tabular)"
    }
  }, selected.length, " seleccionado", selected.length === 1 ? "" : "s"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "sm",
    onClick: () => showToast(`${selected.length} créditos asignados a K. Ríos`)
  }, "Asignar"), /*#__PURE__*/React.createElement(Button, {
    variant: "secondary",
    size: "sm",
    onClick: () => showToast("Exportación de la selección iniciada")
  }, "Exportar selecci\xF3n"), /*#__PURE__*/React.createElement(Button, {
    variant: "destructive",
    size: "sm",
    onClick: () => setConfirm(true)
  }, "Cancelar cr\xE9ditos"), /*#__PURE__*/React.createElement(Button, {
    variant: "link",
    size: "sm",
    onClick: () => setSelected([])
  }, "Quitar selecci\xF3n")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(DataGrid, {
    label: "Cartera de cr\xE9ditos",
    height: 470,
    rows: filtered,
    rowKey: r => r.folio,
    selectable: true,
    selected: selected,
    onSelectionChange: setSelected,
    sortKey: sort.key,
    sortDir: sort.dir,
    onSort: (key, dir) => setSort({
      key,
      dir
    }),
    columns: [{
      key: "folio",
      header: "Folio",
      width: 104,
      sortable: true,
      render: r => /*#__PURE__*/React.createElement("span", {
        style: {
          font: "var(--text-code-sm)"
        }
      }, r.folio)
    }, {
      key: "cliente",
      header: "Cliente",
      sortable: true
    }, {
      key: "sucursal",
      header: "Sucursal",
      width: 118
    }, {
      key: "saldo",
      header: "Saldo",
      numeric: true,
      width: 140,
      sortable: true,
      render: r => mxn(r.saldo)
    }, {
      key: "proximo",
      header: "Próx. pago",
      width: 108,
      render: r => /*#__PURE__*/React.createElement("span", {
        style: {
          fontVariantNumeric: "tabular-nums"
        }
      }, r.proximo)
    }, {
      key: "atraso",
      header: "Estado",
      width: 132,
      sortable: true,
      render: r => r.atraso ? /*#__PURE__*/React.createElement(StatusIndicator, {
        status: "danger",
        label: dias(r.atraso),
        size: "sm"
      }) : /*#__PURE__*/React.createElement(StatusIndicator, {
        status: "success",
        label: "Al corriente",
        size: "sm"
      })
    }, {
      key: "_open",
      header: "",
      width: 52,
      render: r => /*#__PURE__*/React.createElement(IconButton, {
        icon: /*#__PURE__*/React.createElement(Icon, {
          d: I.chevR,
          s: 15
        }),
        label: `Abrir ${r.folio}`,
        size: "sm",
        onClick: () => {
          setOpenRow(r.folio);
          setTab("resumen");
        }
      })
    }]
  })))), /*#__PURE__*/React.createElement(Drawer, {
    open: !!row,
    onClose: () => setOpenRow(null),
    size: "lg",
    title: row ? `Crédito ${row.folio}` : "",
    footer: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      onClick: () => setOpenRow(null)
    }, "Cerrar"), /*#__PURE__*/React.createElement(Button, {
      variant: "primary",
      onClick: () => showToast("Estado de cuenta generado")
    }, "Estado de cuenta"))
  }, row && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10
    }
  }, row.atraso ? /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "danger",
    label: `Vencido · ${dias(row.atraso)} de atraso`
  }) : /*#__PURE__*/React.createElement(StatusIndicator, {
    status: "success",
    label: "Al corriente"
  }), /*#__PURE__*/React.createElement(Tag, {
    tone: "neutral"
  }, row.sucursal)), /*#__PURE__*/React.createElement(Alert, {
    severity: "warning",
    title: "Aprobaci\xF3n pendiente (maker-checker)",
    action: /*#__PURE__*/React.createElement("span", {
      style: {
        display: "inline-flex",
        gap: 8
      }
    }, /*#__PURE__*/React.createElement(Button, {
      size: "sm",
      variant: "secondary",
      onClick: () => showToast("Cambio aprobado · registrado en bitácora")
    }, "Aprobar"), /*#__PURE__*/React.createElement(Button, {
      size: "sm",
      variant: "tertiary",
      onClick: () => showToast("Cambio rechazado · registrado en bitácora")
    }, "Rechazar"))
  }, "J. Duarte solicit\xF3 aumentar el l\xEDmite a ", mxn(row.saldo + 20000), ". Requiere un segundo rol (t\xFA no puedes aprobar tus propias solicitudes)."), /*#__PURE__*/React.createElement(Tabs, {
    value: tab,
    onChange: setTab,
    items: [{
      id: "resumen",
      label: "Resumen"
    }, {
      id: "actividad",
      label: "Actividad",
      count: 4
    }]
  }, tab === "resumen" ? /*#__PURE__*/React.createElement(DescriptionList, {
    columns: 2,
    items: [{
      term: "Cliente",
      detail: row.cliente
    }, {
      term: "Saldo",
      detail: mxn(row.saldo)
    }, {
      term: "CLABE de dispersión",
      detail: /*#__PURE__*/React.createElement(MaskedValue, {
        value: "012180012345678912",
        label: "CLABE"
      })
    }, {
      term: "Próximo pago",
      detail: row.proximo
    }, {
      term: "Tasa anual",
      detail: "24.9% fija"
    }, {
      term: "CAT",
      detail: "32.4% sin IVA · v2026.2"
    }]
  }) : /*#__PURE__*/React.createElement(Timeline, {
    items: [{
      time: "hace 2 h",
      timeExact: "10 jul 2026, 07:04 CDMX",
      actor: "J. Duarte",
      action: "solicitó cambio de límite a",
      object: mxn(row.saldo + 20000),
      tone: "warning"
    }, {
      time: "30/06/2026",
      actor: "Sistema",
      action: "aplicó pago por",
      object: mxn(3450),
      tone: "success"
    }, {
      time: "28/06/2026",
      actor: "K. Ríos",
      action: "consultó la CLABE completa",
      detail: "Evento auditado",
      tone: "info"
    }, {
      time: "15/12/2025",
      actor: "M. López",
      action: "firmó el contrato",
      detail: "NOM-151 · sello 8F42…A1",
      tone: "neutral"
    }]
  })))), /*#__PURE__*/React.createElement(Modal, {
    open: confirm,
    onClose: () => setConfirm(false),
    role: "alertdialog",
    size: "sm",
    title: `¿Cancelar ${selected.length} crédito${selected.length === 1 ? "" : "s"}?`,
    footer: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Button, {
      variant: "secondary",
      "data-autofocus": true,
      onClick: () => setConfirm(false)
    }, "Conservar"), /*#__PURE__*/React.createElement(Button, {
      variant: "destructive",
      onClick: () => {
        setConfirm(false);
        showToast(`${selected.length} créditos enviados a cancelación`);
        setSelected([]);
      }
    }, "Cancelar cr\xE9ditos"))
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      marginTop: 0
    }
  }, "Se notificar\xE1 a cada cliente y la operaci\xF3n quedar\xE1 en la bit\xE1cora con tu usuario.", /*#__PURE__*/React.createElement("strong", null, " Esta acci\xF3n no se puede deshacer."))), /*#__PURE__*/React.createElement(CommandPalette, {
    open: palette,
    onClose: () => setPalette(false),
    onOpen: () => setPalette(true),
    recentIds: ["nueva-solicitud"],
    groups: [{
      header: "Acciones",
      items: [{
        id: "nueva-solicitud",
        label: "Crear solicitud de crédito",
        hint: "C"
      }, {
        id: "registrar-pago",
        label: "Registrar pago"
      }, {
        id: "exportar",
        label: "Exportar cartera (CSV)",
        onSelect: () => showToast("Exportación iniciada")
      }]
    }, {
      header: "Navegación",
      items: [{
        id: "go-creditos",
        label: "Ir a Créditos",
        hint: "G C",
        onSelect: () => setNav("creditos")
      }, {
        id: "go-cobranza",
        label: "Ir a Cobranza",
        keywords: "vencidos atraso",
        onSelect: () => setNav("cobranza")
      }, {
        id: "go-aprobaciones",
        label: "Ir a Aprobaciones",
        onSelect: () => setNav("aprobaciones")
      }]
    }, {
      header: "Expedientes",
      items: ROWS.slice(0, 3).map(r => ({
        id: r.folio,
        label: `${r.folio} · ${r.cliente}`,
        keywords: r.cliente,
        onSelect: () => setOpenRow(r.folio)
      }))
    }]
  }), /*#__PURE__*/React.createElement(ToastRegion, null, toast && /*#__PURE__*/React.createElement(Toast, {
    severity: "success",
    onDismiss: () => setToast(null)
  }, toast)));
}
ReactDOM.createRoot(document.getElementById("root")).render(/*#__PURE__*/React.createElement(OpsConsole, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/ops/OpsConsole.jsx", error: String((e && e.message) || e) }); }

// ui_kits/website/Homepage.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
function Icon({
  children
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: "28",
    height: "28",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, children);
}
const IconTarget = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("circle", {
  cx: "12",
  cy: "12",
  r: "9"
}), /*#__PURE__*/React.createElement("circle", {
  cx: "12",
  cy: "12",
  r: "5"
}), /*#__PURE__*/React.createElement("circle", {
  cx: "12",
  cy: "12",
  r: "1"
}));
const IconHeart = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("path", {
  d: "M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.6l-1-1a5.5 5.5 0 1 0-7.8 7.8l8.8 8.8 8.8-8.8a5.5 5.5 0 0 0 0-7.8Z"
}));
const IconDollar = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("path", {
  d: "M12 1v22M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"
}));
const IconUser = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("circle", {
  cx: "12",
  cy: "8",
  r: "4"
}), /*#__PURE__*/React.createElement("path", {
  d: "M4 21c0-4.4 3.6-7 8-7s8 2.6 8 7"
}));
const IconMail = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("rect", {
  x: "2",
  y: "4",
  width: "20",
  height: "16",
  rx: "2"
}), /*#__PURE__*/React.createElement("path", {
  d: "m3 6 9 7 9-7"
}));
const IconInstagram = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("rect", {
  x: "3",
  y: "3",
  width: "18",
  height: "18",
  rx: "5"
}), /*#__PURE__*/React.createElement("circle", {
  cx: "12",
  cy: "12",
  r: "4"
}), /*#__PURE__*/React.createElement("circle", {
  cx: "17.5",
  cy: "6.5",
  r: "0.6",
  fill: "currentColor"
}));
const IconFacebook = () => /*#__PURE__*/React.createElement(Icon, null, /*#__PURE__*/React.createElement("path", {
  d: "M14 9h3V6h-3a3 3 0 0 0-3 3v2H9v3h2v6h3v-6h3l1-3h-4V9a1 1 0 0 1 1-1Z"
}));
function Nav() {
  const {
    Button
  } = window.TandaMirandaDesignSystem_a4a6f5;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "center",
      justifyContent: "space-between",
      gap: 16,
      padding: "20px 5%"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo/tm-logo-horizontal-negro.svg",
    style: {
      height: 26
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 24,
      font: "var(--text-label-md)",
      letterSpacing: "var(--label-tracking)",
      color: "var(--tm-navy-900)"
    }
  }, /*#__PURE__*/React.createElement("a", {
    style: {
      borderBottom: "2px solid var(--tm-red-600)",
      paddingBottom: 6
    }
  }, "TU CR\xC9DITO"), /*#__PURE__*/React.createElement("a", null, "QUI\xC9NES SOMOS"), /*#__PURE__*/React.createElement("a", null, "CONTACTO")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 16,
      color: "var(--tm-red-600)"
    }
  }, /*#__PURE__*/React.createElement(IconMail, null), /*#__PURE__*/React.createElement(IconInstagram, null), /*#__PURE__*/React.createElement(IconFacebook, null)));
}
function Hero() {
  const {
    Button
  } = window.TandaMirandaDesignSystem_a4a6f5;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      padding: "64px 5% 80px",
      textAlign: "center",
      overflow: "hidden",
      background: "#f2efe9"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/photography/hands-car-protection.jpg",
    style: {
      position: "absolute",
      inset: 0,
      width: "100%",
      height: "100%",
      objectFit: "cover",
      opacity: 0.9
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      background: "linear-gradient(180deg, rgba(255,255,255,0.35), rgba(255,255,255,0.55))"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-display-lg)",
      color: "var(--tm-navy-900)"
    }
  }, "\xA1Este mes estrena auto!"), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-body-lg)",
      color: "var(--tm-navy-900)",
      marginTop: 12
    }
  }, "Descubre la forma m\xE1s f\xE1cil y segura", /*#__PURE__*/React.createElement("br", null), "de obtener un pr\xE9stamo.")));
}
function TrustSplit() {
  const {
    Button
  } = window.TandaMirandaDesignSystem_a4a6f5;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: "var(--tm-navy-900)",
      padding: "56px 5%",
      display: "flex",
      flexWrap: "wrap",
      alignItems: "center",
      gap: 40,
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/photography/office-meeting.jpg",
    style: {
      width: 220,
      height: 220,
      borderRadius: "50%",
      objectFit: "cover",
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 420,
      textAlign: "left"
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      font: "var(--text-h1)",
      color: "#fff",
      margin: 0
    }
  }, "La mejor tasa", /*#__PURE__*/React.createElement("br", null), "del mercado"), /*#__PURE__*/React.createElement("p", {
    style: {
      font: "var(--text-body-md)",
      color: "var(--color-text-inverse-muted)",
      marginTop: 16,
      marginBottom: 28
    }
  }, "Lorem ipsum dolor sit amet, consecteturelit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."), /*#__PURE__*/React.createElement(Button, {
    variant: "primary"
  }, "Aplica hoy mismo")));
}
function Benefits() {
  const {
    FeatureCard,
    SectionHeading
  } = window.TandaMirandaDesignSystem_a4a6f5;
  const items = [{
    icon: /*#__PURE__*/React.createElement(IconTarget, null),
    label: "Rapidez",
    description: "Lorem ipsum dolor sit amet, consectetur"
  }, {
    icon: /*#__PURE__*/React.createElement(IconHeart, null),
    label: "Comunidad",
    description: "Lorem ipsum dolor sit amet, consectetur"
  }, {
    icon: /*#__PURE__*/React.createElement(IconDollar, null),
    label: "Crédito",
    description: "Lorem ipsum dolor sit amet, consectetur"
  }, {
    icon: /*#__PURE__*/React.createElement(IconUser, null),
    label: "Asesoría",
    description: "Lorem ipsum dolor sit amet, consectetur"
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "64px 5%",
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement(SectionHeading, {
    title: "La \xFAnica tanda en la que",
    accent: "siempre eres el No.1"
  }), /*#__PURE__*/React.createElement("p", {
    style: {
      font: "var(--text-body-lg)",
      color: "var(--color-text-secondary)",
      marginTop: 16,
      marginBottom: 48
    }
  }, "Lorem ipsum dolor sit amet, consectetur adipiscing elit,", /*#__PURE__*/React.createElement("br", null), "sed do eiusmod tempor."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      justifyContent: "center",
      gap: 40
    }
  }, items.map(it => /*#__PURE__*/React.createElement(FeatureCard, _extends({
    key: it.label
  }, it)))));
}
function Capture() {
  const {
    Input,
    Button
  } = window.TandaMirandaDesignSystem_a4a6f5;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: "var(--color-bg-muted)",
      padding: "64px 5%",
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-display-md)",
      color: "var(--tm-navy-900)"
    }
  }, "\xA1Alcancemos juntos tus metas!"), /*#__PURE__*/React.createElement("p", {
    style: {
      font: "var(--text-body-md)",
      color: "var(--color-text-secondary)",
      marginTop: 12,
      marginBottom: 32
    }
  }, "Sed do eiusmod tempor incididunt ut labore", /*#__PURE__*/React.createElement("br", null), "et dolore magna aliqua."), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 12,
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement(Input, {
    placeholder: "Correo electr\xF3nico",
    style: {
      minWidth: 200
    }
  }), /*#__PURE__*/React.createElement(Button, null, "Enviar")));
}
function Footer() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: "var(--tm-navy-900)",
      color: "#fff",
      padding: "32px 5%",
      display: "flex",
      flexWrap: "wrap",
      gap: 20,
      alignItems: "center",
      justifyContent: "space-between"
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "../../assets/logo/tm-icon-rojo.svg",
    style: {
      height: 30
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-body-sm)",
      color: "var(--color-text-inverse-muted)"
    }
  }, "Calle 123, Ciudad, PA\xCDS 12345", /*#__PURE__*/React.createElement("br", null), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "#fff"
    }
  }, "contacto@tandamiranda.com")), /*#__PURE__*/React.createElement("div", {
    style: {
      font: "var(--text-body-sm)",
      color: "var(--color-text-inverse-muted)"
    }
  }, "\xA9 2024 Tanda Miranda. Todos los derechos reservados."));
}
function Homepage() {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(Nav, null), /*#__PURE__*/React.createElement(Hero, null), /*#__PURE__*/React.createElement(TrustSplit, null), /*#__PURE__*/React.createElement(Benefits, null), /*#__PURE__*/React.createElement(Capture, null), /*#__PURE__*/React.createElement(Footer, null));
}
window.Homepage = Homepage;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/website/Homepage.jsx", error: String((e && e.message) || e) }); }

__ds_ns.ButtonGroup = __ds_scope.ButtonGroup;

__ds_ns.CopyButton = __ds_scope.CopyButton;

__ds_ns.Fab = __ds_scope.Fab;

__ds_ns.IconButton = __ds_scope.IconButton;

__ds_ns.SegmentedControl = __ds_scope.SegmentedControl;

__ds_ns.SpeedDial = __ds_scope.SpeedDial;

__ds_ns.ToggleGroup = __ds_scope.ToggleGroup;

__ds_ns.Badge = __ds_scope.Badge;

__ds_ns.Button = __ds_scope.Button;

__ds_ns.FeatureCard = __ds_scope.FeatureCard;

__ds_ns.Input = __ds_scope.Input;

__ds_ns.SectionHeading = __ds_scope.SectionHeading;

__ds_ns.Accordion = __ds_scope.Accordion;

__ds_ns.AnchorBadge = __ds_scope.AnchorBadge;

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Card = __ds_scope.Card;

__ds_ns.Carousel = __ds_scope.Carousel;

__ds_ns.BarChart = __ds_scope.BarChart;

__ds_ns.LineChart = __ds_scope.LineChart;

__ds_ns.DonutChart = __ds_scope.DonutChart;

__ds_ns.Charts = __ds_scope.Charts;

__ds_ns.Clamp = __ds_scope.Clamp;

__ds_ns.DataGrid = __ds_scope.DataGrid;

__ds_ns.DescriptionList = __ds_scope.DescriptionList;

__ds_ns.Divider = __ds_scope.Divider;

__ds_ns.Image = __ds_scope.Image;

__ds_ns.List = __ds_scope.List;

__ds_ns.ListItem = __ds_scope.ListItem;

__ds_ns.OrgChart = __ds_scope.OrgChart;

__ds_ns.PageHeader = __ds_scope.PageHeader;

__ds_ns.StatTile = __ds_scope.StatTile;

__ds_ns.StatusIndicator = __ds_scope.StatusIndicator;

__ds_ns.Table = __ds_scope.Table;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.Timeline = __ds_scope.Timeline;

__ds_ns.TreeView = __ds_scope.TreeView;

__ds_ns.Typography = __ds_scope.Typography;

__ds_ns.AmountInput = __ds_scope.AmountInput;

__ds_ns.CatDisclosure = __ds_scope.CatDisclosure;

__ds_ns.ClabeInput = __ds_scope.ClabeInput;

__ds_ns.MaskedValue = __ds_scope.MaskedValue;

__ds_ns.OtpInput = __ds_scope.OtpInput;

__ds_ns.Alert = __ds_scope.Alert;

__ds_ns.Backdrop = __ds_scope.Backdrop;

__ds_ns.EmptyState = __ds_scope.EmptyState;

__ds_ns.ProgressBar = __ds_scope.ProgressBar;

__ds_ns.ProgressRing = __ds_scope.ProgressRing;

__ds_ns.Skeleton = __ds_scope.Skeleton;

__ds_ns.Spinner = __ds_scope.Spinner;

__ds_ns.Toast = __ds_scope.Toast;

__ds_ns.ToastRegion = __ds_scope.ToastRegion;

__ds_ns.Checkbox = __ds_scope.Checkbox;

__ds_ns.Combobox = __ds_scope.Combobox;

__ds_ns.DateInput = __ds_scope.DateInput;

__ds_ns.DatePicker = __ds_scope.DatePicker;

__ds_ns.Field = __ds_scope.Field;

__ds_ns.FileUpload = __ds_scope.FileUpload;

__ds_ns.NumberInput = __ds_scope.NumberInput;

__ds_ns.RadioGroup = __ds_scope.RadioGroup;

__ds_ns.Rating = __ds_scope.Rating;

__ds_ns.Select = __ds_scope.Select;

__ds_ns.Slider = __ds_scope.Slider;

__ds_ns.Switch = __ds_scope.Switch;

__ds_ns.Textarea = __ds_scope.Textarea;

__ds_ns.TimePicker = __ds_scope.TimePicker;

__ds_ns.TransferList = __ds_scope.TransferList;

__ds_ns.AppBar = __ds_scope.AppBar;

__ds_ns.BottomNav = __ds_scope.BottomNav;

__ds_ns.Breadcrumbs = __ds_scope.Breadcrumbs;

__ds_ns.CommandPalette = __ds_scope.CommandPalette;

__ds_ns.Link = __ds_scope.Link;

__ds_ns.MegaMenu = __ds_scope.MegaMenu;

__ds_ns.Menu = __ds_scope.Menu;

__ds_ns.Pagination = __ds_scope.Pagination;

__ds_ns.ScrollProgress = __ds_scope.ScrollProgress;

__ds_ns.SideNav = __ds_scope.SideNav;

__ds_ns.Stepper = __ds_scope.Stepper;

__ds_ns.Tabs = __ds_scope.Tabs;

__ds_ns.Coachmark = __ds_scope.Coachmark;

__ds_ns.Drawer = __ds_scope.Drawer;

__ds_ns.Lightbox = __ds_scope.Lightbox;

__ds_ns.Modal = __ds_scope.Modal;

__ds_ns.Popover = __ds_scope.Popover;

__ds_ns.Tooltip = __ds_scope.Tooltip;

})();
